//! Capture stub.

pub struct Capture;

impl Capture {
    pub fn new() -> Self {
        Self
    }

    pub fn run(&self) -> Result<(), anyhow::Error> {
        todo!("M10: implement capture")
    }
}
