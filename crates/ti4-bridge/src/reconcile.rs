//! Reconcile stub.

pub struct Reconcile;

impl Reconcile {
    pub fn new() -> Self {
        Self
    }

    pub fn run(&self) -> Result<(), anyhow::Error> {
        todo!("M11: implement reconcile")
    }
}
