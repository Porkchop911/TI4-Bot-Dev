# Evidence — OBS-008h2, action-card-count content consequence surface

See `plans/OBS-008H2_ACTION_CARD_COUNT_CONSEQUENCE_SURFACE.md` for design; this records checks.

## Changed paths

- `crates/ti4-engine/src/action_cards.rs` — `enforce_hand_limit`'s discard options attach an exact
  preview.
- `crates/ti4-engine/src/relics.rs` — The Codex's take options attach an exact preview.
- `crates/ti4-policy/src/features.rs` — `content_decision_features` gains a fourth preview
  quantity mapping (`ActionCardsHeld`).
- `plans/OBS-008H2_ACTION_CARD_COUNT_CONSEQUENCE_SURFACE.md`, `plans/evidence/OBS-008H2.md`,
  `plans/EXECUTION_STATE.md`.

## Focused evidence

- `action_cards.rs::obs008h2_discard_over_hand_limit_previews_the_exact_hand_loss`: an 8-card hand
  (one over the printed 7) previews the discard option `8 -> 7`.
- `relics.rs::obs008h2_codex_previews_the_exact_action_card_gain`: an empty hand with one
  discarded card previews the take option `0 -> 1`.
- `features.rs::obs008h2_action_card_count_previews_reach_the_policy`: both map to
  `content:action-cards-after` (7.0 for the discard, 1.0 for the take), and `-change` survives
  `projection::admits`.

## Tests and checks

| command | result |
|---|---|
| `cargo test -p ti4-engine --lib obs008h2` | 2 passed |
| `cargo test -p ti4-engine --lib action_cards::` | 101 passed (100 pre-existing + 1 new) |
| `cargo test -p ti4-engine --lib relics::` | 28 passed (27 pre-existing + 1 new) |
| `cargo test -p ti4-policy --lib obs008h2` | 1 passed |
| `cargo test -p ti4-engine` | 1,204 lib (1,202 + 2 new) + 4 integration + 5 doc passed |
| `cargo test -p ti4-policy --lib` | 229 passed (228 + 1 new, incl. the 102-game campaign, no
  regression) |
| `cargo test -p ti4-training` | 133 passed |
| `cargo clippy -p ti4-engine -p ti4-policy --all-targets -- -D warnings` | clean |
| `rustfmt --edition 2024 --check` (scoped files) + `git diff --check` | clean |

## Independent Tier-C review

OUTSTANDING (deferred per standing repo convention; reviewed elsewhere per user confirmation this
session). `expedition_discard_action_card`/`expedition_discard_secret`/
`return_over_secret_hand_limit` are the same shape for secret objectives but need a new
`SecretObjectivesHeld` quantity, left for a future package. The remaining `content` subtypes stay
without a preview.
