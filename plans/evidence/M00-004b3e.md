# M00-004b3e — tools/tech_census.py Entry Point Inventory

## Scope
`tools/tech_census.py` (134 lines). Technology research census by faction.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/tech_census.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\tech_census.py
  → 134
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\tech_census.py
  → 6 matching lines (1 private function + 1 public function + 1 class-like function + 2 module constants + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\tech_census.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main(argv)` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main(argv: Iterable[str] | None) -> int` |
| Kind | Module-level function |
| Source line | 95 |
| Construction role | Parses args via `argparse.ArgumentParser` with `RawDescriptionHelpFormatter`, loads champions/profiles from JSON file, calls `census()` to replay games and count researched technologies per faction, prints per-faction technology hold rates with labelled names, optionally writes JSON to `--out`. Returns exit code 0. |

### 2. `census()` — Census computation helper
| Field | Value |
|---|---|
| Symbol | `census(profiles, factions, *, seeds, rounds, workers, first_seed) -> dict` |
| Kind | Module-level function |
| Source line | 63 |
| Construction role | Generates game tasks over seeds×rotations, runs parallel via `ProcessPoolExecutor` with initializer `_start` (sets global _PROFILES/_FACTIONS), calls `_play()` per task, aggregates per-faction technology counters, returns dict with games/per_faction_games/held/mean_count. |

### 3. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 134 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--champions` | Path | **required** | Champions file holding a champions table |
| `--seeds` | int | 6 | Number of seeds to simulate |
| `--rounds` | int | 4 | Game rounds to simulate |
| `--workers` | int | 4 | Parallel worker count (low default) |
| `--first-seed` | int | 71_000_000 | Starting seed |
| `--out` | Path | `None` | Optional output report path |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–17 | Documentation, not code |
| `ROOT` | module constant | 21 | Module-level path constant (sys.path manipulation) |
| `_PROFILES` | module constant | 25 | Global state for ProcessPoolExecutor initializer |
| `_FACTIONS` | module constant | 25 | Global state for ProcessPoolExecutor initializer |
| `_start` | private function | 28 | Private function (leading underscore); initializer for parallel workers |
| `_play` | private function | 33 | Private function (leading underscore); per-task game simulation |
| `label` | nested function | 107 | Nested function inside `main()`, not top-level |
| `__future__` import | import | 18 | Future annotations import |
| `argparse` import | import | 20 | Stdlib import |
| `collections` import | import | 21 | Stdlib import |
| `concurrent.futures` import | import | 22 | Stdlib import |
| `json` import | import | 23 | Stdlib import |
| `sys` import | import | 24 | Stdlib import |
| `Path` import | import | 25 | Stdlib import |
| `Iterable` import | import | 26 | Stdlib import |
| `bots` import | import | 29 | External module from `engine`, not constructed here |
| `sim` import | import | 29 | External module from `engine`, not constructed here |
| `technology` import | import | 29 | External module from `engine`, not constructed here |
| `apply_profile` import | import | 30 | External function from `tools.full_sol_policy`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (1 entry point function + 1 census helper function) |
| Excluded top-level definitions | 19 (docstrings + module constants + private functions + nested function + stdlib imports + type imports) |
| Source lines covered | 1–134 |
| Discovery matching lines | 6 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/tech_census.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b3.
