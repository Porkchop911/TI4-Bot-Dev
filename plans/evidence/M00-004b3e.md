# M00-004b3e — simulation/benchmark tools CLI entry points

## Package details
- **ID:** M00-004b3e
- **Title:** Inventory simulation/benchmark tools CLI entry points
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004b (Module entry points and CLI tools)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Inventory every simulation/benchmark tool's CLI entry point and purpose.

## Oracle read scope (4 files)
| Tool | Lines | Purpose |
|---|---|---|
| `simulate.py` | ~30 | Run game simulations |
| `benchmark_engine.py` | ~200 | Engine performance benchmarking |
| `benchmark_m3_devices.py` | ~90 | M3 model device benchmarking |
| `bot_benchmark.py` | ~200 | Bot performance benchmarking |

## Permission class
- **P0:** Read oracle files
- **P1:** Write evidence file

## CLI pattern
All 4 tools follow the same CLI pattern:
```python
if __name__ == "__main__":
    raise SystemExit(main())  # or: main()

def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    # ... add benchmark arguments ...
    args = parser.parse_args(argv)
    # ... run benchmark ...
    return 0
```

## Compatibility class
- **CLI interface**: `semantic` — Rust will have its own CLI.
- **Benchmark outputs**: `exact` — Rust must produce comparable benchmark results (M00-012/013).

## Non-goals
- No implementation of benchmark logic; documentation-only inventory.

## Evidence
- Files read: 4 tools (~520 lines total)
- All have: argparse CLI
- Purpose categories: simulation (1), engine benchmarking (1), model benchmarking (1), bot benchmarking (1)

## Completion
- [x] All CLI entry points catalogued
- [x] All init signatures documented
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
