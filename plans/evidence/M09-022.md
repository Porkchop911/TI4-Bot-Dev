# M09-022 — Ability decomposition policy features

**Package:** `plans/M09-022_ABILITY_DECOMPOSITION_FEATURES.md`
**Branch:** `wp/m09-022-ability-decomposition-features`
**Base:** `dc1fe49` (M09-021 closed)
**Author:** Claude Opus 5
**Normative source:** `docs/MLP_PLAN.md` revision 5 §5.3, §4.1, decision D8
**Permission class:** P1 — no scoped access, no downloads, no generated artifacts.

## Authorship and review

Claude Opus 5 reviewed M08-017 through M09-021 and is the author of this package. It is therefore
**not** eligible to review it. The independent-review seat for M09-022 is open; the package is not
done until that review is resolved (standard, "the implementer must not be the only reviewer of its
own patch").

## Changed paths

| path | net |
|---|---|
| `crates/ti4-policy/src/features.rs` | +1 fact builder, +1 predicate, wiring at 2 context sites and 3 emission sites, +6 focused tests, 2 pins updated |

No engine edits. No change to the legacy hashed extractor, scoring, legality, or replay.

## Corpus measurement this package rests on

Measured against the embedded corpus at `DEFAULT` (= `FULL`) before implementation, and pinned by
test afterwards.

| quantity | measured |
|---|---|
| faction records at `DEFAULT` | 34 |
| records failing the selectable-seat predicate | 1 (`neutral`) |
| **selectable seats** | **33** |
| distinct under abilities only | 32 / 34 — collision `keleresa = keleresm = keleresx` |
| + starting tech | 32 / 34 — same |
| + faction tech | 32 / 34 — same |
| **+ starting fleet, home planets, commodities** | **34 / 34 — none** |

Two deviations from §5.3 as written, both recorded rather than silently absorbed:

1. **§5.3 says 33 playable seats; the corpus holds 34 faction records.** The extra one is
   `neutral`, the Thunder's Edge neutral-units record: empty `homeSystem`, empty `startingFleet`,
   empty `homePlanets`, no abilities, no leaders, no promissory notes, zero commodities, and none
   of the playable-seat fields (`complexity`, `preferredColours`, `priorityNumber`, `wikiURL`).
   Excluding it gives exactly 33. §5.3's number is correct about seats; it simply predates this
   record or never counted it.
2. **The exclusion is a predicate, not a name.** `is_selectable_seat` tests for a non-empty
   `homeSystem`. Exactly one record fails it today — verified independently against
   `startingFleet` and `homePlanets`, which are empty for the same single record and no other. A
   deny-list naming `neutral` would let the next non-seat record through in silence.

The separation structure §5.3 reports is otherwise reproduced exactly, including the single Keleres
collision and the row that resolves it.

## What was implemented

`ability_facts(seen, player)` emits six families for the acting seat only:

| family | value | source |
|---|---|---|
| `ability:<alias>` | 1.0 | `abilities` |
| `faction-start-tech:<alias>` | 1.0 | `startingTech` |
| `faction-tech:<alias>` | 1.0 | `factionTech` |
| `faction-start-unit:<unit>` | count | `startingFleet`, parsed via `Faction::deployments` |
| `faction-home:<planet>` | 1.0 | `homePlanets` |
| `faction-commodities` | value | `commodities` |

Emission follows the structure F-M09-021-2 settled: bare names on **every option under every
crossing mode** including `StateCross::None` (the §4.1 nonlinear-trunk contract), plus crossed
copies under `state-kind:` / `state-option:` for linear-schema delivery. The two namespaces are
disjoint by construction. Facts are computed once per choice in `ChoiceContext`, not per option —
the fleet string is parsed once rather than thirty-odd times on an activation choice.

The six families were added to `EXPLICIT_FIXED_FAMILIES` (27 → 33), a reviewed extension of the
closed grammar. Every legacy name is unchanged.

## Invariant 2 — the active content domain

The faction record resolves through `seen.content()` and `seen.sources()`. It does **not** call
`ContentStore::embedded()` and does not name a scope. This is the M08-019 Y2 defect class
(`annexable()` reading the compiled-in corpus while taking a live position) and the M09-021 AA1
class; `ability_facts_follow_the_active_source_scope` exists so a regression fails a test rather
than passing every one. `bastion` is a Thunder's Edge seat: in scope under `DEFAULT`, out of scope
under `POK`, so a hardcoded scope of either value fails exactly one of the two assertions.

The **store** half of the invariant is currently argued from the call rather than proven by a
second store; see Limitations.

## Tests added

| test | what it pins |
|---|---|
| `the_selectable_seat_predicate_excludes_exactly_the_neutral_record` | 34 records, 33 seats, `neutral` the only exclusion |
| `ability_decomposition_separates_every_selectable_seat` | 33 distinct decompositions, zero collisions, built from **emitted** features |
| `keleres_variants_separate_only_on_the_last_row` | §5.3's collision row, both halves, with a non-emptiness precondition |
| `unseen_factions_contribute_nothing` | a Sol seat names no Hacan-only fact; asserts the fixture is non-vacuous first |
| `ability_facts_follow_the_active_source_scope` | invariant 2, scope half |
| `ability_facts_survive_state_cross_none` | §4.1 contract: all five keyed families present on every option, no crossed copy, option-order deterministic |

Every separation and survival assertion carries a non-emptiness precondition, so none of them can
pass by measuring nothing.

## Gates

```
cargo test -p ti4-policy --lib      132 passed, 0 failed   (126 before this package)
cargo test --workspace             1375 passed, 0 failed   (1369 before this package)
cargo clippy -p ti4-policy --all-targets
    no warning in ti4-policy; one pre-existing ti4-engine warning (strategy.rs:589)
rustfmt --edition 2024 --check crates/ti4-policy/src/features.rs   clean
git diff --check                   clean
```

The formatting pass was checked for collateral: the 56 changed lines are all inside code this
package added. No pre-existing drift elsewhere in the file was absorbed (the M09-021 O-M09-021-2
trap).

## Pins updated, with rationale

- `the_legacy_subvector_is_pinned_against_the_recorded_baseline` — the exclusion filter now uses
  `is_post_baseline_family`, covering M09-021's five objective families and M09-022's six
  decomposition families, in both bare and crossed form. The helper matches on the family segment
  (`:{family}`) rather than a loose substring, so the legacy `kind-faction:` and `option-faction:`
  channels — which contain `-faction:` but never `:faction-` — stay on the pinned side. **No pinned
  legacy value moved**; the test asserts that directly.
- `m09_019b_feature_inventory_is_pinned` — `EXPLICIT_FIXED_FAMILIES` extended 27 → 33 with the
  rationale recorded in the test comment.

## Behavioral baseline

No M08-021 re-baseline triggered. The authored bot (`ScoredBot`) does not use the explicit feature
path, and no engine behavior changed. Not re-measured; if a reviewer disagrees that the authored
path is untouched, the batch is cheap to run.

## Limitations — stated, not hidden

1. **The store half of invariant 2 is not proven by test.** The scope half is. Proving the store
   half needs a second `ContentStore` built by `from_dir` over an edited corpus copy; `from_dir`
   reads a `manifest.json` alongside the JSON, so whether an edited copy loads was not established
   here. Until it is, "resolves through the active store" rests on reading the call, which is
   exactly the standard of evidence M08-019 Y1 showed to be insufficient. Recorded as an open item
   rather than claimed as verified.
2. **Extraction cost was not measured.** `ability_facts` parses the starting fleet once per choice.
   M09-021 established that extraction is already several times the engine's per-decision cost on a
   feature-heavy fixture, so this is not obviously negligible and no claim is made that it is.
   M09-029's gate is where it would show up.
3. **Emitted-feature separation is measured on a one-option fixture** per seat. The decomposition
   is option-invariant by construction, and `ability_facts_survive_state_cross_none` checks the
   multi-option case separately, but the 33-way separation itself was not re-measured across
   crossing modes.

## Open items

| ID | Severity | Item |
|---|---|---|
| O-M09-022-1 | LOW | Invariant 2's store half is argued, not tested (Limitation 1). Either add the `from_dir` test or record the reason it cannot be written. |
| O-M09-022-2 | INFO | Per-choice fleet parsing is unmeasured (Limitation 2). |

## Status

Implementation complete; gates green. **Pending independent review** — which the author cannot
supply. M09-023 does not depend on this package and may start in parallel; M09-024 depends on rows
019–023 and stays blocked until both land.

## Independent Tier-B review of `26ad269` (2026-08-25)

**Verdict: changes required.** The implementation and focused behavior are sound, but the package
does not satisfy its own predeclared active-store acceptance test. Both code and evidence admit that
only the source-scope half is tested; the required alternate `ContentStore::from_dir` regression is
absent. This is F-M09-022-1 (MEDIUM) in `plans/M09-022_OPEN_REVIEW_ITEMS.md`.

Independent results: ability-focused **3/0**; selectable predicate **1/0**; Keleres separation
**1/0**; legacy and inventory pins **1/0** each; combined policy suite **135/0**; scoped Clippy has
no policy warning; rustfmt/diff-check clean. M09-022 remains open pending the alternate-store test
and a narrow recheck.

## F-M09-022-1 correction — alternate-store regression (implementer, 2026-08-25)

The review is right, and the finding is against the package's own predeclared acceptance test. The
evidence recorded the gap honestly as O-M09-022-1, but recording a gap is not the same as closing
one, and "resolves through the active store" was left resting on code inspection — the exact
standard M08-019 Y1 established as insufficient.

### What was added

`ability_facts_use_the_active_content_store`. A second corpus is built through
`ContentStore::from_dir`, one decomposition field is changed in it, and the emitted vector is
asserted to follow the store the observation carries.

- **Fixture:** the embedded corpus directory (30 files, **992 KiB**) copied into the OS temp
  directory under `ti4-m09-022-store-<pid>-<value>`; `factions.json` rewritten so `sol`'s
  `commodities` reads **9** against the corpus's real **4**.
- **Cleanup:** an `EditedCorpus` guard with a `Drop` implementation that removes the directory,
  so it is cleaned on panic as well as on success (`Drop` runs while unwinding). Verified after the
  gate run: **0** directories matching the fixture name remain. Nothing is generated into the repo
  and nothing is committed.
- **Both directions asserted:** the alternate arm reports 9, the embedded arm reports 4, and the
  two are asserted to disagree — so the test cannot pass by both arms reading the same store.
- **Non-degeneracy:** the alternate store must produce abilities at all, and the set of facts
  differing between the two stores must be **exactly** `["faction-commodities"]`. An empty or
  broken alternate corpus fails both. This is what stops the assertion passing for the wrong
  reason.

### Falsification check — the test was proved to catch the defect it exists for

A passing test is not evidence until it can fail. `ability_facts` was temporarily mutated to
`let content = ti4_content::ContentStore::embedded();` — the exact defect the finding names — and
the focused set re-run:

```
test features::tests::ability_facts_use_the_active_content_store ... FAILED
    assertion `left == right` failed: the emitted fact did not follow the alternate store
test result: FAILED. 2 passed; 1 failed; 0 ignored; 133 filtered out
```

Two points in that output. The new test **fails** on the mutant, so it detects the defect. And the
other two `ability_facts_*` tests — including `ability_facts_follow_the_active_source_scope` —
**pass** on the mutant, which confirms the review's central claim: the source-scope test covers a
genuinely different half of the invariant and could never have caught this. The mutation was
reverted and the file re-formatted; the suite is green on the reverted tree.

### Gates after the correction

```
cargo test -p ti4-policy --lib      136 passed, 0 failed   (135 before)
cargo test --workspace             1379 passed, 0 failed   (1378 before)
cargo clippy -p ti4-policy --all-targets
    no warning in ti4-policy; one pre-existing ti4-engine warning
rustfmt --edition 2024 --check crates/ti4-policy/src/features.rs   clean
git diff --check                   clean
temp fixture directories remaining after the run   0
```

### Disposition

**O-M09-022-1 is closed** — the store half of invariant 2 is now proven by a test that has been
shown to fail without the property. O-M09-022-2 (per-choice fleet parsing) remains accepted and
deferred to M09-029, per the review.

Requesting the narrow independent Tier-B recheck. M09-022 remains open until it lands; M09-023 is
already accepted for its delta, and M09-024 unblocks once the combined frontier passes its overlap
recheck.

## Narrow independent Tier-B recheck of `b444f52` (2026-08-25)

**Accepted.** F-M09-022-1 is resolved: the alternate-store regression exercises a valid modified
`ContentStore::from_dir` corpus, proves both alternate and embedded values, is non-degenerate, and
has recorded falsification evidence against the exact embedded-store defect.

Independent results: active-store **1/0**; source-scope **1/0**; M09-023 overlap **3/0**; legacy and
inventory pins **1/0** each; scoped Clippy clean for policy code; rustfmt/diff-check clean; temporary
fixture directories remaining **0**. M09-022 is complete and M09-024 is dependency-ready.
