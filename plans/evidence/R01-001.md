# R01-001 evidence — ReviewBundle v1 contract

## Scope

Documentation-only package defining an operator-authorized independent game-review tooling track.
No Rust, training, simulator, bridge, or historical-Python file was modified or executed.

## Permission declaration

- Permission class: P1.
- Writable paths: `plans/R01_REVIEW_VIEWER.md`, this evidence file, `plans/INDEX.md`, and
  `plans/EXECUTION_STATE.md`.
- External reads: Pi session path explicitly authorized by the operator.
- Network/processes/ports/artifacts/destructive actions/external state: none.

## Sources inspected

- Pi session `2026-08-19T11-26-54-269Z_01a019c6-313c-7c2e-9f35-9cde967d20f3`, message 33.
- `crates/ti4-engine/src/choice.rs` (`DecisionRecord`/`DecisionLog`).
- `crates/ti4-engine/src/event.rs` (stable typed event representation).
- `crates/ti4-sim/src/replay.rs` (still a replay-recording/playback stub).
- `crates/ti4-model/src/view.rs` (current redaction model).
- `plans/M03_CHOICE_TIMING_REPLAY.md`, `plans/M10_SIMULATION_AND_TRAINING.md`, and scoped
  permissions.

## Findings incorporated

- The viewer must not depend on training and must not gate M10.
- Decision logging exists but replay playback remains unimplemented, so R01 capture cannot claim
  replay fidelity until the M03 replay boundary is accepted.
- A self-contained page cannot merely hide secrets client-side; artifacts are audience-specific.
- No HTML server/live-watch capability belongs in the MVP.

## Tier-C review and correction

The first independent Tier-C pass returned changes required. It identified five boundary defects:
the original draft lacked a closed audience/privacy matrix, an implementable JSON grammar and
checksum scope, operational input/render limits, unambiguous timeline/frame semantics, and it
incorrectly implied aggregate M03 replay/state-hash support. The revised contract now uses one
canonical, uncompressed JSON file; full projected-state frames; a typed projection-only export
boundary; exact closed fields and error codes; and an explicit checksum-not-authentication claim.
It contains no raw seed, entropy stream, engine record ID, trace event ID, or raw private digest.

Independent Tier-C re-review by Mendel accepted the corrected contract with no actionable findings.
It confirmed the closed audience projection, strict canonical single-file schema and checksum
scope, fixed validation/render limits, full-frame/timeline continuity, and correct deferral of any
M03 replay fidelity claim. No implementation has started.

## Checks

`git diff --check` passed after the correction. This documentation package has no executable
acceptance test. The independent Tier-C re-review accepted the schema boundary, audience
separation, deterministic rendering DTO, and resource bounds; R01-002 is the next safe package.
