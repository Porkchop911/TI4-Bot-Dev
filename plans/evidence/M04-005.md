# M04-005 — Strategy-card draft

## Package details
- **ID:** M04-005
- **Title:** Strategy-card draft
- **Milestone:** M04 — Game skeleton
- **Package:** M04-005
- **Dependencies:** M04-004 ✅

## Permission class
- **P1:** Write evidence file

## Strategy-card draft (from `engine/game.py`)

### Strategy card set selection
| Source | Alias | Cards |
|---|---|---|
| `thunders_edge` | `te` | TE set |
| `pok` | `pok` | PoK set |
| `base` | `base_game` | Base set |

- `STRATEGY_CARD_SETS` ordered by priority
- First match wins (source in sources → use that set)
- Always 8 strategy cards total

### Cards per player
| Player count | Cards per player |
|---|---|
| 3, 4 | 2 |
| 2, 5, 6 | 1 |

### Pick order
| Player count | Pick order |
|---|---|
| 3p | speaker → A → B → B → A → speaker (snake) |
| 4p | speaker → A → B → C → C → B → A → speaker (snake) |
| 2p | speaker → A (single lap) |
| 5p | speaker → A → B → C → D → D → C → B → A → speaker (snake) |
| 6p | speaker → A → B → C → D → E → E → D → C → B → A → speaker (snake) |

### Snake order logic
```
seats = clockwise_from(speaker)
for lap in range(cards_per_player):
    if lap % 2 == 0: picks.extend(seats)
    else: picks.extend(seats[::-1])
```

### Legal picks
- Only unclaimed strategy cards available
- Speaker picks first in each lap
- Each player picks exactly `cards_per_player` cards

### Deterministic choices
- `Scripted(option_ids)` for conformance tests
- `SeededRandom(seed)` for smoke tests
- `FirstOption` for default (always first available)

## M04-005 acceptance
- [x] Strategy card set selection documented
- [x] Cards per player documented (2 cases)
- [x] Pick order documented (snake logic)
- [x] Legal picks documented
- [x] Deterministic choices documented

## M04-005 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
