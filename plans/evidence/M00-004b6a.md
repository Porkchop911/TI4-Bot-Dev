# M00-004b6a — tools/compare.py Entry Point Inventory

## Scope
`tools/compare.py` (106 lines). Captured TTS board vs engine state divergence report.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/compare.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\compare.py
  → 106
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\compare.py
  → 5 matching lines (1 module constant dict + 1 public function + 1 public function + 1 public function + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\tai4-engine\tools\compare.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main()` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main()` |
| Kind | Module-level function |
| Source line | 69 |
| Construction role | Parses args via `argparse.ArgumentParser`, iterates capture JSON files, calls `fresh_engine_state()` to build baseline engine state, decodes `hs.decode()` per file, calls `rc.compare()` + `rc.planet_attribution_agrees()`, prints system agreement count / planet alignment / divergence list / controlled planet count, optionally calls `planet_report()` for `--planets`, returns 0 (aligned) or 1 (divergence). |

### 2. `fresh_engine_state()` — Engine state builder
| Field | Value |
|---|---|
| Symbol | `fresh_engine_state()` |
| Kind | Module-level function |
| Source line | 38 |
| Construction role | Creates `seated_game` from `SEATS` dict with `Table(default=FirstOption())`, advances via `game.step()` until `Phase.STRATEGY`, returns `game.state`. Not a CLI entry point; helper for baseline state. |

### 3. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 106 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `captures` | Path (positional, nargs+) | **required** | One or more capture JSON files |
| `--planets` | flag | False | Report per-planet placement |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–23 | Documentation, not code |
| `SEATS` | module constant | 27–34 | Dict mapping faction aliases to mod colours |
| `planet_report` | public function | 38 | Public function but not a CLI entry point; report helper |
| `__future__` import | import | 25 | Future annotations import |
| `argparse` import | import | 27 | Stdlib import |
| `json` import | import | 28 | Stdlib import |
| `sys` import | import | 29 | Stdlib import |
| `Path` import | import | 30 | Stdlib import |
| `hexsummary` import | import | 32 | External module from `bridge`, not constructed here |
| `reconcile` import | import | 32 | External module from `bridge`, not constructed here |
| `factions` import | import | 33 | External module from `engine`, not constructed here |
| `galaxy` import | import | 33 | External module from `engine`, not constructed here |
| `FirstOption` import | import | 34 | External class from `engine.choice`, not constructed here |
| `Table` import | import | 34 | External class from `engine.choice`, not constructed here |
| `seated_game` import | import | 35 | External function from `engine.game`, not constructed here |
| `Phase` import | import | 36 | External class from `engine.timing`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (1 entry point function + 1 state builder function) |
| Excluded top-level definitions | 17 (docstrings + module constant + helper function + stdlib imports + type imports) |
| Source lines covered | 1–106 |
| Discovery matching lines | 5 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/compare.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b6.
