# M00-004a28 — engine/ml/rollout.py interface inventory

## Package details
- **ID:** M00-004a28
- **Title:** Inventory rollout.py public construction APIs
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004a (Interface inventory — public construction APIs)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Catalogue every public constructor, factory, and class in `engine/ml/rollout.py`.

## Oracle source
- **Path:** `D:\Projects\ti4-engine\engine\ml\rollout.py`
- **Lines:** ~350 (including blank lines and comments)
- **Oracle commit:** `37061c511a4780d4c0719e0342533a498cd4b457`

## Allowed Rust paths
- `plans/evidence/M00-004a28.md` (evidence file)

## Permission class
- **P0:** Read oracle file
- **P1:** Write evidence file

## Public API inventory

### Class (exception)
| Name | Bases | Line |
|---|---|---|
| `ExpectedChoiceMismatch` | `IllegalChoice` | ~17 |

### Private functions
| Name | Signature | Returns | Line |
|---|---|---|---|
| `_canonical` | `(value: Any) -> Any` | canonicalized value | ~22 |
| `choice_fingerprint` | `(choice: Choice) -> str` | SHA-256 hex string | ~38 |
| `state_fingerprint` | `(state: GameState) -> str` | SHA-256 hex string | ~52 |
| `decision_fingerprint` | `(state: GameState, choice: Choice) -> str` | SHA-256 hex string | ~66 |
| `fork_game` | `(game: Game) -> Game` | deep-copied Game | ~74 |
| `_prepare_future_rng` | `(game: Game, continuations: Mapping[str, Decider], bundle: RolloutSeedBundle) -> None` | None | ~318 |

### Class
| Name | Key attributes/methods | Line |
|---|---|---|
| `ForceOnceDecider` | `__init__(expected_fingerprint: str, option_id: str, continuation: Decider, *, state_provider: Callable[[], GameState])`, `choose(choice: Choice) -> Option` | ~86 |

### Constants
| Name | Type | Value | Line |
|---|---|---|---|
| `RNG_DOMAINS` | `tuple[str, ...]` | `("bot_selection", "combat", "cards", "exploration", "misc")` | ~130 |

### Dataclass (frozen)
| Name | Fields | Line |
|---|---|---|
| `RolloutSeedBundle` | `master_seed: int`, `replicate: int`, `bot_selection: int`, `combat: int`, `cards: int`, `exploration: int`, `misc: int` | ~134 |
| `RolloutSeedBundle.derive` | `(master_seed: int, replicate: int) -> RolloutSeedBundle` | classmethod | ~143 |
| `RolloutSeedBundle.as_dict` | `() -> dict[str, int]` | method | ~151 |
| `RolloutResult` | `option_id: str`, `actor: str`, `horizon_round: int`, `final_round: int`, `final_vp: int`, `finished: bool`, `final_state_fingerprint: str`, `replayed_prefix_length: int = 0`, `seed_bundle: RolloutSeedBundle | None = None` | ~155 |

### Class
| Name | Key attributes/methods | Line |
|---|---|---|
| `ReplayPrefixThenForceDecider` | `__init__(prefix, expected_target_fingerprint, option_id, continuations, state_provider, before_target)`, `choose(choice: Choice) -> Option` | ~172 |

### Public functions
| Name | Signature | Returns | Line |
|---|---|---|---|
| `resumable_choice` | `(game: Game, expected: Choice) -> bool` | bool | ~240 |
| `rollout_resumable_choice` | `(game: Game, choice: Choice, option_id: str, *, horizon_round: int = 4, seed_bundle: RolloutSeedBundle | None = None) -> RolloutResult` | RolloutResult | ~248 |
| `rollout_replayed_choice` | `(initial_game: Game, prefix: tuple[DecisionRecord, ...], target_state: GameState, choice: Choice, option_id: str, *, horizon_round: int = 4, max_steps: int = 10_000, seed_bundle: RolloutSeedBundle | None = None) -> RolloutResult` | RolloutResult | ~287 |

## Dependencies between rollout.py APIs
- `choice_fingerprint` depends on: `_canonical`
- `state_fingerprint` depends on: `_canonical`, `GameState`
- `decision_fingerprint` depends on: `state_fingerprint`, `choice_fingerprint`
- `fork_game` depends on: `state_fingerprint`, `copy.deepcopy`
- `ForceOnceDecider` depends on: `decision_fingerprint`, `ExpectedChoiceMismatch`, `Decider.choose_forced`
- `RolloutSeedBundle.derive` depends on: `hashlib.sha256`, `RNG_DOMAINS`
- `ReplayPrefixThenForceDecider` depends on: `decision_fingerprint`, `ExpectedChoiceMismatch`, `_forced`
- `resumable_choice` depends on: `choice_fingerprint`, `Game.legal_options`
- `rollout_resumable_choice` depends on: `resumable_choice`, `fork_game`, `ForceOnceDecider`, `decision_fingerprint`, `_prepare_future_rng`
- `rollout_replayed_choice` depends on: `fork_game`, `ReplayPrefixThenMacroDecider`, `decision_fingerprint`, `_prepare_future_rng`
- `_prepare_future_rng` depends on: `hashlib.sha256`, RNG seeding on `game.dice._rng` and bot `_rng`

## Compatibility class
- **choice_fingerprint**: `exact` — SHA-256 of canonical JSON; Rust must reproduce for parity.
- **state_fingerprint**: `exact` — SHA-256 of canonical GameState; Rust must reproduce.
- **decision_fingerprint**: `exact` — binds state + choice fingerprint.
- **RolloutSeedBundle**: `exact` — derived via SHA-256 from `ti4-rollout-v1:{master_seed}:{replicate}:{domain}`; Rust must reproduce.
- **fork_game**: `semantic` — deep copy with integrity verification; Rust may use different copy strategy.
- **ForceOnceDecider / ReplayPrefixThenForceDecider**: `semantic` — runtime behavior; Rust may implement differently.

## Non-goals
- No implementation of rollout logic; documentation-only inventory.
- No analysis of rollout training pipeline.

## Evidence
- File: `D:\Projects\ti4-engine\engine\ml\rollout.py` (read-only)
- Public symbols: 1 exception class, 1 constant, 2 dataclasses, 3 classes, 3 public functions
- Private symbols: 4 private functions (_canonical, choice_fingerprint, state_fingerprint, decision_fingerprint, fork_game, _prepare_future_rng)
- Cross-module dependencies: engine.choice (Choice, Decider, DecisionRecord, IllegalChoice, Option), engine.game (Game), engine.state (GameState)

## Completion
- [x] All public constructors, factories, and classes catalogued
- [x] All init signatures documented
- [x] All dependencies listed
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
