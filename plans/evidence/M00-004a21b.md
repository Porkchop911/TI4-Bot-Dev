# M00-004a21b — ML Capture Reference and Decider Slice Inventory

## Scope
`engine/ml/capture.py` lines 120–274. Reference extraction and ChoiceCaptureDecider slice.

## Slice bounds
- Physical lines: 120–274
- Oracle commit: `37061c5` (codex/fully-learned-policy)
- Oracle path: `D:\Projects\ti4-engine\engine\ml\capture.py`

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/capture.py
  → 1 file
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\capture.py | awk 'NR>=120 && NR<=274'
  → 5 matching lines in lines 120–274 (3 private functions + 1 public function + 1 class)
```

## P0/P1 scope
- **P0**: read-only access to oracle lines 120–274
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `typed_references()` (public reference extraction function)
| Field | Value |
|---|---|
| Symbol | `typed_references` |
| Kind | Module-level function |
| Signature | `typed_references(option: Option, known: Catalogue | None = None) -> tuple[TypedReference, ...]` |
| Source line | 128 |
| Construction role | Extracts typed references from an Option using a Catalogue. Deliberately never tokenises IDs or labels. Default `known` parameter falls back to `catalogue()`. Constructs TypedReference elements for: option kind, choice family, kind-specific namespaces (_KIND_NAMESPACE), payload-key namespaces (_PAYLOAD_NAMESPACE), component IDs (leader/tech/relic), action_card (via observation private index), open_transaction trade partners, and breakthrough grants. Returns sorted tuple of TypedReference. |
| Rust relevance | Rust function `fn typed_references(option: &Option, known: Option<&Catalogue>) -> Vec<TypedReference>`. Critical for ML training data — every option's typed references become training features. Must preserve exact reference extraction logic for compatibility. |

### 2. `ChoiceCaptureDecider` (capture wrapper class)
| Field | Value |
|---|---|
| Symbol | `ChoiceCaptureDecider` |
| Kind | Class |
| Source line | 232 |
| Constructor signature | `__init__(self, view: GameView, continuation: Decider, sink: Callable[[CapturedChoice], None], *, run_id: str) -> None` |
| Construction role | Wrap a scored continuation and emit one immutable matrix per decision. Constructor stores: view (GameView, validated via isinstance), continuation (Decider), sink (Callable to receive CapturedChoice), run_id (str). `choose(choice: Choice) -> Option` method: extracts BotObservation from view, retrieves optional ml_decision_context from continuation, calls continuation.choose(choice), verifies exactly one Decision emitted, captures via capture_scored_choice(), increments decision_index, returns chosen. Raises TypeError for non-GameView, RuntimeError if continuation doesn't emit exactly one Decision. |
| Rust relevance | Rust struct wrapping GameView, Decider, sink closure, run_id. Must reproduce identical CapturedChoice for identical game state and continuation output. Critical for ML training data collection pipeline. |

## Exclusions table (top-level only, lines 120–274)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| `_known_reference` | private function | 120 | Private function (leading underscore), not public API |
| `_typed_references` | private function | 137 | Private function (leading underscore), not public API |
| `_payload_json` | private function | 228 | Private function (leading underscore), not public API |
| `__future__` import | import | 3 | Future annotations import |
| `hashlib` import | import | 9 | Stdlib import, not engine API |
| `json` import | import | 10 | Stdlib import, not engine API |
| `os` import | import | 11 | Stdlib import, not engine API |
| `tempfile` import | import | 12 | Stdlib import, not engine API |
| `asdict` import | import | 13 | Stdlib import, not engine API |
| `dataclass` import | import | 13 | Stdlib import, not engine API |
| `Path` import | import | 14 | Stdlib import, not engine API |
| `Callable` import | import | 15 | Stdlib import, not engine API |
| `Iterable` import | import | 15 | Stdlib import, not engine API |
| `Mapping` import | import | 15 | Stdlib import, not engine API |
| `Choice` import | import | 17 | External type dependency from `engine.choice`, not constructed here |
| `Decider` import | import | 17 | External type dependency from `engine.choice`, not constructed here |
| `Option` import | import | 17 | External type dependency from `engine.choice`, not constructed here |
| `Catalogue` import | import | 18 | External type from `engine.ml.catalogue`, not constructed here |
| `Namespace` import | import | 18 | External type from `engine.ml.catalogue`, not constructed here |
| `catalogue` import | import | 18 | External function from `engine.ml.catalogue`, not constructed here |
| `TacticalDecisionContext` import | import | 19 | External type from `engine.ml.context`, not constructed here |
| `numeric_option_features` import | import | 20 | External function from `engine.ml.features`, not constructed here |
| `BotObservation` import | import | 21 | External type from `engine.ml.observation`, not constructed here |
| `_canonical` import | import | 22 | External function from `engine.ml.rollout`, not constructed here |
| `choice_fingerprint` import | import | 22 | External function from `engine.ml.rollout`, not constructed here |
| `GameView` import | import | 23 | External type from `engine.views`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (1 function + 1 class) |
| Excluded top-level definitions | 26 (private functions + stdlib imports + type imports) |
| Slice lines covered | 120–274 |
| Discovery matching lines | 5 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/capture.py lines 120–274 reference and decider slice inventoried.
- **Not complete** for M00-004a21 (functions capture_scored_choice, build_choice_matrix, write_capture_shard deferred).
- **Not complete** for M00-004a.
- **Not complete** for M00-004.
