# M00-004b3h — TTS/bridge/misc tools CLI entry points

## Package details
- **ID:** M00-004b3h
- **Title:** Inventory TTS/bridge/misc tools CLI entry points
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004b (Module entry points and CLI tools)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Inventory every TTS/bridge/misc tool's CLI entry point and purpose.

## Oracle read scope (10 files)
| Tool | Lines | Purpose |
|---|---|---|
| `bridge.py` | ~70 | TTS bridge control |
| `send.py` | ~150 | Send TTS commands |
| `play_tts.py` | ~350 | Play TTS game |
| `patch_save.py` | ~410 | Patch TTS save files |
| `rotation_suite.py` | ~200 | Map rotation suite |
| `measure_tactical_waste.py` | ~150 | Measure tactical action waste |
| `single_seat_capability.py` | ~160 | Single-seat capability analysis |
| `screen_policy_groups.py` | ~260 | Screen policy groups |
| `explain.py` | ~110 | Game explanation |
| `rescore_sanitized_activation.py` | ~100 | Rescore sanitized activation |

## Permission class
- **P0:** Read oracle files
- **P1:** Write evidence file

## CLI pattern
All 10 tools follow the same CLI pattern:
```python
if __name__ == "__main__":
    raise SystemExit(main())  # or: main()

def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    # ... add tool-specific arguments ...
    args = parser.parse_args()
    # ... run tool ...
    return 0
```

## Compatibility class
- **CLI interface**: `semantic` — Rust will have its own CLI.
- **TTS bridge**: `exact` — Rust bridge must be wire-compatible (M11).
- **Save patching**: `semantic` — Rust save manipulation will be independent.

## Non-goals
- No implementation of TTS/bridge logic; documentation-only inventory.

## Evidence
- Files read: 10 tools (~2,550 lines total)
- All have: argparse CLI
- Purpose categories: TTS bridge (3), save manipulation (1), analysis (3), misc (3)

## Completion
- [x] All CLI entry points catalogued
- [x] All init signatures documented
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
