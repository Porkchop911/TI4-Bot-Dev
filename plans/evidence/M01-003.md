# M01-003 — Workspace skeleton

## Package details
- **ID:** M01-003
- **Title:** Workspace skeleton
- **Milestone:** M01 — Repository bootstrap
- **Package:** M01-003
- **Dependencies:** M01-002 ✅

## Permission class
- **P1:** Write evidence file

## Workspace structure (10 crates)

### Root Cargo.toml
```toml
[workspace]
resolver = "2"
members = [
    "crates/ti4-model",
    "crates/ti4-content",
    "crates/ti4-engine",
    "crates/ti4-policy",
    "crates/ti4-sim",
    "crates/ti4-training",
    "crates/ti4-bridge",
    "crates/ti4-legacy",
    "crates/ti4-cli",
    "xtask",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
authors = ["ti4-engine-rs contributors"]
homepage = ""
repository = ""
readme = "README.md"

[workspace.dependencies]
# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "2.0"

# Error handling
anyhow = "1.0"
thiserror = "2.0"

# Logging/tracing
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# RNG
rand = "0.9"
rand_pcg = "0.9"

# Compression
flate2 = "1.0"
lz4_flex = "0.11"

# CLI
clap = { version = "4.5", features = ["derive"] }
serde_yaml = "0.9"

# Parquet
parquet = "53.0"
arrow = "53.0"
tokio = { version = "1.0", features = ["full"] }

# Testing
criterion = "0.5"
proptest = "1.5"
proptest-derive = "0.5"

# Utilities
enumset = "1.1"
bitflags = "2.6"
smallvec = "1.13"
ahash = "0.8"
hashbrown = "0.15"
indexmap = "2.6"
uuid = { version = "1.11", features = ["v4"] }

# ML inference (placeholder)
# ndarray = "0.16"
# ort = "2.0"

# Dev dependencies
cargo-llvm-cov = "0.6"
mimalloc = "0.1"

[workspace.lints.rust]
unsafe_code = "forbid"
unused_imports = "warn"
dead_code = "warn"
deprecated = "warn"
unreachable_pub = "warn"

[workspace.lints.clippy]
all = "warn"
pedantic = "warn"
nursery = "warn"

[profile.dev]
split-debuginfo = "packed"
opt-level = 0

[profile.release]
lto = "thin"
strip = true
codegen-units = 1

[profile.bench]
inherits = "release"
lto = "fat"
strip = false
```

### Crate dependency graph
```
ti4-model (no internal deps)
    ↑
ti4-content (ti4-model)
    ↑
ti4-engine (ti4-model, ti4-content)
    ↑
ti4-policy (ti4-model, ti4-engine)
    ↑
ti4-sim (ti4-model, ti4-content, ti4-engine)
    ↑
ti4-training (ti4-model, ti4-engine, ti4-policy, ti4-sim)
    ↑
ti4-bridge (ti4-model, ti4-engine, ti4-policy)
    ↑
ti4-legacy (ti4-model, ti4-content, ti4-engine)
    ↑
ti4-cli (ti4-engine, ti4-policy, ti4-sim, ti4-training, ti4-bridge, ti4-content)
    ↑
xtask (dev only — no runtime deps)
```

### Crate directory structure
```
crates/
  ti4-model/
    Cargo.toml
    src/
      lib.rs
      id.rs          (typed IDs)
      state.rs       (domain state)
      views.rs       (redacted views)
      schema.rs      (schema contracts)
  ti4-content/
    Cargo.toml
    src/
      lib.rs
      loader.rs      (corpus loading)
      validator.rs   (validation)
      provenance.rs  (provenance, hashes)
  ti4-engine/
    Cargo.toml
    src/
      lib.rs
      choices.rs     (legal actions, choice system)
      timing.rs      (ability resolution)
      rules.rs       (game rules)
      effects.rs     (effect resolution)
      game.rs        (game loop)
      replay.rs      (canonical event stream)
  ti4-policy/
    Cargo.toml
    src/
      lib.rs
      scored_bots.rs (scored bot logic)
      features.rs    (feature extraction)
      inference.rs   (learned policy inference)
  ti4-sim/
    Cargo.toml
    src/
      lib.rs
      maps.rs        (map loading)
      replay.rs      (replay management)
      batches.rs     (batch simulation)
      benchmarks.rs  (benchmark harness)
  ti4-training/
    Cargo.toml
    src/
      lib.rs
      stage1.rs      (stage 1 learning)
      stage2.rs      (stage 2 learning)
      promotion.rs   (policy promotion)
      archives.rs    (archive management)
      parquet.rs     (Parquet capture)
  ti4-bridge/
    Cargo.toml
    src/
      lib.rs
      http.rs        (HTTP server)
      tts.rs         (TTS commands)
      import.rs      (import/reconcile)
      audit.rs       (audit trail)
  ti4-legacy/
    Cargo.toml
    src/
      lib.rs
      artifact.rs    (artifact conversion)
      replay.rs      (replay conversion)
  ti4-cli/
    Cargo.toml
    src/
      main.rs        (executable entry points)
xtask/
  Cargo.toml
  src/
    main.rs        (fixture/validation/maintenance)
```

## M01-003 acceptance
- [x] Workspace Cargo.toml documented (10 crates, dependency graph)
- [x] All 10 crates from master plan specified
- [x] Crate directory structure documented
- [x] Dependency direction enforced by compilation (documented)

## M01-003 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
