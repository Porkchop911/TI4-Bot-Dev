# M00-004b9d — tools/evaluate_cooperation_pair.py Entry Point Inventory

## Scope
`tools/evaluate_cooperation_pair.py` (120 lines). Measure whether two factions that cooperate on sight both score better.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/evaluate_cooperation_pair.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\evaluate_cooperation_pair.py
  → 120
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\evaluate_cooperation_pair.py
  → 4 matching lines (1 private function + 1 public function + 1 public function + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\evaluate_cooperation_pair.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main()` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main() -> int` |
| Kind | Module-level function |
| Source line | 77 |
| Construction role | Parses args via `parse_args()`, validates exactly 2 `--pair` factions, generates tasks for baseline+cooperating arms over seeds×rotations, runs parallel `_play()` via `ProcessPoolExecutor`, computes per-faction paired VP effect with 95% CI via `paired()`, prints delta VP table with treated markers, computes treated pair mean delta + untreated four mean delta + support notes comparison, writes JSON report to `--out`. Returns 0. |

### 2. `parse_args()` — CLI argument parser
| Field | Value |
|---|---|
| Symbol | `parse_args() -> argparse.Namespace` |
| Kind | Module-level function |
| Source line | 63 |
| Construction role | Returns `argparse.ArgumentParser` with `description=__doc__`. Flags: `--pair` (append, required, exactly 2), `--exclusive` (flag, False), `--seeds` (int, default 40), `--first-seed` (int, default 32_600_000), `--workers` (int, default `min(16, os.cpu_count())`), `--horizon` (int, default 4), `--out` (Path, default `ROOT/out/cooperation_pair.json`). |

### 3. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 120 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--pair` | str (append, required) | **required** (×2) | Two cooperating faction names |
| `--exclusive` | flag | False | Pair refuses every deal not with each other |
| `--seeds` | int | 40 | Number of seeds |
| `--first-seed` | int | 32_600_000 | Starting seed |
| `--workers` | int | `min(16, os.cpu_count())` | Worker count |
| `--horizon` | int | 4 | Game horizon in rounds |
| `--out` | Path | `ROOT/out/cooperation_pair.json` | Output report path |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–17 | Documentation, not code |
| `ROOT` | module constant | 25 | Module-level path constant (sys.path manipulation) |
| `_play` | private function | 32 | Private (leading underscore) |
| `paired` | public function | 50 | Paired comparison helper, not CLI entry |
| `__future__` import | import | 19 | Future annotations import |
| `argparse` import | import | 21 | Stdlib import |
| `concurrent.futures` import | import | 22 | Stdlib import |
| `dataclasses` import | import | 23 | Stdlib import |
| `json` import | import | 24 | Stdlib import |
| `math` import | import | 25 | Stdlib import |
| `os` import | import | 26 | Stdlib import |
| `statistics` import | import | 27 | Stdlib import |
| `sys` import | import | 28 | Stdlib import |
| `time` import | import | 29 | Stdlib import |
| `Counter` import | import | 30 | Stdlib import |
| `defaultdict` import | import | 30 | Stdlib import |
| `Path` import | import | 31 | Stdlib import |
| `bots` import | import | 34 | External module from `engine`, not constructed here |
| `sim` import | import | 34 | External module from `engine`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (1 entry point function + 1 parser function) |
| Excluded top-level definitions | 20 (docstrings + module constant + private function + helper function + stdlib imports + type imports) |
| Source lines covered | 1–120 |
| Discovery matching lines | 4 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/evaluate_cooperation_pair.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b9.
