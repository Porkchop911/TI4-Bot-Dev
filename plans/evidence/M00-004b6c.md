# M00-004b6c — tools/ridge_choice_sets.py Entry Point Inventory

## Scope
`tools/ridge_choice_sets.py` (63 lines). Fold-local sparse ridge baseline for choice-set residual models.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/ridge_choice_sets.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\ridge_choice_sets.py
  → 63
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\ridge_choice_sets.py
  → 6 matching lines (1 private function + 1 class + 3 methods + 1 public function)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\ridge_choice_sets.py`
- **P1**: write-only to this evidence file

---

## Entry points

### None found
This file is a **pure library/utility module** with no CLI entry points, no `__main__` guard, no `parse_args()`, and no `main()` function. It provides:
- `RidgeChoiceScorer` class: scikit-learn Ridge model for choice-set scoring
- `with_ridge_baseline()` function: convenience wrapper to add incumbent_z scores

### Top-level definitions inventoried

| Symbol | Kind | Line | Role |
|---|---|---|---|
| `_subtract` | private function | 13 | Feature-dict subtraction helper (leading underscore) |
| `RidgeChoiceScorer` | class | 19 | Ridge model wrapper: `fit()`, `scores()`, `as_state()` |
| `with_ridge_baseline` | public function | 57 | Adds incumbent_z scores to ChoiceSet sequence |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| (none) | — | — | No CLI entry points |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `__future__` import | import | 3 | Future annotations import |
| `itertools` import | import | 5 | Stdlib import |
| `dataclasses.replace` import | import | 6 | Stdlib import |
| `typing.Sequence` import | import | 7 | Stdlib import |
| `numpy` import | import | 9 | External dependency |
| `DictVectorizer` import | import | 10 | External dependency |
| `Ridge` import | import | 11 | External dependency |
| `MaxAbsScaler` import | import | 12 | External dependency |
| `ChoiceSet` import | import | 14 | External class from tools.ml_choice_sets, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (1 class + 1 function, no CLI entry) |
| Excluded top-level definitions | 11 (docstring + private function + stdlib imports + external imports) |
| Source lines covered | 1–63 |
| Discovery matching lines | 6 |
| Unreconciled definitions | 0 |
| CLI entry points | 0 |
| `__main__` guard | absent |

## Status
- **Slice complete**: tools/ridge_choice_sets.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b6.
