# M00-004a8 — Tactical Macro Feature Construction API Inventory

## Scope
`engine/ml/tactical_macro_features.py` (44 physical lines). Sparse feature combination for tactical-action package candidates.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/tactical_macro_features.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\tactical_macro_features.py
  → 44
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+ =' D:\Projects\ti4-engine\engine\ml\tactical_macro_features.py
  → 1 matching line (1 function definition)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\tactical_macro_features.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `tactical_macro_features()` (feature combination function)
| Field | Value |
|---|---|
| Symbol | `tactical_macro_features` |
| Kind | Module-level function |
| Signature | `tactical_macro_features(capture: dict, activation_option: dict, macro: dict) -> dict[str, float]` |
| Source line | 8 |
| Construction role | Combines activation-state features (via `option_features` + `actor` from `engine.ml.model_features`) with a tactical package's realized consequences. Produces a sparse feature dict with keys: `macro:is_tactical_package`, `macro:destination={id}`, `macro:purpose={purpose}`, faction/round-scoped purpose keys, destination|purpose composite, `macro:intent_{name}` for each intent field (ground_required/present/shortfall, fleet_required/present/shortfall), `macro:summary:{name}` for each summary entry, `macro:sequence={kinds>join}`, and `macro:step_position={i}|kind={kind}` per step. Returns `dict[str, float]` — all values are 1.0 or float-cast intent/summary values. |
| Rust relevance | Feature engineering function for ML training pipeline; must reproduce identical feature dict for identical inputs. Depends on `engine.ml.model_features.actor` and `option_features`. |

## Exclusions table
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `__future__` import | import | 3 | Future annotations import |
| `actor` import | import | 5 | External function from `engine.ml.model_features`, not constructed here |
| `option_features` import | import | 5 | External function from `engine.ml.model_features`, not constructed here |
| Local `features` | local variable | 11 | Local dict accumulation, not public API |
| Local `own` | local variable | 12 | Local dict from `actor()`, not public API |
| Local `round_number` | local variable | 13 | Local int, not public API |
| Local `destination` | local variable | 14 | Local str, not public API |
| Local `intent` | local variable | 15 | Local dict/None, not public API |
| Local `purpose` | local variable | 16 | Local str, not public API |
| Local `name` | local variable | 27 | Loop variable, not public API |
| Local `value` | local variable | 33 | Loop variable, not public API |
| Local `kinds` | local variable | 35 | Local list of str, not public API |
| Local `sequence` | local variable | 36 | Local str, not public API |
| Local `index` | local variable | 38 | Loop variable, not public API |
| Local `step` | local variable | 38 | Loop variable, not public API |
| Local `kind` | local variable | 39 | Local str, not public API |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (`tactical_macro_features` function) |
| Excluded definitions | 17 (docstrings + stdlib imports + type imports + local variables) |
| Source lines covered | 1–44 |
| Discovery matching lines | 1 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/tactical_macro_features.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
