# M08-007d — Public spend-capacity observation

## Package specification

- **Milestone / dependencies:** M08-004; M08-007a-c.
- **Objective:** Expose the selecting player's public ready resources or influence as a typed
  `Observed` aggregate for objective-reservation and later factual-feature code.
- **Oracle:** `D:\Projects\ti4-engine\engine\bots.py` `PUBLIC_GOALS` and
  `_reserve_for_objective` at pinned commit
  `37061c511a4780d4c0719e0342533a498cd4b457`; `engine/production.py` availability semantics.
- **Rust edit scope:** `crates/ti4-engine/src/choice.rs`, this evidence file, and
  `plans/EXECUTION_STATE.md` at completion.
- **Permission / external state:** P0 oracle integrity guard and read-only inspection; P1 local
  code, test, evidence, branch, and focused commit. No network, ports, generated artifacts, or
  external-state changes. No destructive actions.
- **Inputs / outputs:** A player and `Spend` kind in; the exact public spend-capacity aggregate
  out, using the engine's existing payment accounting.
- **Compatibility class / invariants:** Semantic observation compatibility. The aggregate contains
  only ready controlled planet values and face-up trade goods, which are public. It must share the
  authoritative `production::available` accounting so the policy does not reimplement payment
  arithmetic. It must not expose the state, per-planet exhaustion identities, private objectives,
  or another player's private holdings.
- **Non-goals:** Reserving/spending assets, objective schedules, trade-good-only objectives,
  secret objectives, payment-choice scoring, and any policy decision change.

## Tests to add

1. A controlled ready planet contributes its printed resource and influence values, plus public
   trade goods, through the observation.
2. Exhausting that planet removes only its printed values while retained trade goods remain usable
   for both spend kinds.

## Commands to run

`PYTHONDONTWRITEBYTECODE=1 python tools/oracle_integrity_guard.py`; focused engine test;
`cargo fmt --all --check`; `cargo test -p ti4-engine`; `cargo test -p ti4-policy`;
`cargo clippy -p ti4-policy -p ti4-engine --all-targets -- -D warnings`; and `git diff --check`.

## Expected evidence

Focused test boundary, mutation check, exact command results, changed paths, and a self-review
record under the existing 2026-08-11 owner waiver.

## Implementation and verification

`Observed::available_spend` delegates to the engine's existing `production::available` function,
so it precisely counts ready controlled planet values and public trade goods for either spend kind
without exposing the underlying state or exhaustion collection.

The focused test starts with one controlled ready planet and two trade goods, verifies both
aggregates, then exhausts the planet and verifies that only the two public goods remain.

| Command | Result |
|---|---|
| `PYTHONDONTWRITEBYTECODE=1 python tools/oracle_integrity_guard.py` | `oracle integrity verified: 238 files` before implementation and after checks |
| `cargo test -p ti4-engine public_spend_capacity_counts_only_ready_planets_and_faceup_goods` | passed; 1 passed, 0 failed |
| `cargo fmt --all --check` | passed |
| `cargo test -p ti4-engine` | 704 unit tests and 5 doc tests passed, 0 failed |
| `cargo test -p ti4-policy` | 51 passed, 0 failed |
| `cargo clippy -p ti4-policy -p ti4-engine --all-targets -- -D warnings` | passed |
| `git diff --check` | passed |

### Mutation check

Replacing the delegate with a constant zero made the focused test fail its ready-planet assertion
(`left: 0`, `right: 7`). The authoritative delegate was restored before final checks.

## Compatibility evidence

The method accepts only an explicit player and `Spend` kind and returns an integer aggregate. It
does not return `GameState`, planet identities, exhaustion identities, objective information, or
any card identity. The source accounting already includes only the player's controlled, unexhausted
planets and their face-up trade goods, matching the public reservation measures in the oracle.

## Changed paths

- `crates/ti4-engine/src/choice.rs`
- `plans/evidence/M08-007d.md`
- `plans/EXECUTION_STATE.md`

## Review

Independent implementation review remains owner-waived (2026-08-11). Self-review checked that the
new observation delegates rather than copies payment arithmetic and does not widen the typed
information boundary. No findings remain.
