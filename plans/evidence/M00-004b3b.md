# M00-004b3b — tools/analyze_score_tails.py Entry Point Inventory

## Scope
`tools/analyze_score_tails.py` (398 lines). High/zero VP tail separation analysis.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/analyze_score_tails.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\analyze_score_tails.py
  → 398
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\analyze_score_tails.py
  → 9 matching lines (1 module constant + 1 tuple constant + 6 functions + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\analyze_score_tails.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main()` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main()` |
| Kind | Module-level function |
| Source line | 269 |
| Construction role | Parses args via `parse_args()`, generates game tasks over seeds×rotations, runs parallel via `ProcessPoolExecutor` calling `_play()`, partitions player-games into high/middle/zero tails, prints feature separation (seed-clustered bootstrap), conversion ratios, conversion quintiles, scoring routes in high tail, faction incidence, writes JSON report to `--out`. Returns exit code 0 or 1 if tail empty. |

### 2. `parse_args()` — CLI argument parser
| Field | Value |
|---|---|
| Symbol | `parse_args()` |
| Kind | Module-level function |
| Source line | 234 |
| Construction role | Returns `argparse.ArgumentParser` with `description=__doc__`. Flags: `--seeds` (int, default 40), `--first-seed` (int, default 32_000_000), `--workers` (int, default `min(16, os.cpu_count())`), `--horizon` (int, default 4), `--high-threshold` (int, default 6), `--promoted` (store_true), `--bootstrap-replicates` (int, default 2000), `--out` (Path, default `ROOT/out/score_tails.json`). |

### 3. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 398 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--seeds` | int | 40 | Number of seeds to simulate |
| `--first-seed` | int | 32_000_000 | Starting seed |
| `--workers` | int | `min(16, os.cpu_count())` | Parallel worker count |
| `--horizon` | int | 4 | Game horizon in rounds |
| `--high-threshold` | int | 6 | VP threshold for high tail |
| `--promoted` | flag | False | Use promoted tactical searches |
| `--bootstrap-replicates` | int | 2000 | Bootstrap replicate count |
| `--out` | Path | `ROOT/out/score_tails.json` | Output report path |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–40 | Documentation, not code |
| `ROOT` | module constant | 44 | Module-level path constant (sys.path manipulation) |
| `DEFAULT_FIRST_SEED` | module constant | 47 | Audit seed block constant (32_000_000) |
| `FEATURES` | module constant | 50–61 | Tuple of 14 observable end-state features |
| `_outcome` | private function | 64 | Private function (leading underscore) |
| `_play` | private function | 98 | Private function (leading underscore) |
| `RATIOS` | module constant | 117–122 | Tuple of ratio pairs for reporting |
| `_ratio` | private function | 125 | Private function (leading underscore) |
| `_conversion_quintiles` | private function | 130 | Private function (leading underscore) |
| `_bootstrap_difference` | private function | 152 | Private function (leading underscore) |
| `__future__` import | import | 42 | Future annotations import |
| `argparse` import | import | 44 | Stdlib import |
| `concurrent.futures` import | import | 45 | Stdlib import |
| `json` import | import | 46 | Stdlib import |
| `os` import | import | 47 | Stdlib import |
| `random` import | import | 48 | Stdlib import |
| `statistics` import | import | 49 | Stdlib import |
| `sys` import | import | 50 | Stdlib import |
| `time` import | import | 51 | Stdlib import |
| `Counter` import | import | 52 | Stdlib import |
| `defaultdict` import | import | 52 | Stdlib import |
| `Path` import | import | 53 | Stdlib import |
| `bots` import | import | 56 | External module from `engine`, not constructed here |
| `galaxy` import | import | 56 | External module from `engine`, not constructed here |
| `objectives` import | import | 56 | External module from `engine`, not constructed here |
| `secrets` import | import | 56 | External module from `engine`, not constructed here |
| `sim` import | import | 56 | External module from `engine`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (1 entry point function + 1 parser function) |
| Excluded top-level definitions | 27 (docstrings + module constants + private functions + stdlib imports + type imports) |
| Source lines covered | 1–398 |
| Discovery matching lines | 9 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/analyze_score_tails.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b3.
