# M08-007b — Public technology-objective progress

## Package specification

- **Milestone / dependencies:** M08-004, M08-006a/b, M08-007a.
- **Objective:** Let public revealed technology objectives steer legal research toward their next
  visible colour-pair or unit-upgrade milestone.
- **Oracle:** `engine/bots.py` `PUBLIC_GOALS`, `goals`, and `demands` at pinned commit
  `37061c511a4780d4c0719e0342533a498cd4b457`.
- **Scope / boundary:** `crates/ti4-policy/src/bot.rs`, evidence, execution state. Reads only
  revealed public aliases, public scored records, and face-up technologies through `Observed`.
- **Non-goals:** Secret objectives, spend reservations, schedules, other goal families, or any
  private exhaustion/resource query.

## Tests

An unscored public Diversify Research objective makes the second technology of an existing colour
win over an otherwise more novel colour, with a named objective component.

## Implementation

`choose_seeing` now recognises unscored revealed `develop`/`revolutionize` and
`diversify`/`master_science` objectives. Legal unit upgrades receive progress pressure; for a
colour objective, completing a visible existing pair receives the stronger component.

## Commands and results

| Command | Result |
|---|---|
| `PYTHONDONTWRITEBYTECODE=1 python tools/oracle_integrity_guard.py` | `oracle integrity verified: 238 files` |
| `cargo fmt --all --check` | passed |
| `cargo test -p ti4-policy` | 50 passed, 0 failed |
| `cargo test -p ti4-engine` | 703 unit tests and 5 doc tests passed, 0 failed |
| `cargo clippy -p ti4-policy -p ti4-engine --all-targets -- -D warnings` | passed |
| `git diff --check` | passed |

### Mutation check

Setting the pair-completion component to zero made the focused test select `nm` rather than `gd`.
The component was restored to 6 before final verification.

## Compatibility evidence

Only `Observed::revealed_objectives`, `Observed::scored_by`, and the selecting seat's public
technologies are consulted. There is no secret-goal, deck, exhaustion, or resource read. This is a
partial public technology-demand slice; reservation/schedule and non-technology objective families
remain unimplemented and documented for later M08-007 children.

## Review

Independent implementation review remains owner-waived (2026-08-11). The implementation review
checked the source-scoped public-only data path and the test mutation boundary.
