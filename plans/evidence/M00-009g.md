# M00-009g — Error Projection Implementation

## Package details
- **ID:** M00-009g
- **Title:** Error projection implementation
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-009g
- **Dependencies:** M00-007f ✅

## Permission class
- **P0:** Read oracle files
- **P1:** Write evidence file

## Implementation plan

### Error projection function
```python
def error_to_ndjson(exc: Exception, context: dict) -> dict:
    """Convert exception to NDJSON error projection (M00-007f schema)."""
    import traceback
    return {
        "type": "error",
        "error_type": type(exc).__name__,
        "message": str(exc),
        "context": {
            "turn": context.get("turn"),
            "round": context.get("round"),
            "phase": context.get("phase"),
            "player": context.get("player"),
            "option_id": context.get("option_id"),
            "card_id": context.get("card_id"),
        },
        "stack_trace": traceback.format_exc(),
    }
```

### Error capture in game loop
```python
try:
    # Game loop
    ...
except Exception as exc:
    error_ndjson = error_to_ndjson(exc, {
        "turn": state.turn,
        "round": state.round,
        "phase": state.phase.value,
        "player": state.active_player,
    })
    output.write(json.dumps(error_ndjson) + "\n")
    raise  # Re-raise to stop the game
```

## M00-009g acceptance
- [x] Error projection function documented
- [x] Implementation follows M00-007f schema
- [x] Error capture in game loop documented

## M00-009g status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
