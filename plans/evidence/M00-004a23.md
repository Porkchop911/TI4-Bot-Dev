# M00-004a23 — ML Tactical Macro APIs Construction API Inventory

## Scope
`engine/ml/tactical_macro.py` (510 physical lines). Intent-level tactical-action packages for sequence-level rollout labels.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/tactical_macro.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\tactical_macro.py
  → 510
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\tactical_macro.py
  → 15 matching lines (7 private functions + 1 private constant + 2 private classes + 1 public constant + 3 public dataclasses + 2 public functions)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\tactical_macro.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `TACTICAL_MACRO_SCHEMA_VERSION` (schema constant)
| Field | Value |
|---|---|
| Symbol | `TACTICAL_MACRO_SCHEMA_VERSION` |
| Kind | Module-level constant |
| Value | `2` |
| Source line | 33 |
| Construction role | Schema version for TacticalMacro records. Used in proposal/detection headers. |
| Rust relevance | Rust constant `TACTICAL_MACRO_SCHEMA_VERSION: i32` or `u32`. Schema versioning for compatibility. |

### 2. `TacticalMacroStep` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `TacticalMacroStep` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `TacticalMacroStep(prompt: str, chosen: str, offered: tuple[str, ...], kind: str, payload_json: str)` |
| Source line | 42 |
| Construction role | Immutable record of one tactical macro step. 5 required fields: prompt (choice prompt text), chosen (option id), offered (tuple of all option ids), kind (option kind), payload_json (canonical JSON of option payload). `from_choice(cls, choice: Choice, option: Option) -> TacticalMacroStep`: classmethod constructing from a Choice+Option pair. |
| Rust relevance | Rust frozen struct representing one step in a tactical macro sequence. Construction via classmethod from Choice/Option. |

### 3. `TacticalMacro` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `TacticalMacro` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `TacticalMacro(schema_version: int, actor: str, activation_option_id: str, steps: tuple[TacticalMacroStep, ...], required_step_count: int, intent: TacticalDecisionContext | None, summary: tuple[tuple[str, float], ...], proposal_end_state_fingerprint: str)` |
| Source line | 63 |
| Construction role | Immutable record of a complete tactical macro package. 8 required fields: schema_version, actor, activation_option_id, steps (tuple of TacticalMacroStep), required_step_count, intent (optional TacticalDecisionContext), summary (tuple of (str, float) pairs), proposal_end_state_fingerprint (SHA-256 of end state). `as_dict() -> dict` returns asdict(self). |
| Rust relevance | Rust frozen struct representing a complete tactical macro. Critical for sequence-level rollout labels. |

### 4. `TacticalMacroRolloutResult` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `TacticalMacroRolloutResult` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `TacticalMacroRolloutResult(activation_option_id: str, actor: str, horizon_round: int, final_round: int, final_vp: int, finished: bool, tactical_end_state_fingerprint: str, final_state_fingerprint: str, replayed_prefix_length: int, consumed_step_count: int, seed_bundle: RolloutSeedBundle | None)` |
| Source line | 78 |
| Construction role | Immutable result of a tactical macro rollout. 11 required fields: activation_option_id, actor, horizon_round, final_round, final_vp, finished, tactical_end_state_fingerprint (SHA-256 of tactical end state), final_state_fingerprint (SHA-256 of final state), replayed_prefix_length, consumed_step_count, seed_bundle (optional). Constructed in `rollout_replayed_tactical_macro`. |
| Rust relevance | Rust frozen struct. Returned by tactical macro rollout function. |

### 5. `propose_replayed_tactical_macro()` (macro proposal function)
| Field | Value |
|---|---|
| Symbol | `propose_replayed_tactical_macro` |
| Kind | Module-level function |
| Signature | `propose_replayed_tactical_macro(game: Game, prefix: tuple[DecisionRecord, ...], target_state: GameState, target_choice: Choice, target_option_id: str, *, horizon_round: int = 4, max_steps: int = 10_000, seed_bundle: RolloutSeedBundle | None = None) -> TacticalMacro` |
| Source line | 351 |
| Construction role | Propose a tactical macro by replaying a frozen initial game to a target state+choice, capturing the tactical plan steps during replay. Validates target_state.round <= horizon_round, records source_before fingerprint, forks game, creates ReplayPrefixThenForceDecider with target fingerprint, seats replay for all players, steps until replay.used (with max_steps limit), verifies cursor == len(prefix), validates target_choice is a plan choice, captures steps via _RecordingDecider during replay, validates tactical_in_progress at target, captures tactical_end_state_fingerprint, computes macro_summary (unit counts, system counts, planet control changes, VP changes), validates source immutability. Returns TacticalMacro. |
| Rust relevance | Rust function `fn propose_replayed_tactical_macro(game: &Game, prefix: &[DecisionRecord], target_state: &GameState, target_choice: &Choice, target_option_id: &str, horizon_round: usize, max_steps: usize, seed_bundle: Option<&RolloutSeedBundle>) -> TacticalMacro`. Critical for generating sequence-level rollout labels. |

### 6. `rollout_replayed_tactical_macro()` (tactical macro rollout function)
| Field | Value |
|---|---|
| Symbol | `rollout_replayed_tactical_macro` |
| Kind | Module-level function |
| Signature | `rollout_replayed_tactical_macro(initial_game: Game, prefix: tuple[DecisionRecord, ...], target_state: GameState, target_choice: Choice, target_option_id: str, *, horizon_round: int = 4, max_steps: int = 10_000, seed_bundle: RolloutSeedBundle | None = None) -> TacticalMacroRolloutResult` |
| Source line | 438 |
| Construction role | Replay from a frozen initial game to a target state+choice, then force the tactical plan and continue to horizon. Validates horizon_round, records source_before, forks game, creates ReplayPrefixThenForceDecider with target fingerprint, seats replay for all players, steps until replay.used (with max_steps limit), verifies cursor == len(prefix), validates target_choice is a plan choice, validates tactical_in_progress at target, captures tactical steps via _RecordingDecider, seats continuations, runs remaining rounds, validates source immutability. Returns TacticalMacroRolloutResult with tactical_end_state_fingerprint, consumed_step_count. |
| Rust relevance | Rust function `fn rollout_replayed_tactical_macro(initial_game: &Game, prefix: &[DecisionRecord], target_state: &GameState, target_choice: &Choice, target_option_id: &str, horizon_round: usize, max_steps: usize, seed_bundle: Option<&RolloutSeedBundle>) -> TacticalMacroRolloutResult`. Core tactical macro rollout for labeled data. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–15 | Documentation, not code |
| `_PLAN_KINDS` | private constant | 38 | Private constant (leading underscore), not public API |
| `_tactical_in_progress` | private function | 92 | Private function (leading underscore), not public API |
| `_forced` | private function | 98 | Private function (leading underscore), not public API |
| `_is_plan_choice` | private function | 105 | Private function (leading underscore), not public API |
| `_semantic_match` | private function | 109 | Private function (leading underscore), not public API |
| `_RecordingDecider` | private class | 139 | Private class (leading underscore), not public API |
| `_ReplayPrefixThenMacroDecider` | private class | 172 | Private class (leading underscore), not public API |
| `_unit_counts` | private function | 269 | Private function (leading underscore), not public API |
| `_system_counts` | private function | 282 | Private function (leading underscore), not public API |
| `_macro_summary` | private function | 296 | Private function (leading underscore), not public API |
| `__future__` import | import | 3 | Future annotations import |
| `json` import | import | 5 | Stdlib import, not engine API |
| `asdict` import | import | 6 | Stdlib import, not engine API |
| `dataclass` import | import | 6 | Stdlib import, not engine API |
| `Callable` import | import | 7 | Stdlib import, not engine API |
| `Mapping` import | import | 7 | Stdlib import, not engine API |
| `Choice` import | import | 9 | External type dependency from `engine.choice`, not constructed here |
| `Decider` import | import | 9 | External type dependency from `engine.choice`, not constructed here |
| `DecisionRecord` import | import | 9 | External type dependency from `engine.choice`, not constructed here |
| `Option` import | import | 9 | External type dependency from `engine.choice`, not constructed here |
| `Game` import | import | 10 | External type dependency from `engine.game`, not constructed here |
| `TacticalDecisionContext` import | import | 11 | External type from `engine.ml.context`, not constructed here |
| `sanitize_unseen_state` import | import | 12 | External function from `engine.ml.counterfactual`, not constructed here |
| `ExpectedChoiceMismatch` import | import | 14 | External type from `engine.ml.rollout`, not constructed here |
| `ReplayPrefixThenForceDecider` import | import | 15 | External class from `engine.ml.rollout`, not constructed here |
| `RolloutSeedBundle` import | import | 16 | External class from `engine.ml.rollout`, not constructed here |
| `_canonical` import | import | 17 | External function from `engine.ml.rollout`, not constructed here |
| `_prepare_future_rng` import | import | 18 | External function from `engine.ml.rollout`, not constructed here |
| `decision_fingerprint` import | import | 19 | External function from `engine.ml.rollout`, not constructed here |
| `fork_game` import | import | 20 | External function from `engine.ml.rollout`, not constructed here |
| `state_fingerprint` import | import | 21 | External function from `engine.ml.rollout`, not constructed here |
| `GameState` import | import | 23 | External type dependency from `engine.state`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 6 (1 constant + 3 dataclasses + 2 functions) |
| Excluded top-level definitions | 33 (docstrings + private constants + private functions + private classes + stdlib imports + type imports) |
| Source lines covered | 1–510 |
| Discovery matching lines | 15 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/tactical_macro.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
