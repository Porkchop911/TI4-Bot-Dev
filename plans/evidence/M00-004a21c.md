# M00-004a21c — ML Capture Serialization Function Slice Inventory

## Scope
`engine/ml/capture.py` lines 275–404. Serialization function slice: capture_scored_choice, build_choice_matrix, write_capture_shard.

## Slice bounds
- Physical lines: 275–404
- Oracle commit: `37061c5` (codex/fully-learned-policy)
- Oracle path: `D:\Projects\ti4-engine\engine\ml\capture.py`

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/capture.py
  → 1 file
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\capture.py | awk 'NR>=275 && NR<=404'
  → 3 matching lines in lines 275–404 (3 public functions)
```

## P0/P1 scope
- **P0**: read-only access to oracle lines 275–404
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `capture_scored_choice()` (decision capture function)
| Field | Value |
|---|---|
| Symbol | `capture_scored_choice` |
| Kind | Module-level function |
| Signature | `capture_scored_choice(observation: BotObservation, choice: Choice, decision, chosen: Option, *, run_id: str, decision_index: int, decision_context: TacticalDecisionContext | None = None) -> CapturedChoice` |
| Source line | 275 |
| Construction role | Build a CapturedChoice after an ordinary scored decision has been resolved. Validates decision.player == choice.player and decision.chosen == chosen.id. Extracts incumbent_breakdown (decision.model_input_breakdown or decision.breakdown), incumbent_scores (decision.model_input_scores or decision.scores). Delegates to build_choice_matrix() with observation, choice, raw_breakdown (decision.raw_breakdown), incumbent_breakdown, incumbent_scores, considered (decision.considered), run_id, decision_index, chosen_option_id, decision_context. Returns CapturedChoice. |
| Rust relevance | Rust function `fn capture_scored_choice(observation: &BotObservation, choice: &Choice, decision: &Decision, chosen: &Option, run_id: &str, decision_index: usize, decision_context: Option<&TacticalDecisionContext>) -> CapturedChoice`. Bridges scored decision to immutable capture record. Critical for ML training data pipeline. |

### 2. `build_choice_matrix()` (choice matrix builder function)
| Field | Value |
|---|---|
| Symbol | `build_choice_matrix` |
| Kind | Module-level function |
| Signature | `build_choice_matrix(observation: BotObservation, choice: Choice, raw_breakdown: Mapping[str, Mapping[str, float]], incumbent_breakdown: Mapping[str, Mapping[str, float]], incumbent_scores: Mapping[str, float], considered_options: Iterable[str], *, run_id: str = "runtime", decision_index: int = -1, chosen_option_id: str = "", decision_context: TacticalDecisionContext | None = None) -> CapturedChoice` |
| Source line | 306 |
| Construction role | Build the same feature matrix before or after a scored decision. Validates option_ids from choice.options covers all breakdown/scores keys. Validates chosen_option_id is legal. Validates considered_options subset of option_ids. For each option: builds payload_json via _payload_json(option), typed_references via _typed_references(option, catalogue(), observation), numeric_features via numeric_option_features(observation, option, references, decision_context), raw_components (sorted), incumbent_components (sorted), incumbent_score (float), incumbent_considered (bool). Returns CapturedChoice with schema_version=CAPTURE_SCHEMA_VERSION, observation_digest (SHA-256), choice_fingerprint (SHA-256 of choice), prompt. Runtime learned scorers need immutable choice rows before option picked. |
| Rust relevance | Rust function `fn build_choice_matrix(observation: &BotObservation, choice: &Choice, raw_breakdown: &Map, incumbent_breakdown: &Map, incumbent_scores: &Map, considered: &Set, run_id: &str, decision_index: usize, chosen_option_id: &str, decision_context: Option<&TacticalDecisionContext>) -> CapturedChoice`. Core ML training data construction; must reproduce identical CapturedChoice for identical inputs. Critical for compatibility. |

### 3. `write_capture_shard()` (atomic JSONL shard writer)
| Field | Value |
|---|---|
| Symbol | `write_capture_shard` |
| Kind | Module-level function |
| Signature | `write_capture_shard(path: Path, captures: tuple[CapturedChoice, ...] | list[CapturedChoice], *, manifest_sha256: str) -> None` |
| Source line | 378 |
| Construction role | Publish a complete JSONL shard atomically; readers never see partial data. Creates parent dirs, writes header record (record_type="header", capture_schema_version, manifest_sha256, decision_count) to temp file via os.fdopen with flush+fsync, then os.replace(temporary, path) for atomicity. On any BaseException: unlink temp file, re-raise. Header is sorted JSON; each capture row is canonical_json(). |
| Rust relevance | Rust function `fn write_capture_shard(path: &Path, captures: &[CapturedChoice], manifest_sha256: &str) -> Result<(), Error>`. Atomic write pattern (temp+rename) must be preserved for data integrity. Critical for ML training data pipeline durability. |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 3 (3 module-level functions) |
| Excluded top-level definitions | 0 (all lines in range are public functions) |
| Slice lines covered | 275–404 |
| Discovery matching lines | 3 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/capture.py lines 275–404 serialization function slice inventoried.
- **Not complete** for M00-004a21 (all capture.py slices now complete).
- **Not complete** for M00-004a.
- **Not complete** for M00-004.
