# Evidence — OBS-008d1, strategy/technology/scoring subtype surface

First slice of `OBS-008d`. Zero engine changes — pure read of context `OBS-003e` already
populated. See `plans/OBS-008D1_STRATEGY_DECISION_SUBTYPE_SURFACE.md` for design and the explicit
non-goals; this records what was checked.

## Changed paths

- `crates/ti4-policy/src/features.rs` — new `strategy_decision_features`; `strategy` added to the
  closed explicit grammar.
- `crates/ti4-policy/src/projection.rs` — `strategy` classified `Transferable`.
- `crates/ti4-policy/src/vocabulary.rs` — append-only registry bump 8 → 9 (`OOV_FAMILIES_V9`
  appends `strategy`), new pinned fingerprint, one-version-back window moved to v8.
- `plans/OBS-008D1_STRATEGY_DECISION_SUBTYPE_SURFACE.md`, `plans/evidence/OBS-008D1.md`,
  `plans/EXECUTION_STATE.md`.

## Focused evidence

- `features.rs::obs008d1_strategy_decisions_carry_their_subtype_and_option_count`:
  `research_technology` (Technology primary), `gain_command_token` (Leadership secondary), and
  `score_objective` (status-phase scoring) each carry `strategy:subtype:*` and
  `strategy:option-count`; the research option's `cost` payload independently reaches
  `payload-number:cost` (confirming the generic pipeline, not new wiring); a synthetic uncovered
  subtype gets neither fact, proving the guard is closed rather than a catch-all; the facts survive
  `mlp_option_features` and `projection::admits`.

## Tests and checks

| command | result |
|---|---|
| `cargo test -p ti4-policy --lib obs008d1` | 1 passed |
| `cargo test -p ti4-policy --lib vocabulary` | 33 passed |
| `cargo test -p ti4-policy --lib projection` | 24 passed |
| `cargo test -p ti4-engine` | 1,196 lib + 4 integration + 5 doc passed (unchanged — no engine
  file touched) |
| `cargo test -p ti4-policy --lib` | 223 passed (incl. the 102-game campaign, 333.54 s, no
  regression) |
| `cargo test -p ti4-training` | 133 passed |
| `cargo clippy -p ti4-policy --all-targets -- -D warnings` | clean |
| `rustfmt --check` (scoped files) + `git diff --check` | clean |

## Independent Tier-C review

OUTSTANDING. First `OBS-008d` slice; the row's remaining subtypes (recorded as non-goals in the
spec) are not yet covered.
