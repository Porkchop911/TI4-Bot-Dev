# M00-004a15 — ML Provenance Construction API Inventory

## Scope
`engine/ml/provenance.py` (180 physical lines). Frozen experiment inputs and non-overlapping seed reservations.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/provenance.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\provenance.py
  → 180
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\provenance.py
  → 8 matching lines (3 @dataclass decorators + 3 classes + 2 functions)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\provenance.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `sha256_file()` (file hashing utility)
| Field | Value |
|---|---|
| Symbol | `sha256_file` |
| Kind | Module-level function |
| Signature | `sha256_file(path: Path) -> str` |
| Source line | 14 |
| Construction role | Computes SHA-256 hex digest of a file at `path`, reading in 1MB chunks. Returns lowercase hex string. Used by `ProfileArtifact.validate()` to verify profile file integrity. |
| Rust relevance | Rust equivalent: `fn sha256_file(path: &Path) -> Result<String, Error>`. Utility function for file integrity verification. |

### 2. `ProfileArtifact` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `ProfileArtifact` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `ProfileArtifact(faction: str, path: str, sha256: str)` |
| Source line | 23 |
| Construction role | Immutable value object representing a faction-specific frozen model profile artifact. 3 required fields: faction (string), path (file path string), sha256 (64-char hex checksum). Constructed via: (a) direct instantiation; (b) `from_mapping()` classmethod which validates sha256 is exactly 64 hex characters. |
| Rust relevance | Immutable struct; Rust equivalent must carry faction (String), path (String), sha256 (String). Construction requires sha256 format validation (64 hex chars). Critical for model provenance tracking. |

### 2a. `ProfileArtifact.from_mapping()` (classmethod factory)
| Field | Value |
|---|---|
| Symbol | `ProfileArtifact.from_mapping` |
| Kind | `@classmethod` factory |
| Signature | `from_mapping(cls, faction: str, value: Mapping[str, Any]) -> ProfileArtifact` |
| Source line | 28 |
| Construction role | Factory that constructs a ProfileArtifact from a mapping (typically JSON deserialized dict). Extracts `path` and `sha256` from value, validates sha256 is exactly 64 hex characters (lowercase). Raises `ValueError` on invalid checksum format. |
| Rust relevance | Rust constructor `ProfileArtifact::from_mapping(faction: &str, value: &Value) -> Result<Self, Error>`. Must implement identical sha256 format validation. |

### 3. `SeedBlock` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `SeedBlock` |
| Kind | `@dataclass(frozen=True, order=True)` |
| Signature | `SeedBlock(name: str, start: int, count: int)` |
| Source line | 51 |
| Construction role | Immutable value object representing a non-overlapping seed reservation block. 3 required fields: name (non-empty str), start (non-negative int), count (positive int). `__post_init__` validates: name is non-empty, start >= 0, count > 0. `stop` property returns `start + count`. `contains(seed)` checks `start <= seed < stop`. `order=True` enables natural comparison by (name, start, count). |
| Rust relevance | Immutable struct with 3 required fields. Construction requires validation (non-empty name, non-negative start, positive count). `order=True` enables natural ordering. Critical for seed reservation management. |

### 4. `validate_seed_blocks()` (seed block validator)
| Field | Value |
|---|---|
| Symbol | `validate_seed_blocks` |
| Kind | Module-level function |
| Signature | `validate_seed_blocks(blocks: tuple[SeedBlock, ...]) -> None` |
| Source line | 70 |
| Construction role | Validates a tuple of SeedBlock entries: (1) all names must be unique; (2) when sorted by start, no two blocks may overlap (left.stop must not exceed right.start). Raises `ValueError` on any violation. Used by `ExperimentSpec.validate()` to ensure seed reservations are non-overlapping. |
| Rust relevance | Rust function `fn validate_seed_blocks(blocks: &[SeedBlock]) -> Result<(), Error>`. Must implement identical uniqueness and overlap validation. Critical for experiment reproducibility. |

### 5. `ExperimentSpec` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `ExperimentSpec` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `ExperimentSpec(schema_version: int, experiment_id: str, setup: str, horizon_round: int, reward: str, observation_schema_version: int, catalogue_schema_version: int, profiles: tuple[ProfileArtifact, ...], seed_blocks: tuple[SeedBlock, ...], source_path: Path)` |
| Source line | 84 |
| Construction role | Immutable experiment specification value object. 10 required fields: schema_version, experiment_id, setup, horizon_round, reward, observation_schema_version, catalogue_schema_version, profiles (tuple of ProfileArtifact), seed_blocks (tuple of SeedBlock), source_path. Constructed via: (a) direct instantiation; (b) `load()` classmethod which reads JSON, deserializes profiles and seed blocks, constructs ExperimentSpec, then validates. |
| Rust relevance | Immutable struct with 10 required fields. Construction via `load()` requires JSON deserialization, ProfileArtifact/SeedBlock sub-object construction, and multi-field validation. Critical for experiment reproducibility and training pipeline configuration. |

### 5a. `ExperimentSpec.load()` (classmethod factory)
| Field | Value |
|---|---|
| Symbol | `ExperimentSpec.load` |
| Kind | `@classmethod` factory |
| Signature | `load(cls, path: Path, *, repository_root: Path | None = None, validate_files: bool = True) -> ExperimentSpec` |
| Source line | 99 |
| Construction role | Factory that loads an ExperimentSpec from a JSON file at `path`. Deserializes: schema_version, experiment_id, setup, horizon_round, reward, observation_schema_version, catalogue_schema_version, profiles (via ProfileArtifact.from_mapping for each sorted faction), seed_blocks (via SeedBlock constructor for each entry). Constructs ExperimentSpec with resolved source_path. Calls `validate()` with optional repository_root and validate_files flags. Raises `ValueError` on any validation failure. |
| Rust relevance | Rust constructor `ExperimentSpec::load(path: &Path, repository_root: Option<&Path>, validate_files: bool) -> Result<Self, Error>`. Must implement identical JSON deserialization, sub-object construction, and validation pipeline. Critical for experiment loading reproducibility. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `__future__` import | import | 3 | Future annotations import |
| `hashlib` import | import | 5 | Stdlib import, not engine API |
| `json` import | import | 6 | Stdlib import, not engine API |
| `dataclass` import | import | 7 | Stdlib import, not engine API |
| `Path` import | import | 8 | Stdlib import, not engine API |
| `Any` import | import | 9 | Stdlib import, not engine API |
| `Mapping` import | import | 9 | Stdlib import, not engine API |
| `SAVE52_FACTIONS` import | import | 11 | External constant from `engine.ml.catalogue`, not constructed here |
| `ProfileArtifact.validate()` | method | 33 | Validation method, not construction |
| `ProfileArtifact.__post_init__` | method | 32 | Validation method, not construction |
| `ProfileArtifact.sha256` | property | 39 | Property, not construction |
| `ProfileArtifact.contains()` | method | 41 | Query method, not construction |
| `SeedBlock.__post_init__` | method | 56 | Validation method, not construction |
| `SeedBlock.stop` | property | 60 | Property, not construction |
| `SeedBlock.contains()` | method | 62 | Query method, not construction |
| `ExperimentSpec.validate()` | method | 111 | Validation method, not construction |
| `ExperimentSpec.seed_block()` | method | 129 | Lookup method, not construction |
| `ExperimentSpec.as_dict()` | method | 134 | Serialization method, not construction |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 7 (1 utility function + 1 dataclass + 1 classmethod factory + 1 dataclass + 1 validator function + 1 dataclass + 1 classmethod factory) |
| Excluded top-level definitions | 19 (docstrings + stdlib imports + type import + methods + properties) |
| Source lines covered | 1–180 |
| Discovery matching lines | 8 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/provenance.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
