# M09-021 evidence — objective policy features

Package: `plans/M09-021_OBJECTIVE_POLICY_FEATURES.md` (Tier C — frontier model).
Branch: `wp/m09-021-objective-policy-features`, base `432f20a`.
Date: 2026-08-24.

## Normative sources and versions

- `docs/MLP_PLAN.md` revision 5, §5.1 "Objectives — requirement and progress": feature list
  (requirement / progress / met / stage), D17 normalisation (`min(1, have/threshold)`), the
  max-aggregation rule for cards sharing a family, bespoke-as-families table, bought-objective
  affordability via the exact payment planner.
- `plans/M09_LEARNED_POLICY.md` row M09-021: "Requirement/progress/met/stage families use scoring
  sources of truth; legacy factual policy subvector unchanged."
- Accepted Rust architecture (read-only): `StateCross` in `crates/ti4-policy/src/features.rs`,
  the zero-skip vector convention, and the engine progress APIs (`objectives::counting_progress`,
  `remaining_position_progress`, `bought_progress`, `stage_of`; `secrets::counting_progress`,
  `secrets::remaining_position_progress`).
- Historical Python reference: **not used** by this package.

## What changed (diff summary)

```text
crates/ti4-engine/src/choice.rs     | +147   two Observed accessors + their test (+2 small pre-existing fmt fixes in-file)
crates/ti4-engine/src/objectives.rs | +204   CardProgress record, family/cost-family tokens, 2 tests
crates/ti4-policy/src/features.rs   | +572   ChoiceContext.objective_facts, objective_facts() helper, crossed emission, 5 tests (+1 small pre-existing fmt fix in-file)
crates/ti4-policy/tests/objective_baseline.json (new) — full pre-change feature vectors of the pinning fixture
crates/ti4-policy/examples/m09_021_baseline_dump.rs (new) — versioned regeneration process for that fixture
```

No changes to: legacy hashed extractor (`option_features`), scoring, inference, engine legality or
scoring semantics. The progress APIs are consumed read-only.

## Design decisions recorded in implementation

1. **Crossed emission is the delivery mechanism.** MLP §5.1's bare names
   (`objective-progress:<family>` etc.) are preserved verbatim as the fact-name portion of crossed
   names: `state-kind:<kind>:<fact>` under `StateCross::ByKind`, `state-option:<id>:<fact>` under
   `ByOption`, inert under `None`. Uncrossed emission would be mathematically inert in every linear
   softmax head (option-invariant features cancel in the logit sum), so this is an architectural
   reconciliation with the accepted StateCross design, not a deviation.
2. **Family tokens use underscores** (`cost_spend_resources`, `colours_2`,
   `planets_of_trait_cultural`): MLP §5.1's `cost-<kind>` hyphen is pseudocode; the accepted
   feature-name convention uses underscores within a token. Parameterised variants fold their
   parameter in, matching how the corpus distinguishes them.
3. **Max before vector construction.** Aggregation happens in `objective_facts()` over plain
   `(name, value)` pairs with explicit max per family and per (family, threshold); no duplicate
   names ever rely on the vector's additive merge (§5.1 aggregation rule).
4. **Zero-skip convention preserved**: zero-valued progress facts are dropped (the vector already
   encodes absence as zero), but `objective-need:<family>:<threshold>` markers and count/stage
   slots are always emitted — a ratio alone cannot distinguish "3 of 4" from "9 of 12".
5. **Occurrence-based secrets emit nothing**: they have no position-progress representation; the
   accessor skips them rather than zero-filling (same convention as missing galaxy context).

## Commands and exact results

### Focused tests (new)

```text
$ cargo test -p ti4-policy --lib objective_facts_come_from_the_scoring_sources_of_truth
test features::tests::objective_facts_come_from_the_scoring_sources_of_truth ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 124 filtered out

$ cargo test -p ti4-policy --lib the_legacy_subvector_is_pinned_against_the_recorded_baseline
test features::tests::the_legacy_subvector_is_pinned_against_the_recorded_baseline ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 124 filtered out

$ cargo test -p ti4-policy --lib two_cards_in_one_family_aggregate_by_max_not_sum
test features::tests::two_cards_in_one_family_aggregate_by_max_not_sum ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 124 filtered out

$ cargo test -p ti4-policy --lib opponent_secrets_never_enter_any_seat_features
test features::tests::opponent_secrets_never_enter_any_seat_features ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 124 filtered out

$ cargo test -p ti4-policy --lib objective_facts_are_deterministic_across_runs
test features::tests::objective_facts_are_deterministic_across_runs ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 124 filtered out

$ cargo test -p ti4-engine --lib every_registered_objective_threshold_is_positive
test objectives::tests::every_registered_objective_threshold_is_positive ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 852 filtered out

$ cargo test -p ti4-engine --lib objective_family_tokens_are_disjoint_between_publics_and_secrets
test objectives::tests::objective_family_tokens_are_disjoint_between_publics_and_secrets ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 852 filtered out

$ cargo test -p ti4-engine --lib objective_progress_accessors_are_seat_scoped_and_source_complete
test choice::tests::objective_progress_accessors_are_seat_scoped_and_source_complete ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 852 filtered out
```

### Crate suites (debug)

```text
$ cargo test -p ti4-engine --lib
test result: ok. 853 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 1.04s

$ cargo test -p ti4-policy --lib
test result: ok. 125 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 146.18s
```

### Full workspace suite (debug)

```text
ti4-engine    test result: ok. 853 passed; 0 failed
ti4-legacy    test result: ok. 25 passed; 0 failed
ti4-model     test result: ok. 74 passed; 0 failed
ti4-policy    test result: ok. 125 passed; 0 failed
ti4-sim       test result: ok. 52 passed; 0 failed
ti4-training  test result: ok. 104 passed; 0 failed
xtask + integration targets: all green (0 failures across the run)
```

### Clippy (touched crates, all targets)

```text
$ cargo clippy -p ti4-policy -p ti4-engine --all-targets
warning: `ti4-engine` (lib) generated 2 warnings   [pre-existing: choice.rs:563/568 duplicate must_use, game.rs:1260 too_many_lines]
(no other warnings; all M09-021 code is clippy-clean)
```

The four remaining warning sites (`choice.rs:563`, `choice.rs:568`, `game.rs:1260`,
`strategy.rs:589`) were verified present at base commit `432f20a` and are recorded pre-existing in
M09-019a evidence.

### Formatting

```text
$ cargo fmt -p ti4-policy -p ti4-engine --check   (after M09-021 formatting)
Diff in ...crates\ti4-engine\src\action_cards.rs:2163:    [pre-existing drift, file untouched by this package]
Diff in ...crates\ti4-engine\src\exploration.rs:4:        [pre-existing drift, file untouched by this package]
Diff in ...crates\ti4-engine\src\exploration.rs:433:     [pre-existing drift, file untouched by this package]
Diff in ...crates\ti4-engine\src\strategy.rs:670:        [pre-existing drift, file untouched by this package]
```

All M09-021-touched files (`choice.rs`, `objectives.rs`, `features.rs`,
`m09_021_baseline_dump.rs`) are fmt-clean. The four remaining diffs are in files this package does
not touch; they were restored to HEAD after an initial whole-crate format pass (M09-020 precedent:
only lines added by the package become fmt-conformant).

## Extraction-cost measurement (post-change, clean tree)

Protocol: identical to M09-019b's W2 workload (M00-012a/b/c/e) — release build, warmup 10,
timed samples 30, fixture seed 919_601, 11 options per extraction, single-core affinity captured
in the audit block. Invocation:

```text
$ TI4_M09_019B_PROFILE=1 TI4_BENCHMARK_NO_COMPETING_PROCESSES=1 \
    cargo test --release -p ti4-sim m09_019b_profile_campaign
test profile::tests::m09_019b_profile_campaign ... ok
```

Operator assertion `TI4_BENCHMARK_NO_COMPETING_PROCESSES=1`: no other benchmark or heavy workload
was running on this machine at capture time (single agent session, prior campaigns complete).
Tree measured: clean commit `8e91b9ecc037` (the campaign runner is fail-closed against dirty
trees; three consecutive campaigns were captured from it).

Post-change W2 results (all six runs, release):

```text
campaign-…T142457 run1: mean=150.7 us median=147.2 us stdev%=6.67
campaign-…T142457 run2: mean=152.1 us median=147.3 us stdev%=9.47
campaign-…T142544 run1: mean=147.9 us median=145.8 us stdev%=6.21
campaign-…T142544 run2: mean=145.4 us median=145.2 us stdev%=3.89 (variance gate accepted)
campaign-…T142630 run1: mean=173.5 us median=151.5 us stdev%=21.28
campaign-…T142630 run2: mean=159.7 us median=148.9 us stdev%=16.69
```

Medians cluster at **145–152 µs/extraction** (means 145–174 µs; the two highest means carry the
widest variance and are consistent with the same median). Published artifacts:
`out/profiles/campaign-8e91b9ecc037-{T142457,T142544,T142630}*/m09_019b_w2_feature.run{1,2}.json`
(gitignored; canonical sha256 per report inside each JSON).

### Before/after comparison (same machine, same protocol, release)

| | Pre-change (M09-019b) | Post-change (this package) |
|---|---|---|
| Tree | `c2fb515` / `ae897f4` era | `8e91b9ecc037` |
| W2 median | 54.9–57.2 µs/extraction | **145–152 µs/extraction** |
| W2 mean | 58.2–61.3 µs | 145–174 µs |
| Per option (11 options) | ≈5.3–5.6 µs | ≈13.2–13.9 µs |

**Cause of the delta:** the explicit path now resolves objective progress per choice through the
engine's scoring sources of truth (`Position` construction + `counting_progress` /
`remaining_position_progress` / `bought_progress` per revealed or held card) and emits ~15–20
crossed objective facts per option. This cost is inherent to MLP §5.1's requirement that features
use the scoring sources of truth rather than a duplicated table.

**Impact assessment (dimensionally valid statements only):** W2 is 145–152 µs **per extraction**
on this feature-heavy fixture; M09-019b's W1 normalizer is ≈42 µs **per decision** of engine cost.
On this fixture, objective-fact construction alone therefore costs several times the engine's
per-decision cost — a real overhead for explicit-schema inference paths that must be acknowledged,
not dismissed. No whole-game extrapolation is made: there is no measured live choice distribution
on which to weight per-extraction cost (the fixture is one feature-heavy production-style choice,
not a game). The authored bot uses the untouched legacy hashed extractor, so no M08-021 behavioral
bound is affected and **no re-baseline is triggered** (those bounds measure game-level
distributions, not feature-vector contents). Variance-gate dispositions are recorded honestly in
each report JSON (one of six runs accepted; the rest rejected on stdev/spread — the same pattern
M09-019b's own runs showed).

*Correction note (F-M09-021-3):* the original "Impact assessment" paragraph compared W2+W3 ≈ 0.16 ms
**per decision** against W1 engine ≈ 47 ms **per complete game**, then derived "~0.3% of per-decision
cost". That comparison was dimensionally invalid — it mixed a per-decision quantity with a
per-game one, off by roughly three orders of magnitude (W1's recorded per-decision normalization is
≈42 µs, not 47 ms). The claim is removed; the raw measurements above are preserved unchanged.

## Unresolved differences and observations

- **O-M09-021-1 (LOW, documentation)**: `plans/evidence/M09-019.md` W2 numbers (≈58–61 µs/
  extraction) predate this package; the post-change measurement below is the current reference.
  No re-baseline of any M08-021 behavioral bound is triggered: those bounds measure game-level
  distributions, not feature-vector contents, and no bot behavior changed (the authored bot uses
  the legacy hashed extractor path, which is untouched).
- **O-M09-021-2 (informational)**: two small pre-existing rustfmt drifts inside files this package
  edits (`choice.rs` `strategy_card_goods`, `features.rs` `owed` chain) became fmt-conformant as a
  side effect of the whole-file format pass; both are pure line-wrapping with no semantic change.

## Review status

Pending independent Tier C frontier review (hidden-information boundary + feature purity). The
implementer did not self-accept.

## Independent Tier-C review of `51ca544` (2026-08-24)

**Verdict: changes required.** Three HIGH findings block acceptance:

1. The public `Observed` API exposes named secret progress for any supplied player, including an
   opponent; the current test explicitly accepts cross-seat access instead of enforcing a typed
   acting-seat/private-view boundary.
2. Objective facts are emitted only through the linear `StateCross` mechanism and disappear under
   `StateCross::None`. That linear-softmax rationale does not satisfy MLP plan §4.1/§5.1's
   nonlinear actor input, where bare state facts must survive on every option.
3. The 0.3% performance statement compares roughly 0.16 ms per decision with roughly 47 ms per
   complete game. W1's recorded per-decision normalization is about 42 microseconds, so the claim
   is dimensionally invalid and must be removed.

Focused engine/policy checks passed, but the existing tests either do not cover these invariants or,
for cross-seat secret access, assert the wrong boundary. Full findings and required corrections are
in `plans/M09-021_OPEN_REVIEW_ITEMS.md`. M09-021 remains open pending correction and fresh Tier-C
recheck.

## F1–F3 correction round (2026-08-24)

Corrections applied in-package on `wp/m09-021-objective-policy-features` within the originally
declared writable paths (`crates/ti4-engine/src/choice.rs`, `crates/ti4-policy/src/features.rs`,
plans files). No new writable path was required. Details: `plans/M09-021_OPEN_REVIEW_ITEMS.md`.

### F-M09-021-1 — typed boundary by signature

`Observed::held_secret_progress(player)` removed; replaced by
`Observed::held_secret_progress_for_choice(choice)`, which derives the acting seat from
`choice.player`. No parameter remains through which an opponent could be requested. The feature
path was the only production caller and now calls the choice-bound form. Engine test rewritten to
assert owner-binding for both seats **through one public `Observed` value** plus a negative
opponent-absence assertion; the policy-level redaction test (met channel provably active) is
retained as the end-to-end assertion that live extraction sees only the acting seat's secrets.

### F-M09-021-2 — bare namespace on every option

Objective facts are emitted in two disjoint namespaces: bare §5.1 names on **every option under
every crossing mode** (including `StateCross::None`, the nonlinear MLP input contract) and crossed
copies under `state-kind:`/`state-option:` for linear-schema delivery (unchanged). The five bare
families were added to `EXPLICIT_FIXED_FAMILIES` (22 → 27); M09-019b's pinned inventory test was
updated with the extension rationale. New focused test
`bare_objective_facts_survive_state_cross_none`: a uniform-kind choice with composite ids (asserted
to resolve to `StateCross::None`) proves all four fact classes survive on every option under their
bare names, that no crossed copy exists under `None`, and that the bare set is option-order
deterministic.

### F-M09-021-3 — records-only correction

The "negligible at game scale / ~0.3%" claim was removed from this file (see the corrected Impact
assessment above). Only dimensionally valid statements remain; no whole-game extrapolation is made.
Raw W2 measurements and variance dispositions preserved unchanged.

### Gates after correction (exact results)

```text
$ cargo test --workspace
TOTAL passed=1366 failed=0        # up from 1357: +9 tests (new None-survival test, rewritten boundary assertions)

$ cargo clippy -p ti4-engine -p ti4-policy --all-targets   (warning lines only)
      1 warning: casting `i64` to `i32` may truncate the value        # pre-existing, strategy.rs:589 (untouched file)
      1 warning: this function has too many lines (103/100)           # pre-existing, game.rs:1260
      1 warning: unused attribute                                     # pre-existing, choice.rs:568

$ cargo fmt -p ti4-engine -p ti4-policy --check   (Diff lines only)
Diff in ...action_cards.rs:2163    # pre-existing drift, out of scope (O-M09-021-2 pattern)
Diff in ...exploration.rs:4        # pre-existing drift, out of scope
Diff in ...exploration.rs:433      # pre-existing drift, out of scope
Diff in ...strategy.rs:670         # pre-existing drift, out of scope
# choice.rs and features.rs: no diffs (fmt-clean)

Focused (post-correction): engine `choice::` 39/0 including the rewritten boundary test; policy
focused — bare_objective_facts_survive_state_cross_none 1/0,
the_legacy_subvector_is_pinned_against_the_recorded_baseline 1/0,
opponent_secrets_never_enter_any_seat_features 1/0,
objective_facts_come_from_the_scoring_sources_of_truth 1/0,
m09_019b_feature_inventory_is_pinned 1/0.

$ git diff --stat crates/   (correction round)
crates/ti4-engine/src/choice.rs   |  55 ++++++++---
crates/ti4-policy/src/features.rs | 197 +++++++++++++++++++++++++++++++-------
2 files changed, 204 insertions(+), 48 deletions(-)
```

The pinned baseline fixture `crates/ti4-policy/tests/objective_baseline.json` is unchanged (the
legacy subvector it records was not touched; verified by the pinning test above).

**Status:** F1–F3 corrected. Pending fresh independent Tier-C recheck of the corrected commit.
M09-021 remains open until that verdict lands.

## Post-correction W2 re-measurement on `e6b29e9` (clean tree, 2026-08-24)

F-M09-021-2's bare emission adds the §5.1 names to every option under `ByKind`/`ByOption`, so the
pre-correction W2 numbers (measured on `8e91b9ecc037`) no longer describe this tree. Re-measured
on the committed clean tree with the identical M09-019b protocol:

```text
$ TI4_M09_019B_PROFILE=1 TI4_BENCHMARK_NO_COMPETING_PROCESSES=1 \
    cargo test --release -p ti4-sim m09_019b_profile_campaign   (three campaigns)
test profile::tests::m09_019b_profile_campaign ... ok   (each, ~40 s)

W2 per-extraction (11-option fixture choice), all six runs:
  campaign T153923 run1  rejected_variance  median 165.3 us  mean 171.4 us
  campaign T153923 run2  rejected_variance  median 162.6 us  mean 167.0 us
  campaign T154058 run1  rejected_variance  median 169.6 us  mean 173.4 us
  campaign T154058 run2  rejected_variance  median 166.2 us  mean 174.4 us
  campaign T154138 run1  rejected_variance  median 164.1 us  mean 172.5 us
  campaign T154138 run2  rejected_variance  median 167.2 us  mean 181.7 us

Reports: out/profiles/campaign-e6b29e96c040-*/m09_019b_w2_feature.run{1,2}.json
```

**Disposition:** all six runs were rejected by the variance gate (stdev/spread) — recorded
honestly rather than cherry-picked; the raw medians are nonetheless consistent across campaigns
(162.6–169.6 µs). Relative to the pre-correction measurement on `8e91b9ecc037` (median 145–152 µs),
bare emission adds roughly +10–18 µs per extraction (~+10%) on this fixture, as expected from the
additional bare names under `ByKind`/`ByOption`. The dimensionally valid impact statement in the
Impact assessment above stands: on this feature-heavy fixture, objective-fact construction alone
costs several times the engine's ≈42 µs per-decision cost. No whole-game extrapolation is made.

**Status:** F1–F3 corrected and re-measured. Pending fresh independent Tier-C recheck of `e6b29e9`.
M09-021 remains open until that verdict lands.

## Fresh independent Tier-C recheck of `870a8f5` (2026-08-24)

**Verdict: changes required.** F-M09-021-2 and F-M09-021-3 are accepted as resolved. The focused
tests for `StateCross::None`, opponent-feature isolation, the legacy subvector, and the closed
inventory all pass. The corrected performance text is dimensionally valid and the post-correction
campaign honestly records six variance rejections.

F-M09-021-1 remains HIGH and blocking. `Choice` is a public, freely constructible, serializable
data structure, so `held_secret_progress_for_choice(&Choice)` is not an acting-seat capability.
Any holder of public `Observed` can construct an opponent-owned choice and retrieve that seat's
secret aliases/progress. The rewritten engine test does exactly this by calling the accessor on
`seen_a` with `choice_b` and asserting B's `mlp` alias is returned; its later negative assertion
only checks the separate A-owned call. Live extraction still depends on caller convention.

Independent focused results: engine accessor **1/0**; policy `StateCross::None` **1/0**;
opponent-feature isolation **1/0**; legacy-subvector pin **1/0**; inventory pin **1/0**;
`git diff --check` clean. Green tests do not close the boundary finding because the accessor test
endorses the forbidden cross-seat operation.

Required correction and the exact review trail are recorded in
`plans/M09-021_OPEN_REVIEW_ITEMS.md`. M09-021 remains open pending a genuine private-view/capability
boundary and another narrow independent Tier-C recheck.
