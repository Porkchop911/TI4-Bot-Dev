# M00-004a16 — ML Observation Construction API Inventory

## Scope
`engine/ml/observation.py` (232 physical lines). Actor-visible, versioned observations for learned decision policies.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/observation.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\observation.py
  → 232
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\observation.py
  → 13 matching lines (1 annotated constant + 6 @dataclass decorators + 6 classes + 1 function)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\observation.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `OBSERVATION_SCHEMA_VERSION` (schema constant)
| Field | Value |
|---|---|
| Symbol | `OBSERVATION_SCHEMA_VERSION` |
| Kind | Module-level constant |
| Signature/Value | `1` |
| Source line | 12 |
| Construction role | Schema version identifier for BotObservation. Used in `BotObservation.from_view()` to set the `schema_version` field on constructed instances. |
| Rust relevance | Must be preserved in Rust observation construction; any schema change requires migration path. |

### 2. `UnitObservation` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `UnitObservation` |
| Kind | `@dataclass(frozen=True, order=True)` |
| Signature | `UnitObservation(type_id: str, owner: str, sustained_damage: bool, galvanized: bool)` |
| Source line | 16 |
| Construction role | Immutable observation of a single unit's visible state. 4 required fields: type_id, owner, sustained_damage, galvanized. `order=True` enables natural comparison. |
| Rust relevance | Immutable struct with 4 required fields. Used as element in `space_units` and `planet_forces` tuples within `SystemObservation`. |

### 3. `PlanetForcesObservation` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `PlanetForcesObservation` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `PlanetForcesObservation(planet: str, units: tuple[UnitObservation, ...])` |
| Source line | 24 |
| Construction role | Immutable observation of forces on a single planet. 2 required fields: planet (system name), units (sorted tuple of UnitObservation). |
| Rust relevance | Immutable struct with 2 fields. Used as element in `planet_forces` tuple within `SystemObservation`. |

### 4. `SystemObservation` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `SystemObservation` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `SystemObservation(system: str, command_tokens: tuple[str, ...], planet_control: tuple[tuple[str, str], ...], space_units: tuple[UnitObservation, ...], planet_forces: tuple[PlanetForcesObservation, ...])` |
| Source line | 30 |
| Construction role | Immutable observation of a single system's visible state. 5 required fields: system (name), command_tokens (sorted), planet_control (sorted), space_units (sorted), planet_forces (sorted by planet name). |
| Rust relevance | Immutable struct with 5 required fields. Used as element in `systems` tuple within `BotObservation`. |

### 5. `PlayerObservation` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `PlayerObservation` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `PlayerObservation(id: str, faction: str, seat: int, home_system: str | None, home_planets: tuple[str, ...], strategy_card: str | None, strategy_card_exhausted: bool, tactic_tokens: int, fleet_tokens: int, strategic_tokens: int, passed: bool, victory_points: int, trade_goods: int, commodities: int, action_card_count: int, secret_objective_count: int, plot_count: int, technologies: tuple[str, ...], exhausted_technologies: tuple[str, ...], breakthrough: str | None, relic_fragments: tuple[tuple[str, int], ...], relics: tuple[str, ...], public_objectives_forbidden: bool, leaders: tuple[tuple[str, str], ...], controlled_planets: tuple[tuple[str, str], ...], scored_objectives: tuple[str, ...], captured_unit_count: int)` |
| Source line | 39 |
| Construction role | Immutable observation of a single player's visible state. 28 required fields covering: identity (id, faction, seat), home systems (home_system, home_planets), strategy cards (strategy_card, strategy_card_exhausted), tokens (tactic/fleet/strategic), flags (passed, public_objectives_forbidden), resources (victory_points, trade_goods, commodities), card counts (action/secret_objective/plot), technologies (technologies, exhausted_technologies, breakthrough), relics (relic_fragments, relics), leaders, controlled planets, scored objectives, captured units. All tuple fields are sorted. |
| Rust relevance | Immutable struct with 28 required fields. Most complex observation type; Rust equivalent must carry all fields identically. Used as element in `players` tuple within `BotObservation`. |

### 6. `PrivateObservation` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `PrivateObservation` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `PrivateObservation(action_cards: tuple[str, ...], secret_objectives: tuple[str, ...], plots: tuple[str, ...])` |
| Source line | 70 |
| Construction role | Immutable observation of private player state visible only to the viewing actor. 3 required fields: action_cards, secret_objectives, plots. Constructed in `BotObservation.from_view()` from the viewer's own private data. |
| Rust relevance | Immutable struct with 3 required fields. Critical for privacy boundary — contains opponent-hidden information. |

### 7. `BotObservation` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `BotObservation` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `BotObservation(schema_version: int, viewer: str, round: int, phase: str, active: str | None, active_system: str | None, pending: str | None, speaker: str, seating_order: tuple[str, ...], initiative_order: tuple[str, ...], unclaimed_strategy_cards: tuple[str, ...], strategy_card_goods: tuple[tuple[str, int], ...], players: tuple[PlayerObservation, ...], private: PrivateObservation, systems: tuple[SystemObservation, ...], exhausted_planets: tuple[str, ...], revealed_objectives: tuple[str, ...], laws: tuple[tuple[str, str], ...], custodians_removed: bool, frontier_tokens: tuple[str, ...], expedition_slices: tuple[tuple[str, str], ...], fracture_in_play: bool, ingress_tokens: tuple[str, ...], wormhole_tokens: tuple[tuple[str, str], ...], breach_tokens: tuple[str, ...], thunders_edge_system: str | None, galactic_event: str | None, support_holders: tuple[tuple[str, str], ...])` |
| Source line | 77 |
| Construction role | Immutable observation of everything a learned policy may inspect at one decision. 30 required fields covering: schema version, viewer identity, game state (round, phase, active, speaker, seating, initiative), resources (strategy cards, tokens), player observations (players, private), system observations (systems, exhausted_planets), objectives/laws, game flags (custodians_removed, frontier_tokens, expedition_slices, fracture_in_play, ingress_tokens, wormhole_tokens, breach_tokens, thunders_edge_system, galactic_event, support_holders). All tuple fields are sorted. |
| Rust relevance | Core observation type; Rust equivalent must carry all 30 fields identically. Construction via `from_view()` classmethod requires GameView dependency, privacy validation (opponent cards/secret_objectives must be HIDDEN), and full state extraction. Critical for ML training data collection and policy evaluation. |

### 7a. `BotObservation.from_view()` (classmethod factory)
| Field | Value |
|---|---|
| Symbol | `BotObservation.from_view` |
| Kind | `@classmethod` factory |
| Signature | `from_view(cls, view: GameView) -> BotObservation` |
| Source line | 105 |
| Construction role | Factory that constructs a BotObservation from a GameView. Validates: view is GameView instance; opponent action_cards and secret_objectives are all HIDDEN (privacy assertion). Extracts all state from view.state, constructing PlayerObservation via `_player()`, SystemObservation via `_system()`, PrivateObservation from viewer's own data. Returns fully constructed BotObservation. |
| Rust relevance | Rust constructor `BotObservation::from_view(view: &GameView) -> Result<Self, Error>`. Must implement identical privacy validation and state extraction pipeline. Depends on `GameView` (from `engine.views`). Critical for ML training data collection. |

### 8. `observation_for()` (observation factory function)
| Field | Value |
|---|---|
| Symbol | `observation_for` |
| Kind | Module-level function |
| Signature | `observation_for(view: GameView) -> BotObservation` |
| Source line | 231 |
| Construction role | Thin wrapper that delegates to `BotObservation.from_view(view)`. Provides a module-level entry point for observation construction. |
| Rust relevance | Rust function `fn observation_for(view: &GameView) -> Result<BotObservation, Error>`. Thin delegation wrapper. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `_unit` | private function | 177 | Private function (leading underscore), not public API |
| `_system` | private function | 183 | Private function (leading underscore), not public API |
| `_player` | private function | 199 | Private function (leading underscore), not public API |
| `__future__` import | import | 3 | Future annotations import |
| `hashlib` import | import | 5 | Stdlib import, not engine API |
| `json` import | import | 6 | Stdlib import, not engine API |
| `asdict` import | import | 7 | Stdlib import, not engine API |
| `dataclass` import | import | 7 | Stdlib import, not engine API |
| `GameState` import | import | 9 | External type dependency from `engine.state`, not constructed here |
| `Player` import | import | 9 | External type dependency from `engine.state`, not constructed here |
| `SystemState` import | import | 9 | External type dependency from `engine.state`, not constructed here |
| `GameView` import | import | 10 | External type dependency from `engine.views`, not constructed here |
| `HIDDEN` import | import | 10 | External constant from `engine.views`, not constructed here |
| `BotObservation.canonical_json()` | method | 147 | Serialization method, not construction |
| `BotObservation.digest` | property | 151 | Property, not construction |
| `BotObservation.__post_init__` | method | 99 | Validation method, not construction |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 8 (1 constant + 6 dataclasses + 1 classmethod factory + 1 function) |
| Excluded top-level definitions | 18 (docstrings + private functions + stdlib imports + type imports + methods + property) |
| Source lines covered | 1–232 |
| Discovery matching lines | 13 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/observation.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
