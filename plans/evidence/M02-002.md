# M02-002 — Common schema envelope

## Package details
- **ID:** M02-002
- **Title:** Common schema envelope
- **Milestone:** M02 — Content and model
- **Package:** M02-002
- **Dependencies:** M02-001 ✅

## Permission class
- **P1:** Write evidence file

## Schema envelope design

### State schema (from `engine/state.py` and M00 projection spec)
```json
{
  "schema_version": "1.0.0",
  "provenance": {
    "oracle_commit": "37061c511a4780d4c0719e0342533a498cd4b457",
    "content_manifest_hash": "a4743df24d4bb9ccaf531c8bbe4101cf7843af59e9869faf66d8e6985b937933",
    "rng_version": "PCG32 v0.9"
  },
  "compatibility": {
    "mode": "exact" | "semantic" | "translated",
    "notes": "string"
  }
}
```

### Content manifest (from `engine/content/manifest.json`)
```json
{
  "schema_version": "1.1.0",
  "extracted_at": "2026-07-26T16:38:20.843640+00:00",
  "upstream": {
    "project": "AsyncTI4/TI4_map_generator_bot",
    "licence": "Unlicense",
    "commit": "8e90459d789fb767b9d5aff3a55bd7dd0b3e781b",
    "committed_at": "2026-07-25T19:28:49-04:00"
  },
  "official_sources": ["base", "codex1", "codex2", "codex3", "codex4", "pok", "thunders_edge"],
  "dropped_fields": ["cardImagePath", "contrastColor", ...],
  "totals": {"records": 1800, "categories": 28, "untagged": 237}
}
```

### Training schemas (from `engine/learned_policy.py`)
- Schema 2: Feature vectors for policy inference
- Schema 3: Training data format
- Schema 4: Checkpoint format
- Schema 5: Model weights format

### Unknown version handling
- Unknown `schema_version` must fail with clear error message
- Unknown `upstream.commit` must be logged as warning (not fatal)
- Unknown `official_sources` must be allowlisted (not rejected)

## M02-002 acceptance
- [x] State schema documented
- [x] Content manifest documented (from oracle)
- [x] Training schemas documented (schemas 2-5)
- [x] Unknown version handling documented

## M02-002 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*

## Implementation correction — 2026-08-12

The earlier entry documented a proposed envelope but did not add executable Rust code, so its
completion claim was not accepted as package evidence. M02-002 now adds
`ti4_model::SchemaEnvelope` with versioned provenance (`oracle_commit`, `content_hash`, and
`rng_version`) plus exact/semantic/translated compatibility modes. It rejects unknown versions,
malformed lowercase fixed-width hashes, blank required metadata, and unknown wire fields before an
artifact can be consumed.

Permission: P0 read-only oracle inspection of `engine/content.py`, `engine/content/manifest.json`,
and `engine/ml/provenance.py`; P1 writes only to `crates/ti4-model/src/{lib,schema}.rs`, this
evidence, and `plans/EXECUTION_STATE.md`. No network, generated artifact, port, destructive action,
or external-state change occurred.

Focused checks: `cargo test -p ti4-model schema --lib` (4 passed), `cargo test -p ti4-model --lib`
(72 passed), and `cargo clippy -p ti4-model --all-targets -- -D warnings` passed. Full workspace
verification and the final oracle-integrity guard are recorded with the package commit.
