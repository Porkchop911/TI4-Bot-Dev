# M09-024b1 — MLP input projection, bare-seat family, registry migration

**Specified by:** the Tier-C architecture ruling and its clarification in
`plans/M09-024b_ARCHITECTURE_EVALUATION_REQUEST.md` (2026-08-25), which names this package's
contents directly. No separate package document; the ruling is the specification.
**Branch:** `wp/m09-022-ability-decomposition-features`
**Base:** `5293f88`
**Author:** Claude Opus 5 — not eligible to review this package.
**Permission class:** P1 — no scoped access, no downloads, no artifact.
**Review tier:** C — schema.

## What the ruling required, and where each part is

| required | delivered |
|---|---|
| implement and pin the MLP projection | `crates/ti4-policy/src/projection.rs` |
| bare-seat family | `seat-state:<fact>`, all eight facts, on every option under every crossing mode |
| registry migration | `OOV_FAMILIES_V2` = v1 in place + `seat-state` appended; `OOV_REGISTRY_VERSION` 1 → 2 |
| pre-lookup suppression | names dropped in `project_vector` before any column is consulted |
| tests: `StateCross::ByOption` | `the_seat_facts_survive_by_option_under_the_bare_family` |
| tests: redaction | `opponent_secrets_do_not_survive_the_projection_either` |
| tests: excluded-family absence | `the_projection_suppresses_every_unbounded_cross` |
| tests: unchanged legacy/schema-4 vectors | `the_schema_four_vector_is_untouched` + both existing pins |
| pin the dead rows' inactive classification | `the_suppressed_families_keep_dead_reserved_rows` |

## Design decisions worth a reviewer's attention

**A projection, not a change to the extractor.** The MLP takes a *view* of the schema-4 vector.
`explicit_choice_features` is byte-for-byte what it was, so the six trained champions, the
legacy-subvector pin and the M09-019b inventory pin are all untouched. This is why the bare seat
family lives in `projection.rs` and not in `features.rs`: emitting `seat-state:` from the extractor
would have moved the schema-4 vector, which the ruling explicitly forbids ("while leaving the
existing schema-4 vector unchanged").

**One new public seam in `features.rs`**: `FeatureVector::from_pairs`, because a projection that
derives a second view needs to build a vector without going through the extractor. `seat_facts` was
also made `pub` so the projection can read the eight facts rather than re-deriving them — a second
implementation of the same eight would be a place for them to drift apart.

**Suppressed, not routed.** An excluded name is dropped, not collapsed into its family OOV column.
Collapsing 186,088 distinct names into three columns would inject a dense signal that means
nothing. The three reserved rows are retained so no v1 index moves, and are classified dead:
`dead_reserved_families()` / `is_dead_reserved()` exist so a reader asking why three columns are
always zero finds the answer in the code. M09-026/M09-028 must zero them, mask them from
optimization, and assert them zero at save/load alongside free rows.

**Why v2 appends rather than sorts.** Sorting `seat-state` in would place it between `route` and
`state-kind` and move every reserved column after it. Appending keeps v1's reserved indices exactly
where they were. The migration still shifts the *ordinary* columns that follow the reserved block,
and that is affordable for exactly one reason: **no v1 artifact or tensor exists** — M09-024b wrote
none. The doc comment records that after the first artifact this becomes a full reviewed
tensor/layout migration.

**The consequence of appending, handled as the clarification directed.** The frozen list is no
longer sorted, so the old order-based drift test would fail for the wrong reason. It is split:
`the_frozen_registry_covers_the_live_grammar` compares as a **set**, and
`the_reserved_order_is_pinned_and_v2_preserves_every_v1_index` pins the order *and* checks
element-by-element that v2 preserves every v1 index — which is the migration's actual claim, and
stronger than what the single test asserted before.

## A defect the tests caught

`projecting_a_name_set_agrees_with_projecting_a_vector` failed on first run. The seat-state facts
were being added with `FeatureKey::of(name)`, which computes a key but does **not** register the
name. The values were present in the vector and `value_of` found them — the `ByOption` test passed
— but `names_of` resolved those keys to the empty string, so the eight facts were invisible to
anything that reads a vector by name. M09-024b2 builds the vocabulary from names, so the columns
would have been missing from `slots.json` while the model asked for them.

Fixed by using `intern::register`. Recorded because the test that caught it exists precisely to
check that the two paths agree, and because a passing `ByOption` test alongside a broken name path
is exactly the kind of half-covered property this chain keeps finding.

## Tests — ten (seven new in `projection`, three changed or new in `vocabulary`)

| test | what it establishes |
|---|---|
| `the_projection_suppresses_every_unbounded_cross` | no excluded family survives; asserts the unprojected vector *does* carry them first |
| `the_seat_facts_survive_by_option_under_the_bare_family` | the ruling's correction, on the branch it named; asserts a non-zero fact exists first |
| `the_seat_facts_are_present_under_every_crossing_mode` | `ByKind`, `ByOption` and `None` all carry all eight |
| `the_schema_four_vector_is_untouched` | extractor output identical before/after; no `seat-state:` in it |
| `the_projection_keeps_state_kind_and_the_bounded_families` | the predicate excludes a shape, not all crosses |
| `opponent_secrets_do_not_survive_the_projection_either` | the projection cannot widen the hidden-information boundary |
| `projecting_a_name_set_agrees_with_projecting_a_vector` | vocabulary-by-name and model-by-vector agree — the test that caught the defect above |
| `the_frozen_registry_covers_the_live_grammar` | set-based coverage |
| `the_reserved_order_is_pinned_and_v2_preserves_every_v1_index` | order pinned; every v1 index preserved |
| `the_suppressed_families_keep_dead_reserved_rows` | dead rows retained, classified, and every other family live |

Each carries a non-vacuity precondition where one is possible.

## Gates

```
cargo test -p ti4-policy --lib projection    7 passed, 0 failed
cargo test -p ti4-policy --lib vocabulary   26 passed, 0 failed   (24 before)
cargo test --workspace                    1415 passed, 0 failed   (1406 before)
cargo clippy -p ti4-policy --all-targets   0 warnings in projection.rs or vocabulary.rs
rustfmt --edition 2024 --check             clean
git diff --check                           clean
```

Both pins pass unmodified: the legacy subvector did not move, and the schema-4 inventory is
unchanged — the projection adds no name to it.

## Limitations

1. **`EXCLUDED_FAMILIES` is a list behind a predicate.** The ruling states the predicate as a shape
   ("the Cartesian product of two free lexical identities, or of a full option identity and a state
   fact"), which is not mechanically checkable from a family name. `is_unbounded_cross` is the
   current grammar's answer to it, consulted at one place so a new family's admission is a
   deliberate edit. A future family with either shape is excluded only if someone adds it here —
   the ruling makes that an architecture-review obligation, and code cannot enforce it.
2. **No capacity figure.** This package changes what the vocabulary will contain but builds no
   corpus. `V_cap` is M09-024b2's to derive and confirm ≤ 24,576.
3. **The dead rows are classified, not yet zeroed.** Zero-initialisation, optimizer masking and
   save/load assertions live on the tensor and are M09-026/M09-028 obligations.

## Open items

| ID | Severity | Item |
|---|---|---|
| O-M09-024b1-1 | INFO | The unbounded-cross predicate is enforced by a list, not by a checkable property (Limitation 1). |
| O-M09-024b1-2 | INFO | Dead-row zeroing/masking deferred to the tensor packages (Limitation 3). |

## Status

Implementation complete; gates green. **Pending independent Tier-C review.** M09-024b2 follows on
acceptance and is P2 — it needs a scoped-access declaration before it starts.

## Independent Tier-C review of `4f63973` (2026-08-25)

**Changes required.** The bare-seat delivery, pre-lookup suppression for the three named crosses,
unchanged schema-4 extractor, and v2 prefix construction are sound. Two HIGH schema findings block
acceptance:

1. admission is an open-default three-name deny-list. It admits new families automatically and
   already admits the legacy-only `kind-faction` and `option-faction` names from source (a), while
   omitting their two unreachable OOV rows from dead-row metadata;
2. v2 is derived from v1 and tested against that same v1, so reordering v1 passes both set coverage
   and prefix tests. Neither version has an independent ordered fingerprint/fixture.

Full required corrections and dispositions are in `plans/M09-024b1_OPEN_REVIEW_ITEMS.md`.
Independent gates: projection **7/0**, vocabulary **26/0**, full policy library **169/0**; scoped
Clippy only reports the documented pre-existing engine warning. M09-024b1 remains open and
M09-024b2 blocked.

## F-M09-024b1-1 / F-M09-024b1-2 correction (implementer, 2026-08-25)

Both findings are correct. F1 in particular is an inversion of the ruling's own words: it says an
unclassified family "is excluded by default and requires another architecture review to enter the
dense input", and I implemented a deny-list, which admits by default. I quoted that sentence in the
module doc while writing the opposite of it.

### F-M09-024b1-1 — admission is now closed by default

`EXCLUDED_FAMILIES` is gone. In its place is `FAMILY_ROLES`, a **total** classification of all 39
registered families into `Transferable`, `UnboundedCross` and `LegacyOnly`. `role_of` returns
`Option`, and `None` — a family nobody classified — is not admitted. A family nobody classified is
a family nobody decided to put in the model.

The table is written out rather than derived from `EXPLICIT_FIXED_FAMILIES`, for the same reason
the OOV registry is: deriving it would admit a newly added family to the dense input as a side
effect of an ordinary grammar edit. `the_classification_covers_exactly_the_registry` fails when the
table and the registry drift, and its message says the decision is an architecture one rather than
a test to make green.

**The two legacy-only families the finding named.** `kind-faction` and `option-faction` are never
emitted by the schema-4 explicit path — the explicit test asserts exactly that — but they *are* in
the r6 checkpoint, which is discovery source (a). The old `admits` let them through, so M09-024b2
would have carried roughly 6,188 stale columns and could have reproduced the contaminated capacity
instead of deriving the corrected one. They are now `LegacyOnly` and rejected.

**Dead rows are five, not three.** `inactive_families()` returns both non-transferable roles, and
`is_dead_reserved` is defined against the classification rather than against the crosses alone.
M09-026/M09-028's zeroing and masking inventory is corrected accordingly.

### F-M09-024b1-2 — each version has an independent ordered fingerprint

The finding is exact: `OOV_FAMILIES_V2` is built from `OOV_FAMILIES_V1` and the order test compared
v2 back to its own source, so swapping two v1 entries moved both together and every assertion
stayed green. The migration removed the sorted comparison's order-sensitivity without replacing it
— the same failure M09-024a's F1 correction existed to prevent, reintroduced one package later by
the fix for it.

`registry_fingerprint` is SHA-256 over the ordered names, and each version pins its own:

```
v1  7bde13aa2972405de8944f3fdb9593453f3efb34f7f90817374658e8dbdc7a04
v2  8bb0d25c5c49d9c751a2385016b3c3dcd1a70b86fcd856f1508148de1a5006ac
```

Neither is derivable from the other. The set-coverage and v1-prefix assertions are kept alongside.

### Falsification checks — one per finding

**F2, swapping two v1 entries with the version unchanged:**

```
test vocabulary::tests::the_reserved_order_is_pinned_and_v2_preserves_every_v1_index ... FAILED
    the ordered v1 registry changed. Reserved model rows are addressed by this order;
    a reorder is a migration, not an edit.
test result: FAILED. 25 passed; 1 failed
```

Under the previous code this mutation passed every registry test. That is the gap closed.

**F1, reclassifying a legacy-only family as transferable:**

```
test projection::tests::legacy_only_checkpoint_names_are_rejected ... FAILED
test projection::tests::every_inactive_family_is_reported_and_every_other_is_live ... FAILED
test result: FAILED. 10 passed; 2 failed
```

Both reverted; 174/0 on the reverted tree.

### Tests — five added, twelve in `projection`

`the_classification_covers_exactly_the_registry`, `an_unclassified_family_is_not_admitted`,
`legacy_only_checkpoint_names_are_rejected` (with a non-vacuity check that the same call keeps a
transferable name), `every_inactive_family_is_reported_and_every_other_is_live`, and
`the_unit_suffix_rule_resolves_to_one_role` — the `<kind>-unit` families share one registry entry,
so they must share one role rather than falling through to unclassified.

### Gates after the correction

```
cargo test -p ti4-policy --lib projection   12 passed, 0 failed   (7 before)
cargo test -p ti4-policy --lib vocabulary   26 passed, 0 failed
cargo test -p ti4-policy --lib             174 passed, 0 failed   (169 before)
cargo test --workspace                    1420 passed, 0 failed   (1415 before)
cargo clippy -p ti4-policy --all-targets    0 warnings in either file
rustfmt --edition 2024 --check              clean
git diff --check                            clean
```

### Dispositions

F-M09-024b1-1 and F-M09-024b1-2 resolved. O-M09-024b1-1 is closed rather than carried: it was
escalated into F1 and the deny-list it described no longer exists. O-M09-024b1-2 stands as
deferred, with its inventory corrected from three families to five.

**One consequence worth flagging for M09-024b2.** With the legacy-only families now rejected, the
corrected single-path union loses roughly 6,188 further names beyond the three crosses. The derived
capacity is likely to land at **16,384** rather than the 24,576 ceiling — which the clarification
already anticipates ("may therefore derive 16,384"). 024b2 measures it; nothing here assumes it.

Requesting a fresh independent Tier-C recheck. M09-024b1 remains open and M09-024b2 blocked.

## Fresh independent Tier-C recheck of `0b8bd8e` (2026-08-25) — changes required

The two original findings are substantively corrected and independent focused gates pass
(projection 12/0, vocabulary 26/0), but `role_of` still treats any previously unknown family whose
name ends in `-unit` as transferable. That leaves admission open by suffix rather than bounding it
to canonical decision kinds. **F-M09-024b1-3 HIGH** is recorded in
`plans/M09-024b1_OPEN_REVIEW_ITEMS.md`, together with the stale five-row/1,280-weight documentation
correction. The operator override is not converted into reviewer acceptance; M09-024b1 remains
open and M09-024b2 review-blocked.
