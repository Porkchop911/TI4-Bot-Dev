# M00-004b3g — export/extract/collect tools CLI entry points

## Package details
- **ID:** M00-004b3g
- **Title:** Inventory export/extract/collect tools CLI entry points
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004b (Module entry points and CLI tools)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Inventory every export/extract/collect tool's CLI entry point and purpose.

## Oracle read scope (8 files; 2 are library modules)
| Tool | Lines | Purpose | CLI? |
|---|---|---|---|
| `export_strategy_model.py` | ~100 | Export strategy model artifacts | Yes |
| `export_tactical_macro_model.py` | ~100 | Export tactical macro model artifacts | Yes |
| `extract_asyncti4.py` | ~220 | Extract AsyncTi4 game data | Yes |
| `collect_ml_rollout_pilot.py` | ~450 | Collect ML rollout pilot data | Yes |
| `collect_tactical_macro_pilot.py` | ~330 | Collect tactical macro pilot data | Yes |
| `collect_tactical_plan_pilot.py` | ~180 | Collect tactical plan pilot data | Yes |
| `decision_capture.py` | ~80 | Decision capture library (no CLI) | No |
| `label_decisions.py` | ~250 | Label decisions for training | Yes |
| `ml_choice_sets.py` | ~60 | ML choice sets library (no CLI) | No |
| `ml_manifest.py` | ~80 | ML manifest operations | Yes |

## Permission class
- **P0:** Read oracle files
- **P1:** Write evidence file

## CLI pattern
All 8 CLI tools follow the same CLI pattern:
```python
if __name__ == "__main__":
    raise SystemExit(main())  # or: main()

def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    # ... add export/extract/label arguments ...
    args = parser.parse_args(argv)
    # ... run operation ...
    return 0
```

## Compatibility class
- **CLI interface**: `semantic` — Rust will have its own CLI.
- **Export formats**: `exact` — Rust must produce compatible model artifacts.
- **Decision capture**: `exact` — Rust must produce compatible capture format.
- **ML manifests**: `exact` — Rust must produce compatible manifests.

## Non-goals
- No implementation of export/extract logic; documentation-only inventory.

## Evidence
- Files read: 10 files (~1,850 lines total)
- 8 CLI tools + 2 library modules
- All CLI tools have: argparse CLI

## Completion
- [x] All CLI entry points catalogued
- [x] All init signatures documented
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
