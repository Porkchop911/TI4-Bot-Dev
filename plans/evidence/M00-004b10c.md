# M00-004b10c — tools/evaluate_research_model.py Entry Point Inventory

## Scope
`tools/evaluate_research_model.py` (85 lines). Frozen research-family M1 evaluation on disjoint targeted K=8 holdout.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/evaluate_research_model.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\evaluate_research_model.py
  → 85
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\evaluate_research_model.py
  → 5 matching lines (1 module constant set + 1 private function + 1 public function + 3 module constants)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\evaluate_research_model.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main(argv)` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main(argv: list[str] | None) -> int` |
| Kind | Module-level function |
| Source line | 31 |
| Construction role | Parses args via `argparse.ArgumentParser`, loads train/holdout JSONs, validates research choice family via `_is_research_choice()`, calls `pair_rows()` for train/holdout, validates seed overlap, computes ridge predictions via `_m1_predictions()`, builds report with `_metrics()`, `_label_split_half()`, `_cluster_bootstrap()`, `_diagnostics()`, writes atomic JSON report to `--output`, prints report JSON. Returns 0 or parser.error(). |

### 2. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 85 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--train` | Path | `DEFAULT_TRAIN` | Training panel JSON |
| `--holdout` | Path | `DEFAULT_HOLDOUT` | Holdout panel JSON |
| `--output` | Path | `DEFAULT_OUTPUT` | Output report path |

## Module-level constants
| Symbol | Value | Line |
|---|---|---|
| `DEFAULT_TRAIN` | `ROOT/out/ml_research_validation_240x4.json` | 23 |
| `DEFAULT_HOLDOUT` | `ROOT/out/ml_research_holdout_240x8.json` | 24 |
| `DEFAULT_OUTPUT` | `ROOT/out/ml_research_holdout_report.json` | 25 |
| `VARIANT` | `"full_bilinear"` | 26 |
| `ALPHA` | `300.0` | 27 |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `ROOT` | module constant | 11 | Module-level path constant (sys.path manipulation) |
| `_is_research_choice` | private function | 30 | Private (leading underscore) |
| `__future__` import | import | 3 | Future annotations import |
| `argparse` import | import | 5 | Stdlib import |
| `json` import | import | 6 | Stdlib import |
| `sys` import | import | 7 | Stdlib import |
| `Path` import | import | 8 | Stdlib import |
| `numpy` import | import | 10 | External dependency |
| `_cluster_bootstrap` import | import | 13 | External function from tools.evaluate_ml_audit |
| `_diagnostics` import | import | 13 | External function from tools.evaluate_ml_audit |
| `_label_split_half` import | import | 13 | External function from tools.evaluate_ml_audit |
| `_m1_predictions` import | import | 13 | External function from tools.evaluate_ml_audit |
| `write_atomic_json` import | import | 16 | External function from tools.ml_manifest |
| `_metrics` import | import | 17 | External function from tools.train_m1_pilot |
| `pair_rows` import | import | 17 | External function from tools.train_m1_pilot |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (1 entry point function) |
| Excluded top-level definitions | 18 (docstrings + module constants + private function + stdlib imports + external imports) |
| Module-level constants inventoried | 5 (DEFAULT_TRAIN, DEFAULT_HOLDOUT, DEFAULT_OUTPUT, VARIANT, ALPHA) |
| Source lines covered | 1–85 |
| Discovery matching lines | 5 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/evaluate_research_model.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b10.
