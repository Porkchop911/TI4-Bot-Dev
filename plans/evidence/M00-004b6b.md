# M00-004b6b — tools/compare_m3_ridge.py Entry Point Inventory

## Scope
`tools/compare_m3_ridge.py` (126 lines). Cluster-aware comparison of one frozen M3 checkpoint with macro ridge.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/compare_m3_ridge.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\compare_m3_ridge.py
  → 126
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\compare_m3_ridge.py
  → 5 matching lines (1 private function + 1 private function + 1 private function + 1 public function + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\compare_m3_ridge.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main(argv)` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main(argv: list[str] | None) -> int` |
| Kind | Module-level function |
| Source line | 67 |
| Construction role | Parses args via `argparse.ArgumentParser`, loads train/evaluation/checkpoint JSONs, decodes `tactical_macro_choice_sets()`, loads M3 checkpoint via `torch.load()`, builds `SparseFeatureEncoder` + `DeepSetOptionScorer`, computes M3 scores via `_predict()`, computes ridge predictions via `_prediction()`, computes per-faction metrics via `_metrics()`, runs cluster bootstrap, writes atomic JSON report to `--output`, prints Spearman intervals. Returns 0 or SystemExit on mismatch. |

### 2. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 126 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--train` | Path (append, required) | **required** | Training payload JSON files (repeatable) |
| `--evaluation` | Path | **required** | Evaluation JSON file |
| `--checkpoint` | Path | **required** | M3 checkpoint file |
| `--output` | Path | **required** | Output report path |
| `--bootstrap-replicates` | int | 5_000 | Bootstrap replicate count |
| `--seed` | int | 52 | Random seed |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `ROOT` | module constant | 11 | Module-level path constant (sys.path manipulation) |
| `_m3_pair_predictions` | private function | 19 | Private (leading underscore) |
| `_spearman` | private function | 31 | Private (leading underscore) |
| `_cluster_bootstrap` | private function | 37 | Private (leading underscore) |
| `__future__` import | import | 3 | Future annotations import |
| `argparse` import | import | 5 | Stdlib import |
| `itertools` import | import | 6 | Stdlib import |
| `json` import | import | 7 | Stdlib import |
| `math` import | import | 8 | Stdlib import |
| `sys` import | import | 9 | Stdlib import |
| `Path` import | import | 10 | Stdlib import |
| `numpy` import | import | 12 | External dependency |
| `torch` import | import | 13 | External dependency |
| `spearmanr` import | import | 14 | External dependency |
| `DeepSetOptionScorer` import | import | 16 | External class from tools |
| `_prediction` import | import | 17 | External function from tools |
| `SparseFeatureEncoder` import | import | 18 | External class from tools |
| `tactical_macro_choice_sets` import | import | 18 | External function from tools |
| `write_atomic_json` import | import | 21 | External function from tools |
| `_metrics` import | import | 22 | External function from tools |
| `_predict` import | import | 23 | External function from tools |
| `macro_pair_rows` import | import | 24 | External function from tools |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (1 entry point function) |
| Excluded top-level definitions | 23 (docstrings + module constant + private functions + stdlib imports + external imports) |
| Source lines covered | 1–126 |
| Discovery matching lines | 5 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/compare_m3_ridge.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b6.
