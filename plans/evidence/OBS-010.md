# Evidence — OBS-010, critic alignment

See `plans/OBS-010_CRITIC_ALIGNMENT.md` for design; this records checks.

## Changed paths

- `crates/ti4-policy/src/critic.rs` — two new critic-local fact groups (`actor_inventory_facts`,
  `opponent_slot_facts`) wired into `critic_facts`, unconditionally (matching the policy path's
  own ungated treatment of the facts they mirror).
- `plans/OBS-010_CRITIC_ALIGNMENT.md`, `plans/evidence/OBS-010.md`, `plans/EXECUTION_STATE.md`.

## Focused evidence

- `critic.rs::obs010_the_critic_carries_actor_inventory_and_opponent_relationship_facts`: a seat
  holding two relics (one exhausted), a breakthrough, and a leader, plus a combat-counterpart
  opponent, produces `critic-state:relics_held`, `critic-state:relic:codex:ready`,
  `critic-state:relic:dominusorb:exhausted`, `critic-state:relics_exhausted`,
  `critic-state:breakthrough_held`, `critic-state:breakthrough:letnevbt`,
  `critic-state:leaders_unlocked`, `critic-state:leader:hero1:unlocked`, and at least one
  `critic-state:opponent_slot:*:relationship_combat` fact.
- `critic.rs::opponents_contribute_counts_and_never_identities` (pre-existing, unmodified):
  passed against the enlarged inventory, confirming no raw seat id appears as a name segment
  anywhere in the full critic output, including the new opponent-slot and relic/leader-identity
  facts.
- `critic.rs::the_inventory_excludes_everything_section_four_one_forbids` (pre-existing,
  unmodified): passed against the enlarged inventory, confirming none of the new names contain
  "prompt"/"option"/"kind"/"payload"/"target"/"legal"/"scoreable"/"choice".

## Tests and checks

| command | result |
|---|---|
| `cargo test -p ti4-policy --lib obs010` | 1 passed |
| `cargo test -p ti4-policy --lib critic::` | 7 passed (6 pre-existing + 1 new) |
| `cargo test -p ti4-engine` | 1,208 lib + 4 integration + 5 doc passed (unchanged -- no engine
  changes in this package) |
| `cargo test -p ti4-policy --lib` | 232 passed (231 + 1 new, incl. the 102-game campaign,
  286.56s, no regression) |
| `cargo test -p ti4-training` | 133 passed |
| `cargo clippy -p ti4-engine -p ti4-policy --all-targets -- -D warnings` | clean |
| `rustfmt --edition 2024 --check` (scoped files) + `git diff --check` | clean |

## Independent Tier-C review

OUTSTANDING (deferred per standing repo convention; reviewed elsewhere per user confirmation this
session). Two judgment calls worth checking: (1) adding the opponent-slot facts *alongside* the
existing anonymized aggregate rather than replacing it; (2) leaving `OBS-006`'s candidate-centred
board facts out of the critic as a deliberate exclusion (they are inherently target-specific,
which the critic's founding constraint forbids) rather than a remaining gap.
