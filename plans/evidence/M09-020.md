# M09-020 evidence — Durable baselines and sealed data roles

Package: `plans/M09-020_DURABLE_BASELINES_SEALED_ROLES.md`. Branch
`wp/m09-020-durable-baselines-sealed-roles` from base commit `1a06ca9`. Normative source: MLP plan
revision 5 §10. Review tier C (frontier model) — pending independent review at the time of writing.

All outputs below are pasted verbatim from this machine on the current tree.

## Deliverable 1 — sealed final pool (reproducible; recipe committed, file not)

### Generation and corpus-has-not-moved proof

The two pre-existing pools were regenerated with the exact §10 commands into scratch paths
(`out/tmp-regen-*.json`, removed afterwards) and hashed against the committed files:

```text
$ cargo run --release -p ti4-training --example generate_pool -- --seed 777 --boards 1000 --min 8 --max 12 --out out/tmp-regen-holdout.json
wrote out/tmp-regen-holdout.json: 1000 boards, 6 home slots, validated
$ cargo run --release -p ti4-training --example generate_pool -- --seed 1 --boards 4000 --min 8 --max 12 --out out/tmp-regen-train.json
wrote out/tmp-regen-train.json: 4000 boards, 6 home slots, validated
$ sha256sum out/pools/full_np8_12_holdout.json out/tmp-regen-holdout.json out/pools/full_np8_12_train.json out/tmp-regen-train.json
aba33c81aa04cefb15857b8ed1d40173f6f3de5e9b6e9633a6855c1d5a4c27e5 *out/pools/full_np8_12_holdout.json
aba33c81aa04cefb15857b8ed1d40173f6f3de5e9b6e9633a6855c1d5a4c27e5 *out/tmp-regen-holdout.json
106153d4384435b19bd27d7210140b4b46da84c72d7e5ce704ffc52083f2c6df *out/pools/full_np8_12_train.json
106153d4384435b19bd27d7210140b4b46da84c72d7e5ce704ffc52083f2c6df *out/tmp-regen-train.json
```

Both pools reproduce bit-for-bit on the current tree — the corpus has not moved. The final pool
was generated earlier in this package with the exact §10 command (`--seed 20260822 --boards 1000
--min 8 --max 12`) and hashes to:

```text
$ sha256sum out/pools/full_np8_12_final.json
693253ecbcb33ac61c416110836286242be39271ecf49381a99c90acca653245 *out/pools/full_np8_12_final.json
```

This checksum is assigned to the final pool in `plans/evidence/MLP-ARTIFACTS.md` and in the static
manifest of `crates/ti4-sim/src/artifacts.rs`.

### Zero canonical board-hash overlap (re-verified at evidence time)

Pool files store boards as `arrangements` (ordered planet-id lists); the check compares full
arrangement tuples:

```text
$ python -c "...(set of tuple(a) for a in d['arrangements']) per pool; pairwise intersections)..."
train intersect holdout: 0
train intersect final:   0
holdout intersect final: 0
```

All three pools share `schema=ti4-map-pool-v1`, 37 coords, 6 slots (train 4000 / validation 1000 /
final 1000 arrangements). No policy was run on the final pool by this package or any test.

## Deliverable 2 — durable fixtures (not reproducible; archived)

### Sealing tool run (committed command: `cargo run --release -p ti4-sim --example seal_baselines`)

The tool verifies each raw input's sha256 against the durable manifest before writing, compresses
with zstd crate 0.13.3 level 19 single-threaded `encode_all`, double-compresses to detect
nondeterminism, enforces the combined ≤ 50 MiB cap, and refuses to overwrite an existing sealed
fixture whose bytes differ from regenerated bytes:

```text
$ cargo run --release -p ti4-sim --example seal_baselines
sealed fixtures/mlp-baselines/final10000.zst (33886908 bytes -> 4524217 bytes)
sealed fixtures/mlp-baselines/frozen5000.zst (6261762 bytes -> 580437 bytes)
combined compressed: 5104654 bytes (cap 52428800)
```

The tool was run a second time after the code refactor; it completed successfully, which means the
regenerated bytes were byte-identical to the already-sealed fixtures (the tool refuses on any
mismatch). Combined compressed size 5,104,654 bytes ≤ 52,428,800 (50 MiB) — cap satisfied.

### Committed fixture hashes and manifest match

```text
$ sha256sum fixtures/mlp-baselines/final10000.zst fixtures/mlp-baselines/frozen5000.zst
c6bc823a23d6f8c7636e89a817fc3536c7a63628cf652741521d92e5f1d4e543 *fixtures/mlp-baselines/final10000.zst
6f9b90152e619b608b594b62fcfd3e59831b721570822efb75cd5bc6c5cca491 *fixtures/mlp-baselines/frozen5000.zst
```

Both match `compressed_sha256` in `fixtures/mlp-baselines/manifest.json` (schema
`ti4-mlp-baselines-v1`; raw shas `be792a2a…ce5b55b` and `0d0fa9e5…ca4a`, sizes 33,886,908 /
6,261,762; tool "zstd crate 0.13.3 (single-threaded encode_all)", level 19, threads 1; extraction
commands recorded). The committed fixture-integrity test re-decompresses both fixtures and verifies
the raw bytes against the durable manifest:

```text
$ cargo test -p ti4-sim artifacts
test artifacts::tests::checkpoints_are_known_identities_but_not_pools ... ok
test artifacts::tests::final_role_is_refused_by_train_and_validation_checks ... ok
test artifacts::tests::manifest_roles_are_as_recorded ... ok
test artifacts::tests::role_violation_error_names_roles ... ok
test artifacts::tests::unknown_bytes_fail_closed ... ok
test artifacts::tests::sealed_fixtures_extract_to_the_recorded_checkpoints ... ok
test result: ok. 6 passed; 0 failed; 0 ignored; 0 measured; 37 filtered out; finished in 0.64s
```

## Deliverable 3 — manifest and fail-closed role enforcement

The five-artifact durable manifest is `plans/evidence/MLP-ARTIFACTS.md` (roles: train / validation /
final / baseline ×2; the seed-777 holdout's logical role is recorded as **validation** despite its
filename).

### Module and wiring

- `crates/ti4-sim/src/artifacts.rs`: static durable manifest (full sha256 → role),
  `verify_pool_role(path, allowed)` hashing the exact bytes read, `is_known_checkpoint(sha256)`,
  `pool_role()`. Errors: `UnknownArtifact` / `RoleViolation` — fail-closed tests, not operator
  conventions (§10).
- Wired at both live corpus entry points:
  - `ti4_sim::baseline::run_panel`: after checksum verification, before champions load or any game.
    Allowed roles: Train, Validation.
  - `stage2_training.rs --map-pool`: before `MapPool::load`. Allowed roles: Train, Validation.

### Hermetic fail-closed tests (pasted above): final refused by train/validation checks; known
train/validation checksums pass (`manifest_roles_are_as_recorded`); unknown bytes fail closed;
checkpoints are known identities but not pools.

### End-to-end proofs on real files

Negative — the sealed final pool is rejected by the live training command before any rollout:

```text
$ cargo run --release -p ti4-training --example stage2_training -- --map-pool out/pools/full_np8_12_final.json
Error: "out/pools/full_np8_12_final.json: artifact role Final is not allowed here (allowed roles: [Train, Validation])"
error: process didn't exit successfully: `target\release\examples\stage2_training.exe --map-pool out/pools/full_np8_12_final.json` (exit code: 1)
```

Positive — the validation-role pool passes the gate and the command proceeds to normal operation
(output truncated; training started, confirming the gate did not over-restrict):

```text
$ cargo run --release -p ti4-training --example stage2_training -- --map-pool out/pools/full_np8_12_holdout.json
l1z1x cand_vp 1.995 acc_vp 1.995 own_vp -0.0000 (SE 0.0796) own_clr +0.0313 (SE 0.0482, n=32) rejected: VP merit failed [own VP margin: gain -0.0000 is not above 0.0500; own sigma evidence: paired gain -0.0000 does not exceed 2.0 x SE 0.0796 over 32 seeds]; clearance merit failed [clearance sigma evidence: paired gain 0.0313 does not exceed 2.0 x SE 0.0482 over 32 seeds]
promotion: sol (per_faction)
```

Positive — the M09-019a panel still runs on the validation pool with role gating active, producing
identical numbers to the pre-gating baseline (mean VP p1 2.700 / p2 2.467 / p3 2.167 / p4 2.600 /
p5 2.600 / p6 2.533):

```text
$ cargo run --release -p ti4-sim --example m09_019a_panel
  seat p5: mean VP 2.600
  seat p6: mean VP 2.533
pool sha256 aba33c81aa04cefb15857b8ed1d40173f6f3de5e9b6e9633a6855c1d5a4c27e5 | checkpoint sha256 be792a2a207ced25d589162d875bae4fb1f320c8e5637045486db6a24ce5b55b
```

### Baseline test suite after the role gate landed

The hermetic synthetic pool in `panel_rejects_empty_and_failed_runs` is now (correctly) rejected by
the fail-closed role gate before reaching the game-failure path; that test was re-pointed to assert
the unmanifested-pool rejection, and per-game failure collection was factored into a unit-tested
helper:

```text
$ cargo test -p ti4-sim baseline::
test baseline::tests::failed_games_preserves_seed_and_reason ... ok
test baseline::tests::checksum_verification_fails_closed_on_tampered_bytes ... ok
test baseline::tests::champions_load_validates_and_records_checksum ... ok
test baseline::tests::panel_rejects_empty_and_failed_runs ... ok
test baseline::tests::learned_play_on_a_pooled_board_is_deterministic ... ok
test result: ok. 5 passed; 0 failed; 0 ignored; 0 measured; 38 filtered out; finished in 0.09s
```

## Scope extension S1 — `.gitignore` negation block (declared here, before the edit)

The spec's writable paths include `fixtures/mlp-baselines/` as committed output, but line 46 of
`.gitignore` (`fixtures/*`) ignored everything under `fixtures/`, retaining only
`legacy_entropy/bounded-v1/**`. A three-line negation block following that exact convention was
added:

```text
!fixtures/mlp-baselines/
fixtures/mlp-baselines/*
!fixtures/mlp-baselines/**
```

Verified with `git check-ignore -v`: the last matching pattern for both `.zst` files and
`manifest.json` is the negation, and `git status --short` shows `?? fixtures/mlp-baselines/`. This
is the minimal change required to satisfy the spec's own acceptance criterion ("fixtures committed
with manifest") using the repository's established convention; no other ignore behavior changed.

## Gate results

```text
$ cargo test -p ti4-sim
test result: ok. 43 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 12.65s

$ cargo test --workspace   (summary over all crates)
WORKSPACE TOTAL passed: 1347 | failed: 0

$ cargo clippy -p ti4-sim --all-targets   (only pre-existing ti4-engine warnings remain)
warning: unused attribute
warning: this function has too many lines (103/100)

$ cargo fmt -p ti4-sim --check
fmt clean
```

Pre-existing rustfmt drift in `crates/ti4-training` (~30 files, present before this package; the
crate was never fmt-clean) is out of scope per protocol — only the lines added by this package in
`stage2_training.rs` were made fmt-conformant (the remaining diffs at lines 917/1046/1127 are
pre-existing and untouched).

## Observations forwarded to the ledger

- O-M09-020-1: ~30 diagnostic example binaries load pools without role wiring (spec non-goal; they
  produce no baselines or checkpoints — recorded with rationale in the open-review-items ledger).
- O-M09-020-2: `is_known_checkpoint` is provided and tested but has no call site yet; its consumer
  arrives with M10-038 (teacher-checksum rejection).
- O-M09-020-3: pre-existing rustfmt drift in ti4-training examples (see Gate results) — candidate
  for a separate formatting-only package if ever desired.
