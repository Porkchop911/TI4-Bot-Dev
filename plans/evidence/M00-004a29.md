# M00-004a29 — engine/ml/tactical_macro.py interface inventory

## Package details
- **ID:** M00-004a29
- **Title:** Inventory tactical_macro.py public construction APIs
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004a (Interface inventory — public construction APIs)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Catalogue every public constructor, factory, and class in `engine/ml/tactical_macro.py`.

## Oracle source
- **Path:** `D:\Projects\ti4-engine\engine\ml\tactical_macro.py`
- **Lines:** ~370 (including blank lines and comments)
- **Oracle commit:** `37061c511a4780d4c0719e0342533a498cd4b457`

## Allowed Rust paths
- `plans/evidence/M00-004a29.md` (evidence file)

## Permission class
- **P0:** Read oracle file
- **P1:** Write evidence file

## Public API inventory

### Constants
| Name | Type | Value | Line |
|---|---|---|---|
| `TACTICAL_MACRO_SCHEMA_VERSION` | `int` | `2` | ~24 |
| `_PLAN_KINDS` | `frozenset[str]` | `{"move", "load", "commit", "order", "custodians"}` | ~27 |

### Dataclass (frozen)
| Name | Fields | Line |
|---|---|---|
| `TacticalMacroStep` | `prompt: str`, `chosen: str`, `offered: tuple[str, ...]`, `kind: str`, `payload_json: str` | ~32 |
| `TacticalMacroStep.from_choice` | `(cls, choice: Choice, option: Option) -> TacticalMacroStep` | classmethod | ~37 |
| `TacticalMacro` | `schema_version: int`, `actor: str`, `activation_option_id: str`, `steps: tuple[TacticalMacroStep, ...]`, `required_step_count: int`, `intent: TacticalDecisionContext | None`, `summary: tuple[tuple[str, float], ...]`, `proposal_end_state_fingerprint: str` | ~45 |
| `TacticalMacro.as_dict` | `() -> dict` | method | ~54 |
| `TacticalMacroRolloutResult` | `activation_option_id: str`, `actor: str`, `horizon_round: int`, `final_round: int`, `final_vp: int`, `finished: bool`, `tactical_end_state_fingerprint: str`, `final_state_fingerprint: str`, `replayed_prefix_length: int`, `consumed_step_count: int`, `seed_bundle: RolloutSeedBundle | None` | ~58 |

### Private functions
| Name | Signature | Returns | Line |
|---|---|---|---|
| `_tactical_in_progress` | `(state: GameState, actor: str) -> bool` | bool | ~69 |
| `_forced` | `(continuation: Decider, choice: Choice, option_id: str) -> Option` | Option | ~75 |
| `_is_plan_choice` | `(choice: Choice) -> bool` | bool | ~80 |
| `_semantic_match` | `(choice: Choice, expected: TacticalMacroStep) -> Option | None` | Option or None | ~83 |
| `_unit_counts` | `(state: GameState, player: str) -> dict[str, int]` | unit count dict | ~132 |
| `_system_counts` | `(state: GameState, system_id: str, player: str) -> tuple[int, int]` | (ships, ground) | ~142 |
| `_macro_summary` | `(before: GameState, after: GameState, actor: str, destination: str, steps: tuple[...]) -> tuple[tuple[str, float], ...]` | summary tuple | ~150 |

### Private classes
| Name | Key attributes/methods | Line |
|---|---|---|
| `_RecordingDecider` | `__init__(actor, continuation, state_provider)`, `choose(choice) -> Option`, `choose_forced(choice, option_id) -> Option` | ~102 |
| `_ReplayPrefixThenMacroDecider` | `__init__(prefix, expected_target_fingerprint, macro, continuations, state_provider, before_target)`, `choose(choice) -> Option` | ~122 |

### Public functions
| Name | Signature | Returns | Line |
|---|---|---|---|
| `propose_replayed_tactical_macro` | `(initial_game: Game, prefix: tuple[DecisionRecord, ...], target_state: GameState, choice: Choice, activation_option_id: str, *, seed_bundle: RolloutSeedBundle, sanitize_unseen: bool = False, max_steps: int = 10_000) -> TacticalMacro` | TacticalMacro | ~182 |
| `rollout_replayed_tactical_macro` | `(initial_game: Game, prefix: tuple[DecisionRecord, ...], target_state: GameState, choice: Choice, macro: TacticalMacro, *, horizon_round: int = 4, max_steps: int = 10_000, seed_bundle: RolloutSeedBundle | None = None) -> TacticalMacroRolloutResult` | TacticalMacroRolloutResult | ~237 |

## Dependencies between tactical_macro.py APIs
- `propose_replayed_tactical_macro` depends on: `fork_game`, `ReplayPrefixThenForceDecider`, `state_fingerprint`, `decision_fingerprint`, `ExpectedChoiceMismatch`, `_RecordingDecider`, `TacticalMacroStep.from_choice`, `_tactical_in_progress`, `TacticalMacro`, `_macro_summary`, `sanitize_unseen_state`, `_prepare_future_rng`
- `rollout_replayed_macro` depends on: `fork_game`, `state_fingerprint`, `_ReplayPrefixThenMacroDecider`, `TacticalMacroRolloutResult`, `_tactical_in_progress`, `_prepare_future_rng`
- `_semantic_match` depends on: `_canonical` (from rollout.py), `Choice.option`
- `_macro_summary` depends on: `_unit_counts`, `_system_counts`
- `_RecordingDecider` depends on: `TacticalMacroStep.from_choice`, `_tactical_in_progress`, `_forced`
- `_ReplayPrefixThenMacroDecider` depends on: `decision_fingerprint`, `ExpectedChoiceMismatch`, `_forced`, `_semantic_match`, `_is_plan_choice`

## Compatibility class
- **TacticalMacroStep** schema: `exact` — schema_version=2; Rust must emit identical JSON.
- **TacticalMacro** schema: `exact` — schema_version=2; Rust must emit identical JSON.
- **TacticalMacroRolloutResult** schema: `exact` — Rust must emit identical JSON for parity.
- **propose_replayed_tactical_macro** output: `exact` — deterministic from same inputs; Rust must reproduce.
- **rollout_replayed_tactical_macro** output: `exact` — deterministic from same inputs; Rust must reproduce.
- **_semantic_match**: `semantic` — resolution strategy; Rust may differ in edge cases.

## Non-goals
- No implementation of tactical macro logic; documentation-only inventory.
- No analysis of tactical rollout training pipeline.

## Evidence
- File: `D:\Projects\ti4-engine\engine\ml\tactical_macro.py` (read-only)
- Public symbols: 2 constants, 3 dataclasses (2 with classmethods, 1 with method), 2 public functions
- Private symbols: 7 private functions, 2 private classes
- Cross-module dependencies: rollout.py (fork_game, ReplayPrefixThenForceDecider, state_fingerprint, decision_fingerprint, ExpectedChoiceMismatch, _canonical, _prepare_future_rng), context.py (TacticalDecisionContext), counterfactual.py (sanitize_unseen_state), engine.choice (Choice, Decider, DecisionRecord, Option), engine.game (Game), engine.state (GameState)

## Completion
- [x] All public constructors, factories, and classes catalogued
- [x] All init signatures documented
- [x] All dependencies listed
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
