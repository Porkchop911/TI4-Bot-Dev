# Evidence — OBS-008d2, strategy/technology/scoring consequence surface

Second slice of `OBS-008d`, closing `OBS-008d1`'s "no preview" non-goal for three of its eight
subtypes. See `plans/OBS-008D2_STRATEGY_CONSEQUENCE_SURFACE.md` for design; this records checks.

## Changed paths

- `crates/ti4-engine/src/strategy_cards.rs` — `gain_tokens`'s three pool options and
  `research_option` (shared by `offer_research`/`paid_research`) each attach an exact preview.
- `crates/ti4-engine/src/objectives.rs` — `ScoringWindow::pending_choice` attaches an exact
  victory-point preview to every scoring/decline option.
- `crates/ti4-policy/src/features.rs` — `strategy_decision_features` reads previews, mapping five
  quantities to `strategy:*` under the existing family.
- `plans/OBS-008D2_STRATEGY_CONSEQUENCE_SURFACE.md`, `plans/evidence/OBS-008D2.md`,
  `plans/EXECUTION_STATE.md`.

## Focused evidence

- `strategy_cards.rs::obs008d2_token_gain_and_research_preview_their_exact_consequence`: with the
  seat at 3/2/1 tactic/fleet/strategic tokens, each pool option previews its own count rising to
  4/3/2 respectively; with one technology already owned, a research option previews 1 -> 2.
- `objectives.rs::obs008d2_scoring_previews_the_exact_victory_point_gain`: with 2 victory points,
  scoring an offered secret previews 2 -> 3; declining previews 2 -> 2.
- `features.rs::obs008d2_strategy_previews_reach_the_policy`: exact
  `strategy:tactic-tokens-{before,after,change}` (3/4/1) and `strategy:technologies-after` (2)
  reach the policy and survive `mlp_option_features`/`projection::admits`.

## Tests and checks

| command | result |
|---|---|
| `cargo test -p ti4-engine --lib obs008d2` | 2 passed |
| `cargo test -p ti4-policy --lib obs008d2` | 1 passed |
| `cargo test -p ti4-engine --lib strategy_cards::` | 27 passed (26 pre-existing + 1 new) |
| `cargo test -p ti4-engine` | 1,198 lib + 4 integration + 5 doc passed |
| `cargo test -p ti4-policy --lib` | 224 passed (incl. the 102-game campaign, 339.35 s, no
  regression) |
| `cargo test -p ti4-training` | 133 passed |
| `cargo clippy -p ti4-engine -p ti4-policy --all-targets -- -D warnings` | clean |
| `rustfmt --check` (scoped files) + `git diff --check` | clean (a pre-existing, unrelated
  formatting drift in `strategy_cards.rs::redistribute_tokens` was left untouched, matching how
  prior packages treated the same hunk) |

## Independent Tier-C review

OUTSTANDING. Closes two of `OBS-008d`'s open items from `OBS-008d1`'s evidence; four subtypes
(`place_structure`, `ready_planet`, `politics_choose_speaker`, `politics_place_agenda`) and the
rest of the row (turn/pass mechanics, further strategy-card subtypes) remain open.
