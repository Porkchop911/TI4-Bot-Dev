# M03-009 — Ability registration

## Package details
- **ID:** M03-009
- **Title:** Ability registration
- **Milestone:** M03 — Choice, timing, and replay
- **Package:** M03-009
- **Dependencies:** M03-008 ✅

## Permission class
- **P1:** Write evidence file

## Ability registration (from `engine/timing.py`)

### Ability (frozen dataclass)
| Field | Python type | Rust type | Notes |
|---|---|---|---|
| `id` | `str` | `String` | Unique ability identifier |
| `owner` | `str` | `PlayerId` | Ability owner |
| `event_type` | `str` | `String` | Triggering event type |
| `relation` | `Relation` | `Relation` | WHEN or AFTER |
| `effect` | `Callable[[Event, Resolver], None]` | `Box<dyn Fn(Event, &mut Resolver)>` | Ability effect |
| `condition` | `Callable[[Event, Resolver], bool] \| None` | `Option<Box<dyn Fn(Event, &Resolver) -> bool>>` | Eligibility gate |
| `frequency` | `Frequency` | `Frequency` | ONCE_PER_TRIGGER, ONCE_PER_TURN, ONCE_PER_ROUND, UNLIMITED |
| `optional` | `bool` | `bool` | Can be declined |
| `repeatable_in_window` | `bool` | `bool` | Multiple uses per window |
| `option_payload` | `Callable[[Event, Resolver], dict] \| None` | `Option<Box<dyn Fn(Event, &Resolver) -> HashMap<String, Value>>>` | Context for deciders |

### Resolver registration methods
| Method | Python signature | Rust signature | Purpose |
|---|---|---|---|
| `register` | `(self, *abilities)` | `fn(&mut self, abilities: &[Ability])` | Register abilities |
| `forbid` | `(self, label, predicate)` | `fn(&mut self, label, predicate)` | Register "cannot" effect |
| `is_forbidden` | `(self, event) -> str \| None` | `fn(&self, event) -> Option<String>` | Check "cannot" |

### Deterministic registration
- Registration order preserved within event bucket
- Resolution order = registration order (within frequency constraints)
- No hash-map iteration for resolution order

### Frequency metadata
| Frequency | Scope key | Lifecycle |
|---|---|---|
| `ONCE_PER_TRIGGER` | `(ability_id, ("trigger", event_uid))` | Cleared when event resolves |
| `ONCE_PER_TURN` | `(ability_id, ("turn", turn_number))` | Cleared at round start |
| `ONCE_PER_ROUND` | `(ability_id, ("round", round_number))` | Cleared at round start |
| `UNLIMITED` | `None` | Never cleared |

## M03-009 acceptance
- [x] Ability documented (10 fields)
- [x] Registration methods documented (3 methods)
- [x] Deterministic registration documented
- [x] Frequency metadata documented (4 frequencies)

## M03-009 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
