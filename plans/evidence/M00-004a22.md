# M00-004a22 — ML Rollout APIs Construction API Inventory

## Scope
`engine/ml/rollout.py` (424 physical lines). Deterministic identifiers and force-once primitives for rollout labelling.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/rollout.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\rollout.py
  → 424
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\rollout.py
  → 16 matching lines (3 private functions + 10 public functions/classes/constants/dataclasses + 1 class)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\rollout.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `ExpectedChoiceMismatch` (exception class)
| Field | Value |
|---|---|
| Symbol | `ExpectedChoiceMismatch` |
| Kind | Class (inherits `IllegalChoice`) |
| Source line | 17 |
| Construction role | Exception raised when a rollout fork did not reach the exact choice a rollout task expected. No additional fields; inherits from `IllegalChoice`. Constructed via `raise ExpectedChoiceMismatch(message)`. |
| Rust relevance | Rust error type; Rust equivalent must carry error message and propagate through rollout pipeline. |

### 2. `choice_fingerprint()` (deterministic fingerprint function)
| Field | Value |
|---|---|
| Symbol | `choice_fingerprint` |
| Kind | Module-level function |
| Signature | `choice_fingerprint(choice: Choice) -> str` |
| Source line | 41 |
| Construction role | Produces SHA-256 fingerprint of a Choice by canonicalizing player, prompt, and options (id, kind, label, payload via _canonical). Returns hex digest. Used in `decision_fingerprint`, `capture.py`, and `resumable_choice`. |
| Rust relevance | Rust function `fn choice_fingerprint(choice: &Choice) -> String`. Deterministic SHA-256 of canonicalized choice fields. Critical for integrity verification. |

### 3. `state_fingerprint()` (state hash function)
| Field | Value |
|---|---|
| Symbol | `state_fingerprint` |
| Kind | Module-level function |
| Signature | `state_fingerprint(state: GameState) -> str` |
| Source line | 59 |
| Construction role | Hashes every field in a GameState (including fields excluded from `__eq__`) via _canonical + JSON + SHA-256. Validates state is GameState instance. Used in `fork_game`, `decision_fingerprint`, `rollout_resumable_choice`, `rollout_replayed_choice` to verify source immutability. |
| Rust relevance | Rust function `fn state_fingerprint(state: &GameState) -> String`. Deterministic full-state hash. Critical for verifying fork immutability. |

### 4. `decision_fingerprint()` (decision binding function)
| Field | Value |
|---|---|
| Symbol | `decision_fingerprint` |
| Kind | Module-level function |
| Signature | `decision_fingerprint(state: GameState, choice: Choice) -> str` |
| Source line | 74 |
| Construction role | Binds a legal choice to the exact full state that produced it. Combines state_fingerprint(state) and choice_fingerprint(choice) in sorted JSON, then SHA-256. Returns hex digest. Used in `ForceOnceDecider`, `ReplayPrefixThenForceDecider`, `rollout_resumable_choice`. |
| Rust relevance | Rust function `fn decision_fingerprint(state: &GameState, choice: &Choice) -> String`. Deterministic binding of state+choice. Critical for force-once verification. |

### 5. `fork_game()` (game fork function)
| Field | Value |
|---|---|
| Symbol | `fork_game` |
| Kind | Module-level function |
| Signature | `fork_game(game: Game) -> Game` |
| Source line | 87 |
| Construction role | Deep-copies a live game and proves the copy operation did not mutate its source. Records before/after state fingerprints, raises RuntimeError if source mutated, if clone is same object, or if clone state differs from source at creation. Returns independent Game clone. |
| Rust relevance | Rust function `fn fork_game(game: &Game) -> Game`. Deep copy with source immutability verification. Critical for counterfactual rollout safety. |

### 6. `ForceOnceDecider` (force-one-option decider class)
| Field | Value |
|---|---|
| Symbol | `ForceOnceDecider` |
| Kind | Class |
| Source line | 101 |
| Constructor signature | `__init__(self, expected_fingerprint: str, option_id: str, continuation: Decider, *, state_provider: Callable[[], GameState]) -> None` |
| Construction role | Force one option at one exact choice, then delegate forever. Constructor stores: expected_fingerprint, option_id, continuation, state_provider, used (False). `choose(choice: Choice) -> Option`: if used, delegates; else verifies decision_fingerprint matches expected, raises ExpectedChoiceMismatch on mismatch; retrieves option, sets used=True, calls choose_forced if available, returns option. |
| Rust relevance | Rust struct wrapping Decider with force-once logic. Must reproduce identical option forcing for identical fingerprint. Critical for counterfactual rollout. |

### 7. `RNG_DOMAINS` (RNG domain constant)
| Field | Value |
|---|---|
| Symbol | `RNG_DOMAINS` |
| Kind | Module-level constant |
| Value | `("bot_selection", "combat", "cards", "exploration", "misc")` |
| Source line | 143 |
| Construction role | Tuple of RNG domain names used in RolloutSeedBundle derivation. Defines the closed set of rollout RNG streams. |
| Rust relevance | Rust constant `RNG_DOMAINS: [&str; 5]`. Schema constant; must be preserved for seed derivation compatibility. |

### 8. `RolloutSeedBundle` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `RolloutSeedBundle` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `RolloutSeedBundle(master_seed: int, replicate: int, bot_selection: int, combat: int, cards: int, exploration: int, misc: int)` |
| Source line | 147 |
| Construction role | Immutable bundle of rollout RNG seeds. 7 fields: master_seed, replicate, bot_selection, combat, cards, exploration, misc. `derive(master_seed, replicate) -> RolloutSeedBundle`: classmethod deriving per-domain seeds via SHA-256("ti4-rollout-v1:{master_seed}:{replicate}:{domain}")[:8] big-endian masked to 0x7FFFFFFF. Validates non-negative inputs. `as_dict() -> dict[str, int]` returns asdict(self). |
| Rust relevance | Rust frozen struct with derive classmethod. Deterministic seed derivation critical for reproducibility. |

### 9. `RolloutResult` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `RolloutResult` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `RolloutResult(option_id: str, actor: str, horizon_round: int, final_round: int, final_vp: int, finished: bool, final_state_fingerprint: str, replayed_prefix_length: int = 0, seed_bundle: RolloutSeedBundle | None = None)` |
| Source line | 173 |
| Construction role | Immutable result of a rollout. 9 fields: option_id, actor, horizon_round, final_round, final_vp, finished, final_state_fingerprint, replayed_prefix_length (default 0), seed_bundle (default None). Constructed in `rollout_resumable_choice` and `rollout_replayed_choice`. |
| Rust relevance | Rust frozen struct. Returned by rollout functions; must carry all fields identically for compatibility. |

### 10. `ReplayPrefixThenForceDecider` (replay decider class)
| Field | Value |
|---|---|
| Symbol | `ReplayPrefixThenForceDecider` |
| Kind | Class |
| Source line | 185 |
| Constructor signature | `__init__(self, prefix: tuple[DecisionRecord, ...], expected_target_fingerprint: str, option_id: str, continuations: Mapping[str, Decider], state_provider: Callable[[], GameState], before_target: Callable[[], None] | None = None) -> None` |
| Construction role | Replay a verified global decision prefix, force one row, then continue. Constructor stores: prefix, expected_target_fingerprint, option_id, continuations, state_provider, before_target, cursor (0), used (False). `choose(choice: Choice) -> Option`: if cursor < len(prefix), verifies player/prompt/offered match expected record, increments cursor, calls _forced; else if not used, verifies decision_fingerprint matches target, checks option present, calls before_target, sets used=True, returns forced option; else delegates. `_continuation(player)` retrieves by player. `_forced(continuation, choice, option_id)` calls choose_forced. |
| Rust relevance | Rust struct for prefix replay with force-once. Critical for replay-based rollout. |

### 11. `resumable_choice()` (resumability check function)
| Field | Value |
|---|---|
| Symbol | `resumable_choice` |
| Kind | Module-level function |
| Signature | `resumable_choice(game: Game, expected: Choice) -> bool` |
| Source line | 269 |
| Construction role | Checks whether Game.step can reconstruct a choice from state alone. Compares choice_fingerprint of game.legal_options() against expected. Returns False for choices opened inside ability/reaction calls (non-resumable). |
| Rust relevance | Rust function `fn resumable_choice(game: &Game, expected: &Choice) -> bool`. Compatibility check for rollout labeling. |

### 12. `rollout_resumable_choice()` (resumable rollout function)
| Field | Value |
|---|---|
| Symbol | `rollout_resumable_choice` |
| Kind | Module-level function |
| Signature | `rollout_resumable_choice(game: Game, choice: Choice, option_id: str, *, horizon_round: int = 4, seed_bundle: RolloutSeedBundle | None = None) -> RolloutResult` |
| Source line | 281 |
| Construction role | Force one state-resumable row and continue the frozen policy to a horizon. Validates horizon_round, resumable_choice, then: records source_before fingerprint, forks game, gets continuation, wraps in ForceOnceDecider, seats forced decider, applies seed_bundle RNG if present, calls clone.step(), verifies forced.used, runs remaining rounds, verifies source immutability. Returns RolloutResult. |
| Rust relevance | Rust function `fn rollout_resumable_choice(game: &Game, choice: &Choice, option_id: &str, horizon_round: usize, seed_bundle: Option<&RolloutSeedBundle>) -> RolloutResult`. Core counterfactual rollout for labeled data. |

### 13. `rollout_replayed_choice()` (replayed rollout function)
| Field | Value |
|---|---|
| Symbol | `rollout_replayed_choice` |
| Kind | Module-level function |
| Signature | `rollout_replayed_choice(initial_game: Game, prefix: tuple[DecisionRecord, ...], target_state: GameState, choice: Choice, option_id: str, *, horizon_round: int = 4, max_steps: int = 10_000, seed_bundle: RolloutSeedBundle | None = None) -> RolloutResult` |
| Source line | 334 |
| Construction role | Replay from a frozen initial game, force a possibly nested choice, continue. Validates horizon_round, then: records source_before, forks game, creates ReplayPrefixThenForceDecider with target fingerprint, seats replay for all players, steps until replay.used (with max_steps limit), verifies cursor == len(prefix), seats continuations, runs remaining rounds, verifies source immutability. Returns RolloutResult with replayed_prefix_length. |
| Rust relevance | Rust function `fn rollout_replayed_choice(initial_game: &Game, prefix: &[DecisionRecord], target_state: &GameState, choice: &Choice, option_id: &str, horizon_round: usize, max_steps: usize, seed_bundle: Option<&RolloutSeedBundle>) -> RolloutResult`. Core replay-based rollout for labeled data. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `_canonical` | private function | 21 | Private function (leading underscore), not public API |
| `_prepare_future_rng` | private function | 403 | Private function (leading underscore), not public API |
| `__future__` import | import | 3 | Future annotations import |
| `copy` import | import | 5 | Stdlib import, not engine API |
| `hashlib` import | import | 6 | Stdlib import, not engine API |
| `json` import | import | 7 | Stdlib import, not engine API |
| `asdict` import | import | 8 | Stdlib import, not engine API |
| `dataclass` import | import | 8 | Stdlib import, not engine API |
| `fields` import | import | 8 | Stdlib import, not engine API |
| `is_dataclass` import | import | 8 | Stdlib import, not engine API |
| `Enum` import | import | 9 | Stdlib import, not engine API |
| `Any` import | import | 10 | Stdlib import, not engine API |
| `Callable` import | import | 10 | Stdlib import, not engine API |
| `Mapping` import | import | 10 | Stdlib import, not engine API |
| `Choice` import | import | 12 | External type dependency from `engine.choice`, not constructed here |
| `Decider` import | import | 12 | External type dependency from `engine.choice`, not constructed here |
| `DecisionRecord` import | import | 12 | External type dependency from `engine.choice`, not constructed here |
| `IllegalChoice` import | import | 12 | External type dependency from `engine.choice`, not constructed here |
| `Option` import | import | 12 | External type dependency from `engine.choice`, not constructed here |
| `Game` import | import | 13 | External type dependency from `engine.game`, not constructed here |
| `GameState` import | import | 14 | External type dependency from `engine.state`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 13 (1 exception class + 7 functions + 3 classes + 1 constant + 1 dataclass + 1 dataclass) |
| Excluded top-level definitions | 23 (docstrings + private functions + stdlib imports + type imports) |
| Source lines covered | 1–424 |
| Discovery matching lines | 16 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/rollout.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
