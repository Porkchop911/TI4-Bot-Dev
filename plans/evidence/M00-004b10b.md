# M00-004b10b — tools/evaluate_promoted_tactical_compatibility.py Entry Point Inventory

## Scope
`tools/evaluate_promoted_tactical_compatibility.py` (115 lines). Compatibility panel with every final-tested tactical promotion enabled together.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/evaluate_promoted_tactical_compatibility.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\evaluate_promoted_tactical_compatibility.py
  → 115
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\evaluate_promoted_tactical_compatibility.py
  → 4 matching lines (1 private function + 1 private function + 1 public function + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\evaluate_promoted_tactical_compatibility.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main(argv)` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main(argv: list[str] | None) -> int` |
| Kind | Module-level function |
| Source line | 57 |
| Construction role | Parses args via `argparse.ArgumentParser`, generates baseline+promoted arm tasks over seeds×rotations, runs parallel `_play()` via `ProcessPoolExecutor`, computes per-faction paired VP delta via `_paired()`, aggregates search diagnostics (calls/failures/candidates/overrides/failure_reasons), builds report JSON, writes to `--output`, prints games/elapsed/factions/search. Returns 0. |

### 2. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 115 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--seeds` | int | 20 | Number of seeds |
| `--first-seed` | int | 31_548_000 | Starting seed |
| `--workers` | int | `min(16, os.cpu_count())` | Worker count |
| `--horizon` | int | 4 | Game horizon in rounds |
| `--output` | Path | `ROOT/out/promoted_tactical_compatibility.json` | Output report path |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `ROOT` | module constant | 14 | Module-level path constant (sys.path manipulation) |
| `_play` | private function | 20 | Private (leading underscore) |
| `_paired` | private function | 37 | Private (leading underscore) |
| `__future__` import | import | 3 | Future annotations import |
| `argparse` import | import | 5 | Stdlib import |
| `concurrent.futures` import | import | 6 | Stdlib import |
| `json` import | import | 7 | Stdlib import |
| `math` import | import | 8 | Stdlib import |
| `os` import | import | 9 | Stdlib import |
| `statistics` import | import | 10 | Stdlib import |
| `sys` import | import | 11 | Stdlib import |
| `time` import | import | 12 | Stdlib import |
| `Path` import | import | 13 | Stdlib import |
| `bots` import | import | 16 | External module from `engine`, not constructed here |
| `sim` import | import | 16 | External module from `engine`, not constructed here |
| `install_promoted_tactical_search` import | import | 17 | External function from `engine.ml.promoted`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (1 entry point function) |
| Excluded top-level definitions | 18 (docstrings + module constant + private functions + stdlib imports + type imports) |
| Source lines covered | 1–115 |
| Discovery matching lines | 4 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/evaluate_promoted_tactical_compatibility.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b10.
