# M04-013 — Random-legal bot

## Package details
- **ID:** M04-013
- **Title:** Random-legal bot
- **Milestone:** M04 — Game skeleton
- **Package:** M04-013
- **Dependencies:** M03-006 (Native RNG), M04-012 ✅

## Permission class
- **P1:** Write evidence file

## Random-legal bot (seeded random behavior)

### SeatedRandom decider
```python
class SeededRandom:
    def __init__(self, seed: int):
        self._rng = random.Random(seed)
    
    def choose(self, choice: Choice) -> Option:
        return self._rng.choice(choice.options)
```

### Bot behavior
- Seats all players with `SeededRandom(seed)`
- Never fabricates an action (always picks from offered options)
- Uniform random selection over legal options
- Deterministic for same seed

### Game integration
- `seated_game(assignments, table=Table(...), dice_seed=seed)` creates game
- `game.table.seat(player_id, SeededRandom(seed))` for each player
- `game.step(choice, game.table.ask(choice))` advances game

### Smoke test properties
- Every game terminates (no infinite loops)
- No illegal state reached
- All phases traversed
- Deterministic: same seed → same game

## M04-013 acceptance
- [x] SeatedRandom documented
- [x] Bot behavior documented (3 properties)
- [x] Game integration documented
- [x] Smoke test properties documented (4 properties)

## M04-013 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
