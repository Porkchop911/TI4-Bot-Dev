# M00-004a6 — ML Context Construction API Inventory

## Scope
`engine/ml/context.py` (24 physical lines). This is a single-class module with no imports from other engine modules (only stdlib `dataclasses`).

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/context.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\context.py
  → 24
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+ =' D:\Projects\ti4-engine\engine\ml\context.py
  → 2 matching lines (1 @dataclass decorator + 1 class)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\context.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `TacticalDecisionContext` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `TacticalDecisionContext` |
| Kind | `@dataclass(frozen=True)` |
| Source line | 9 |
| Construction role | Immutable value object carrying actor-visible tactical decision state. All fields are required (no defaults). Constructed directly via `TacticalDecisionContext(destination=..., purpose=..., ground_required=..., ground_present=..., ground_shortfall=..., fleet_required=..., fleet_present=..., fleet_shortfall=...)`. Re-exported from `engine.ml` package (`__init__.py` line 19). |
| Rust relevance | Immutable struct; Rust equivalent must carry all 8 fields identically. Construction requires all 8 args — no builder defaults available. Critical for ML training pipeline context passing. |

### Constructor field inventory
| # | Field | Type | Default | Source line |
|---|---|---|---|---|
| 1 | `destination` | `str` | required | 13 |
| 2 | `purpose` | `str` | required | 14 |
| 3 | `ground_required` | `int` | required | 15 |
| 4 | `ground_present` | `int` | required | 16 |
| 5 | `ground_shortfall` | `int` | required | 17 |
| 6 | `fleet_required` | `float` | required | 18 |
| 7 | `fleet_present` | `float` | required | 19 |
| 8 | `fleet_shortfall` | `float` | required | 20 |

## Exclusions table
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `__future__` import | import | 3 | Future annotations import |
| `dataclasses.dataclass` import | import | 5 | Stdlib import, not engine API |
| Class docstring | docstring | 8–12 | Documentation, not code |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (`TacticalDecisionContext` dataclass) |
| Constructor fields | 8 (all required, no defaults) |
| Excluded definitions | 4 (docstrings + stdlib imports) |
| Source lines covered | 1–24 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/context.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
