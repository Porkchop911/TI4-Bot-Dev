# Evidence — OBS-008g2, reused-quantity content consequence surface

See `plans/OBS-008G2_REUSED_QUANTITY_CONSEQUENCE_SURFACE.md` for design; this records checks.

## Changed paths

- `crates/ti4-engine/src/faction_abilities.rs` — Munitions Reserves' reroll option and every Peace
  Accords annex candidate attach an exact preview.
- `crates/ti4-engine/src/action_cards.rs` — Skilled Retreat's destination options attach an exact
  preview, plus a `resolve_card_capturing_on` test helper (galaxy-aware `resolve_card_capturing`).
- `crates/ti4-policy/src/features.rs` — `content_decision_features` gains three more preview
  quantity mappings (`TradeGoods`, `PlanetsControlled`, `ShipsInSystem`).
- `plans/OBS-008G2_REUSED_QUANTITY_CONSEQUENCE_SURFACE.md`, `plans/evidence/OBS-008G2.md`,
  `plans/EXECUTION_STATE.md`.

## Focused evidence

- `faction_abilities.rs::obs008g2_munitions_reserves_previews_its_exact_trade_good_cost`: with 5
  trade goods, the reroll option previews `5 -> 3`.
- `faction_abilities.rs::obs008g2_peace_accords_previews_the_planet_count_gain`: the offered
  candidate previews the seat's controlled-planet count rising by exactly one from its actual
  pre-annex count.
- `action_cards.rs::obs008g2_skilled_retreat_previews_the_exact_arrival_count`: with a two-cruiser
  fleet at the active system, an occupied destination (one cruiser already there) previews
  `1 -> 3`; an empty destination previews `0 -> 2`.
- `features.rs::obs008g2_reused_quantity_previews_reach_the_policy`: all three map to
  `content:trade-goods-after` (3.0), `content:planets-controlled-change` (1.0), and
  `content:ships-after` (2.0), and each `-change` name survives `projection::admits`.

## Tests and checks

| command | result |
|---|---|
| `cargo test -p ti4-engine --lib obs008g2` | 3 passed |
| `cargo test -p ti4-engine --lib faction_abilities::` | 26 passed (24 pre-existing + 2 new) |
| `cargo test -p ti4-engine --lib action_cards::` | 100 passed (99 pre-existing + 1 new) |
| `cargo test -p ti4-policy --lib obs008g2` | 1 passed |
| `cargo test -p ti4-engine` | 1,202 lib (1,199 + 3 new) + 4 integration + 5 doc passed |
| `cargo test -p ti4-policy --lib` | 228 passed (227 + 1 new, incl. the 102-game campaign,
  325.84s, no regression) |
| `cargo test -p ti4-training` | 133 passed |
| `cargo clippy -p ti4-engine -p ti4-policy --all-targets -- -D warnings` | clean |
| `rustfmt --edition 2024 --check` (scoped files) + `git diff --check` | clean |

## Independent Tier-C review

OUTSTANDING (deferred per standing repo convention; reviewed elsewhere per user confirmation this
session). `orbital_drop_choose_planet`, `crashlanding_choose_ground`/`crashlanding_choose_planet`,
and `silence_choose_system` were checked and found to be genuine identity choices without a clean
per-option quantity delta; `orbital_drop_deploy_mech` was checked and found already covered via the
`production` family (its kind already routes it there). The remaining `content` subtypes stay
without a preview, left for further opportunistic passes.
