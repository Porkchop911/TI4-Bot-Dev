# M06-022 — Counting-family objective progress

| Field | Value |
|---|---|
| Status | accepted; independent Tier-B review complete and all findings resolved |
| Branch/base | `wp/m06-022-counting-objective-progress` / `5d027e8` |
| Permission | P1; scoped engine source, tests, plans, and Cargo artifacts only |
| Historical Python use | none |

## Implementation

- Added public `CountFamily` and `RequirementProgress { family, have, threshold }` contracts.
- Mapped all 24 public and 10 secret counting aliases, retaining trait/unit/per-colour parameters.
- Refactored every affected scoring predicate to derive legality from `RequirementProgress::satisfied`.
- Counts use exact integer reductions. Same-trait, same-colour, fleet, and ground-force families
  report a maximum in one qualifying category/location; distinct-system/planet families deduplicate.
- Outer-rim progress returns unavailable without a galaxy. Unknown aliases return unavailable.
- Queries borrow immutable state and focused tests prove structural equality is preserved.

## Focused evidence

`all_public_counting_aliases_have_typed_progress_and_shared_legality` and
`all_secret_counting_aliases_have_typed_progress_and_shared_legality` table-check all 34 aliases,
families, parameters, non-zero thresholds, and equality between progress completion and registered
legality. `map_dependent_counting_progress_is_unavailable_without_a_map_and_read_only` covers the
unavailable/purity boundary. Existing focused objective tests continue to cover dual traits,
per-colour technologies, maximum-in-one-system fleets, fighter exclusion, distinct mech planets,
attachments, planetless systems, and threshold legality.

## Commands and exact results

```text
cargo test -p ti4-engine all_public_counting_aliases_have_typed_progress_and_shared_legality --quiet
  -> 1 passed
cargo test -p ti4-engine all_secret_counting_aliases_have_typed_progress_and_shared_legality --quiet
  -> 1 passed
cargo test -p ti4-engine --quiet
  -> 822 passed, 0 failed; 5 doctests passed
cargo test --workspace --quiet
  -> exit 0; all workspace tests and doctests passed
cargo clippy -p ti4-engine --all-targets
  -> one package-local nonminimal-bool warning found and fixed; remaining warnings are the
     documented duplicate must_use, Game::apply_tactical length, and strategy-test cast
git diff --check
  -> passed
```

No benchmark or performance claim applies. No Python comparison was made. Claude Opus 5
independently accepted the package after mechanically finding zero family/threshold drift and
reproducing the workspace/822-test gates plus two byte-identical 150-game runs. G1/G2 expanded
M06-023 to all seventeen remaining position secrets with explicit unavailable semantics; G3's stale
registry comment was corrected. No blocking finding remains.
