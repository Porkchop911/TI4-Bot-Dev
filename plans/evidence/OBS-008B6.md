# Evidence — OBS-008b6, ground casualty / coexisting-combat identity / custodians

Batched: three producers, one commit, one verification pass. See
`plans/OBS-008B6_GROUND_COMBAT_AND_CUSTODIANS_SURFACE.md` for design; this records what was
checked, not the design again.

## Changed paths

- `crates/ti4-engine/src/invasion.rs` — `absorb_ground` unit/damaged payload;
  `deterministic::spend`-based custodians preview.
- `crates/ti4-policy/src/features.rs` — `dropped`-token computation extended to
  `start_next_ground_combat`; `bombardment_target_features` renamed `opponent_identity_features`
  and handles both subtypes; `payment_decision_features` admitted by subtype for
  `remove_custodians`, with a `VictoryPoints` delta arm added.
- `plans/OBS-008B6_GROUND_COMBAT_AND_CUSTODIANS_SURFACE.md`, `plans/evidence/OBS-008B6.md`,
  `plans/EXECUTION_STATE.md`.

## Focused evidence

- `invasion.rs::obs008b6_ground_casualty_and_custodians_previews`: every ground-casualty option
  carries `unit`; `fight|b` still carries the raw seat id (engine routing untouched); removing
  custodians previews `VictoryPoints 0 -> 1`; declining previews `0 -> 0`.
- `features.rs::obs008b6_ground_casualty_next_combat_and_custodians_features`: `casualty-unit:
  is-ground` reaches the policy from the ground-casualty payload with no new wiring;
  `combat:target-slot-0` is emitted for the coexisting-combat option and `option:b` never is;
  `pay:subtype:remove_custodians` and exact `pay:victory-points-*` reach the policy (declining's
  genuine-zero change dropped as sparse).

## Tests and checks

| command | result |
|---|---|
| `cargo test -p ti4-engine --lib obs008b6` | 1 passed |
| `cargo test -p ti4-policy --lib obs008b6` | 1 passed |
| `cargo test -p ti4-engine --lib invasion::` | 44 passed (43 pre-existing + 1 new) |
| `cargo test -p ti4-engine` | 1,196 lib + 4 integration + 5 doc passed |
| `cargo test -p ti4-policy --lib` | 221 passed (incl. the 102-game campaign, 335.67 s, no
  regression) |
| `cargo test -p ti4-training` | 133 passed |
| `cargo clippy -p ti4-engine -p ti4-policy --all-targets -- -D warnings` | clean |
| `rustfmt --check` (scoped files) + `git diff --check` | clean |

## Independent Tier-C review

OUTSTANDING. This closes `OBS-008b` (six slices, `aab65bc` through this commit). All six, plus the
open residual (whether other producers share the raw-identity-leak pattern `b5`/`b6` fixed), are
owed review.
