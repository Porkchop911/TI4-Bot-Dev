# M00-004b6d — tools/ridge_learning_curve.py Entry Point Inventory

## Scope
`tools/ridge_learning_curve.py` (135 lines). Learning curve diagnostic for tactical ridge model (data-limited vs representation-limited).

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/ridge_learning_curve.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\ridge_learning_curve.py
  → 135
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\ridge_learning_curve.py
  → 5 matching lines (1 module constant tuple + 1 private function + 1 public function + 1 public function + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\ridge_learning_curve.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main()` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main()` |
| Kind | Module-level function |
| Source line | 72 |
| Construction role | Parses args via `parse_args()`, loads choice sets from dataset JSONs, splits seeds into train/holdout via fixed RNG, iterates FRACTIONS (0.1–1.0) with `--repeats` per fraction, fits `RidgeChoiceScorer` per run, evaluates via `_evaluate()`, prints Spearman/regret/best-arm table, prints final step comparison, writes JSON report to `--out`. Returns 0. |

### 2. `parse_args()` — CLI argument parser
| Field | Value |
|---|---|
| Symbol | `parse_args()` |
| Kind | Module-level function |
| Source line | 55 |
| Construction role | Returns `argparse.ArgumentParser` with `description=__doc__`. Flags: `--dataset` (Path, append, required), `--alpha` (float, default 10.0), `--holdout-fraction` (float, default 0.25), `--repeats` (int, default 5), `--seed` (int, default 52), `--out` (Path, default `ROOT/out/ridge_learning_curve.json`). |

### 3. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 135 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--dataset` | Path (append, required) | **required** | Training dataset JSON files (repeatable) |
| `--alpha` | float | 10.0 | Ridge regularization parameter |
| `--holdout-fraction` | float | 0.25 | Fraction of seeds held out |
| `--repeats` | int | 5 | Repeats per fraction |
| `--seed` | int | 52 | Random seed |
| `--out` | Path | `ROOT/out/ridge_learning_curve.json` | Output report path |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–15 | Documentation, not code |
| `ROOT` | module constant | 23 | Module-level path constant (sys.path manipulation) |
| `FRACTIONS` | module constant | 27 | Tuple of training data fractions (0.1–1.0) |
| `_evaluate` | private function | 30 | Private (leading underscore) |
| `__future__` import | import | 17 | Future annotations import |
| `argparse` import | import | 19 | Stdlib import |
| `json` import | import | 20 | Stdlib import |
| `random` import | import | 21 | Stdlib import |
| `statistics` import | import | 22 | Stdlib import |
| `sys` import | import | 23 | Stdlib import |
| `Path` import | import | 24 | Stdlib import |
| `numpy` import | import | 26 | External dependency |
| `spearmanr` import | import | 27 | External dependency |
| `tactical_macro_choice_sets` import | import | 29 | External function from tools.ml_choice_sets |
| `RidgeChoiceScorer` import | import | 30 | External class from tools.ridge_choice_sets |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (1 entry point function + 1 parser function) |
| Excluded top-level definitions | 15 (docstrings + module constants + private function + stdlib imports + external imports) |
| Source lines covered | 1–135 |
| Discovery matching lines | 5 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/ridge_learning_curve.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b6.
