# M00-004b5 — Policy Analysis Tool Entry Point Inventory

## Scope
Three policy-analysis tools: `tools/characterize_complete_policy.py` (155 lines), `tools/policy_coverage.py` (155 lines), `tools/screen_policy_groups.py` (317 lines).

---

## 1. tools/characterize_complete_policy.py (155 lines)

### Entry points
| Symbol | Kind | Line | Role |
|---|---|---|---|
| `main()` | function | 119 | CLI entry: loads baseline/candidate profiles, runs parallel `_play()` for both, calls `_summarize()`, prints JSON summaries to stdout, writes full output JSON to `--output`. |
| `parse_args()` | inline in main | 120 | Argparse parser with 8 flags (no named function). |
| `__main__` guard | script | 155 | `main()` (no `SystemExit` wrapper). |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--baseline` | Path | **required** | Baseline profile JSON |
| `--candidate` | Path | **required** | Candidate profile JSON |
| `--candidate-key` | str | `"champion_profile"` | Key within candidate JSON |
| `--faction` | str | **required** | Faction to characterize |
| `--workers` | int | 16 | Worker count |
| `--seeds` | int | 12 | Seeds per profile×rotation |
| `--first-seed` | int | 400_000 | Starting seed |
| `--output` | Path | **required** | Output report path |

### Exclusions (top-level)
| Symbol | Kind | Line | Reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `ROOT` | constant | 10 | sys.path constant |
| `_load` | private function | 13 | Profile loader (leading underscore) |
| `_play` | private function | 22 | Per-task game simulation (leading underscore) |
| `_summarize` | private function | 64 | Summary aggregation (leading underscore) |
| `__future__` import | import | 3 | Future annotations |
| `argparse` import | import | 5 | Stdlib |
| `collections` import | import | 6 | Stdlib |
| `concurrent.futures` import | import | 7 | Stdlib |
| `json` import | import | 8 | Stdlib |
| `statistics` import | import | 9 | Stdlib |
| `sys` import | import | 10 | Stdlib |
| `Path` import | import | 11 | Stdlib |
| `bots` import | import | 13 | External module |
| `sim` import | import | 13 | External module |
| `strategy` import | import | 13 | External module |
| `technology` import | import | 13 | External module |
| `units` import | import | 13 | External module |
| `apply_profile` import | import | 14 | External function |
| `profile_id` import | import | 14 | External function |
| `validate_profile` import | import | 14 | External function |

---

## 2. tools/policy_coverage.py (155 lines)

### Entry points
| Symbol | Kind | Line | Role |
|---|---|---|---|
| `main(argv)` | function | 89 | CLI entry: loads champions JSON if `--champions`, generates game tasks, runs parallel `_play()` with spy on `_apply_policy_score`, aggregates pairs/kinds/residuals/onboard, builds schema alias map via `build_leaves()`, prints coverage report + blind weights, writes JSON to `--out`. |
| `parse_args()` | inline in main | 91 | Argparse parser with 8 flags, uses `RawDescriptionHelpFormatter`. |
| `__main__` guard | script | 155 | `raise SystemExit(main())` |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--champions` | Path | None | Champions table JSON |
| `--seeds` | int | 30 | Seeds per board×rotation |
| `--rounds` | int | 4 | Game rounds |
| `--boards` | str (nargs+) | ["six"] | Board types (choices: six, three) |
| `--first-seed` | int | 97_000_000 | Starting seed |
| `--workers` | int | 30 | Worker count |
| `--fixed-maps` | flag | False | Disable `--vary-maps` |
| `--out` | Path | None | Output report path |

### Exclusions (top-level)
| Symbol | Kind | Line | Reason |
|---|---|---|---|
| Module docstring | docstring | 1–15 | Documentation |
| `ROOT` | constant | 21 | sys.path constant |
| `_CHAMPIONS` | constant | 24 | Global state for ProcessPoolExecutor initializer |
| `_start` | private function | 26 | Private (leading underscore); initializer for parallel workers |
| `_play` | private function | 30 | Private (leading underscore); per-task game with spy |
| `__future__` import | import | 17 | Future annotations |
| `argparse` import | import | 19 | Stdlib |
| `collections` import | import | 20 | Stdlib |
| `json` import | import | 21 | Stdlib |
| `sys` import | import | 22 | Stdlib |
| `Path` import | import | 23 | Stdlib |
| `Iterable` import | import | 24 | Stdlib |
| `bots` import | import | 26 | External module |
| `sim` import | import | 26 | External module |
| `apply_profile` import | import | 27 | External function |

---

## 3. tools/screen_policy_groups.py (317 lines)

### Entry points
| Symbol | Kind | Line | Role |
|---|---|---|---|
| `main()` | function | 226 | CLI entry: loads initial champions, builds Hadamard design, verifies orthogonality, generates game tasks, runs parallel `_play()`, calls `estimate()` + `clipping_rate()`, prints per-faction group effects with confidence intervals, writes JSON to `--out`. |
| `parse_args()` | function | 208 | Argparse parser with 8 flags. |
| `__main__` guard | script | 317 | `main()` (no `SystemExit` wrapper). |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--runs` | int | 256 | Design rows (power of two) |
| `--seeds` | int | 6 | Seeds per run×rotation |
| `--first-seed` | int | 21_000_000 | Starting seed |
| `--workers` | int | `min(16, os.cpu_count())` | Worker count |
| `--horizon` | int | 4 | Game horizon |
| `--magnitude` | float | 2.0 | Perturbation magnitude |
| `--seed` | int | 4_242 | Direction randomisation seed |
| `--out` | Path | `ROOT/out/policy_group_screen.json` | Output report path |

### Exclusions (top-level)
| Symbol | Kind | Line | Reason |
|---|---|---|---|
| Module docstring | docstring | 1–23 | Documentation |
| `VARIANTS` | constant | 26 | Independent random directions per group (2) |
| `_ANCHORS` | constant | 64 | Global state for worker initializer |
| `_GROUPS` | constant | 64 | Global state for worker initializer |
| `_SIGNS` | constant | 64 | Global state for worker initializer |
| `_COLUMN` | constant | 64 | Global state for worker initializer |
| `_DESIGN` | constant | 64 | Global state for worker initializer |
| `_MAGNITUDE` | constant | 64 | Global state for worker initializer |
| `_CACHE` | constant | 64 | Global state for worker initializer |
| `hadamard` | public function | 29 | Hadamard matrix construction helper |
| `grouped` | public function | 45 | Group parameters by name |
| `_worker_start` | private function | 71 | Private (leading underscore); worker initializer |
| `_bundle` | private function | 82 | Per-run profile bundle builder (leading underscore) |
| `_play` | private function | 106 | Per-task game runner (leading underscore) |
| `clipping_rate` | public function | 123 | Parameter bound clipping estimator |
| `estimate` | public function | 148 | Least-squares VP effect estimator |
| `__future__` import | import | 25 | Future annotations |
| `argparse` import | import | 27 | Stdlib |
| `concurrent.futures` import | import | 28 | Stdlib |
| `copy` import | import | 29 | Stdlib |
| `json` import | import | 30 | Stdlib |
| `os` import | import | 31 | Stdlib |
| `sys` import | import | 32 | Stdlib |
| `time` import | import | 33 | Stdlib |
| `Path` import | import | 34 | Stdlib |
| `numpy` import | import | 36 | External dependency |
| `FACTIONS` import | import | 38 | External from tools |
| `FactionCase` import | import | 38 | External from tools |
| `_run_case` import | import | 38 | External from tools |
| `initial_champions` import | import | 38 | External from tools |
| `Parameter` import | import | 42 | External from tools |
| `get_value` import | import | 42 | External from tools |
| `parameters` import | import | 42 | External from tools |
| `set_value` import | import | 42 | External from tools |

---

## Summary

| Tool | Lines | Entry points | Flags | Exclusions |
|---|---|---|---|---|
| `characterize_complete_policy.py` | 155 | 2 (+ __main__) | 8 | 21 |
| `policy_coverage.py` | 155 | 2 (+ __main__) | 8 | 15 |
| `screen_policy_groups.py` | 317 | 2 (+ __main__) | 8 | 30 |
| **Total** | **627** | **6** | **24** | **66** |

## Status
- M00-004b5 evidence complete.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b6 (if applicable).
