# M00-004a9 — Tactical Macro Runtime Construction API Inventory

## Scope
`engine/ml/tactical_macro_runtime.py` (49 physical lines). Dependency-free runtime for the frozen tactical-macro teacher.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/tactical_macro_runtime.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\tactical_macro_runtime.py
  → 49
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+ =' D:\Projects\ti4-engine\engine\ml\tactical_macro_runtime.py
  → 3 matching lines (1 constant + 1 @dataclass decorator + 1 class)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\tactical_macro_runtime.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `TACTICAL_MACRO_MODEL_SCHEMA_VERSION` (schema constant)
| Field | Value |
|---|---|
| Symbol | `TACTICAL_MACRO_MODEL_SCHEMA_VERSION` |
| Kind | Module-level constant |
| Signature/Value | `1` |
| Source line | 14 |
| Construction role | Schema version identifier for tactical macro model files. Used in `TacticalMacroModel.load()` to validate loaded JSON payloads. |
| Rust relevance | Must be preserved in Rust model loading; any schema change requires migration path. |

### 2. `TacticalMacroModel` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `TacticalMacroModel` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `TacticalMacroModel(model_id: str, weights: Mapping[str, float], metadata: Mapping[str, object])` |
| Source line | 18 |
| Construction role | Immutable model value object holding a tactical macro ridge regression model. Constructed via: (a) direct instantiation with model_id, weights, metadata; (b) `load()` classmethod which reads a JSON file, validates schema version, validates model_type == "tactical_macro_ridge", validates weights are non-empty dict, validates all weights are finite, verifies model_id matches SHA-256 hash of remaining payload fields. |
| Rust relevance | Core model type; Rust equivalent must carry model_id (str), weights (HashMap<String, f64>), metadata (HashMap<String, serde_value>). Construction via `load()` requires JSON deserialization, schema validation, weight validation, and SHA-256 integrity check. |

### 2a. `TacticalMacroModel.load()` (classmethod factory)
| Field | Value |
|---|---|
| Symbol | `TacticalMacroModel.load` |
| Kind | `@classmethod` factory |
| Signature | `load(cls, path: str | Path) -> TacticalMacroModel` |
| Source line | 23 |
| Construction role | Loads a tactical macro model from a JSON file at `path`. Validates: (1) `model_schema_version` matches `TACTICAL_MACRO_MODEL_SCHEMA_VERSION`; (2) `model_type` == "tactical_macro_ridge"; (3) `weights` is non-empty dict; (4) all weights are finite floats; (5) `model_id` matches SHA-256(`json.dumps(remaining_payload, sort_keys=True, separators=(",", ":"))`)[:12]. Returns `cls(actual_hash, converted_weights, training_metadata)`. Raises `ValueError` on any validation failure. |
| Rust relevance | Rust constructor `TacticalMacroModel::load(path: &Path) -> Result<Self, Error>`. Must implement identical validation pipeline and SHA-256 integrity check. Critical for model loading reproducibility. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `__future__` import | import | 3 | Future annotations import |
| `hashlib` import | import | 5 | Stdlib import, not engine API |
| `json` import | import | 6 | Stdlib import, not engine API |
| `math` import | import | 7 | Stdlib import, not engine API |
| `dataclass` import | import | 8 | Stdlib import, not engine API |
| `Path` import | import | 9 | Stdlib import, not engine API |
| `Mapping` import | import | 10 | Stdlib import, not engine API |
| `tactical_macro_features` import | import | 12 | External function from `engine.ml.tactical_macro_features`, not constructed here |
| `TacticalMacroModel.score()` | method | 44 | Scoring method, not construction |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 3 (1 constant + 1 dataclass + 1 classmethod factory) |
| Excluded top-level definitions | 10 (docstrings + stdlib imports + type imports + scoring method) |
| Source lines covered | 1–49 |
| Discovery matching lines | 3 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/tactical_macro_runtime.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
