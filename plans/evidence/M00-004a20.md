# M00-004a20 — ML Model Feature APIs Construction API Inventory

## Scope
`engine/ml/model_features.py` (349 physical lines). Sparse learned-policy features shared by offline fitting and runtime scoring.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/model_features.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\model_features.py
  → 349
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\model_features.py
  → 13 matching lines (4 private constants + 1 public function + 7 private functions + 1 public function)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\model_features.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `actor()` (actor extraction function)
| Field | Value |
|---|---|
| Symbol | `actor` |
| Kind | Module-level function |
| Signature | `actor(capture: dict) -> dict` |
| Source line | 28 |
| Construction role | Extracts the actor player dict from a capture dict by matching `capture["player"]` against `capture["observation"]["players"]`. Returns the first matching player dict. Used pervasively by all feature functions in this module as the primary actor-visible state accessor. |
| Rust relevance | Rust function `fn actor(capture: &dict) -> &dict`. Simple lookup utility; must return identical player dict for identical capture input. Critical dependency for all feature functions. |

### 2. `option_features()` (option feature combination function)
| Field | Value |
|---|---|
| Symbol | `option_features` |
| Kind | Module-level function |
| Signature | `option_features(capture: dict, option: dict) -> dict[str, float]` |
| Source line | 302 |
| Construction role | Combines option effects and explicit state-option interactions into a sparse feature dict. Computes heuristic_z (z-score of option's incumbent_score vs all options). Extracts raw_components as `raw:` and `raw_present:` features. Adds `ref:` features for typed_references. Adds `effect:` features for numeric_features. Dispatches to option-kind-specific feature extractors: `_strategy_option_features`, `_research_option_features`, `_production_option_features`, `_activation_option_features`, `_action_phase_option_features`. Adds `decision_context` features if present. Adds `_context()` numeric/categorical features with interaction terms (heuristic_z, raw_total, kind, raw). Returns `dict[str, float]` with heuristic_z, family_alpha, raw_total, kind, considered, raw, ref, effect, option-kind-specific, context, and interaction features. |
| Rust relevance | Core feature engineering function; must reproduce identical feature dict for identical capture/option inputs. Depends on `actor`, `_context`, `_available_planet_value`, `_strategy_option_features`, `_research_option_features`, `_production_option_features`, `_activation_option_features`, `_action_phase_option_features`. Critical for ML training and runtime scoring. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `_STRATEGY_NAMES` | private constant | 11 | Private constant (leading underscore), not public API |
| `_PLANETS` | private constant | 15 | Private constant (leading underscore), not public API |
| `_PRODUCTION_ALLOWANCE` | private constant | 19 | Private constant (leading underscore), not public API |
| `_INTEGRATED_ECONOMY_ALLOWANCE` | private constant | 23 | Private constant (leading underscore), not public API |
| `_context` | private function | 36 | Private function (leading underscore), not public API |
| `_available_planet_value` | private function | 69 | Private function (leading underscore), not public API |
| `_strategy_option_features` | private function | 79 | Private function (leading underscore), not public API |
| `_research_option_features` | private function | 142 | Private function (leading underscore), not public API |
| `_production_option_features` | private function | 169 | Private function (leading underscore), not public API |
| `_activation_option_features` | private function | 225 | Private function (leading underscore), not public API |
| `_action_phase_option_features` | private function | 247 | Private function (leading underscore), not public API |
| `__future__` import | import | 3 | Future annotations import |
| `json` import | import | 5 | Stdlib import, not engine API |
| `re` import | import | 6 | Stdlib import, not engine API |
| `statistics` import | import | 7 | Stdlib import, not engine API |
| `content` import | import | 9 | External module dependency from `engine`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (2 module-level functions) |
| Excluded top-level definitions | 17 (docstrings + private constants + private functions + stdlib imports + type import) |
| Source lines covered | 1–349 |
| Discovery matching lines | 13 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/model_features.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
