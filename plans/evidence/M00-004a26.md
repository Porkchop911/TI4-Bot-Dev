# M00-004a26 — engine/ml/capture.py interface inventory

## Package details
- **ID:** M00-004a26
- **Title:** Inventory capture.py public construction APIs
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004a (Interface inventory — public construction APIs)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Catalogue every public constructor, factory, and class in `engine/ml/capture.py`.

## Oracle source
- **Path:** `D:\Projects\ti4-engine\engine\ml\capture.py`
- **Lines:** 348 (including blank lines and comments)
- **Oracle commit:** `37061c511a4780d4c0719e0342533a498cd4b457`

## Allowed Rust paths
- `plans/evidence/M00-004a26.md` (evidence file)

## Permission class
- **P0:** Read oracle file
- **P1:** Write evidence file

## Public API inventory

### Constants
| Name | Type | Value | Line |
|---|---|---|---|
| `CAPTURE_SCHEMA_VERSION` | `int` | `3` | ~28 |

### Dataclasses (frozen)
| Name | Fields | Line |
|---|---|---|
| `TypedReference` | `namespace: str`, `id: str` | ~32 |
| `CapturedOption` | `id: str`, `kind: str`, `label: str`, `payload_json: str`, `payload_sha256: str`, `typed_references: tuple[TypedReference, ...]`, `numeric_features: tuple[tuple[str, float], ...]`, `raw_components: tuple[tuple[str, float], ...]`, `incumbent_components: tuple[tuple[str, float], ...]`, `incumbent_score: float`, `incumbent_considered: bool` | ~37 |
| `CapturedChoice` | `schema_version: int`, `run_id: str`, `decision_index: int`, `player: str`, `observation: BotObservation`, `observation_digest: str`, `choice_fingerprint: str`, `prompt: str`, `chosen_option_id: str`, `decision_context: TacticalDecisionContext | None`, `options: tuple[CapturedOption, ...]` | ~51 |

### CapturedChoice methods
| Name | Signature | Line |
|---|---|---|
| `as_dict` | `() -> dict` | ~64 |
| `canonical_json` | `() -> str` | ~65 |

### Module-level mappings (constants)
| Name | Type | Line |
|---|---|---|
| `_KIND_NAMESPACE` | `Mapping[str, tuple[Namespace, ...]]` | ~69 |
| `_PAYLOAD_NAMESPACE` | `Mapping[str, Namespace]` | ~86 |

### Public functions
| Name | Signature | Returns | Line |
|---|---|---|---|
| `typed_references` | `(option: Option, known: Catalogue | None = None) -> tuple[TypedReference, ...]` | typed reference tuple | ~108 |
| `capture_scored_choice` | `(observation: BotObservation, choice: Choice, decision, chosen: Option, *, run_id: str, decision_index: int, decision_context: TacticalDecisionContext | None = None) -> CapturedChoice` | CapturedChoice | ~182 |
| `build_choice_matrix` | `(observation: BotObservation, choice: Choice, raw_breakdown: Mapping[...], incumbent_breakdown: Mapping[...], incumbent_scores: Mapping[...], considered_options: Iterable[str], *, run_id: str, decision_index: int, chosen_option_id: str, decision_context: TacticalDecisionContext | None = None) -> CapturedChoice` | CapturedChoice | ~203 |
| `write_capture_shard` | `(path: Path, captures: tuple[CapturedChoice, ...] | list[CapturedChoice], *, manifest_sha256: str) -> None` | None | ~268 |

### Private functions (not exported)
| Name | Signature | Line |
|---|---|---|
| `_known_reference` | `(known: Catalogue, namespace: Namespace, value: object) -> TypedReference | None` | ~100 |
| `_typed_references` | `(option: Option, known: Catalogue, observation: BotObservation | None) -> tuple[TypedReference, ...]` | ~112 |
| `_payload_json` | `(option: Option) -> str` | ~159 |

### Class
| Name | Key attributes/methods | Line |
|---|---|---|
| `ChoiceCaptureDecider` | `__init__(view: GameView, continuation: Decider, sink: Callable[[CapturedChoice], None], *, run_id: str)`, `choose(choice: Choice) -> Option` | ~164 |

## Dependencies between capture.py APIs
- `typed_references` depends on: `Catalogue.contains`, `Catalogue.get`, `Namespace`, `Option`
- `capture_scored_choice` depends on: `BotObservation`, `Choice`, `CapturedChoice`, `TacticalDecisionContext`, `build_choice_matrix`
- `build_choice_matrix` depends on: `BotObservation`, `Choice`, `CapturedOption`, `TacticalDecisionContext`, `numeric_option_features` (from features.py), `_canonical` (from rollout.py), `choice_fingerprint` (from rollout.py)
- `write_capture_shard` depends on: `Path`, `CapturedChoice.canonical_json`
- `ChoiceCaptureDecider` depends on: `GameView`, `Decider`, `BotObservation`, `TacticalDecisionContext`, `Choice`, `Option`, `capture_scored_choice`

## Compatibility class
- **CapturedChoice** schema: `exact` — schema_version=3; Rust must emit byte-identical JSON for compatibility with existing capture shards.
- **CapturedOption** schema: `exact` — payload_sha256 is SHA-256 of canonical JSON; Rust must reproduce.
- **TypedReference** schema: `exact` — namespace + id pair.
- **typed_references** output: `exact` — deterministic tuple of TypedReference sorted by (namespace, id).
- **numeric_option_features** output: `exact` — sorted tuple of (name, value) pairs.
- **write_capture_shard** output: `exact` — atomic JSONL with header row; Rust must match for artifact compatibility.

## Non-goals
- No implementation of capture logic; documentation-only inventory.
- No analysis of downstream consumers (training, telemetry, Parquet).
- No schema migration analysis.

## Evidence
- File: `D:\Projects\ti4-engine\engine\ml\capture.py` (read-only)
- Line count: 348
- Public symbols: 3 constants (CAPTURE_SCHEMA_VERSION, _KIND_NAMESPACE, _PAYLOAD_NAMESPACE), 3 dataclasses, 4 public functions, 1 class
- Private symbols: 3 functions (_known_reference, _typed_references, _payload_json)
- Cross-module dependencies: features.py (numeric_option_features), rollout.py (_canonical, choice_fingerprint), context.py (TacticalDecisionContext), observation.py (BotObservation), catalogue.py (Catalogue, Namespace)

## Completion
- [x] All public constructors, factories, and classes catalogued
- [x] All init signatures documented
- [x] All dependencies listed
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
