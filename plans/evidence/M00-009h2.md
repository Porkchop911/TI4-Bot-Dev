# M00-009h2 — Initial-setup reproducibility verification

## Package specification

- **Milestone/package:** M00-009h2, child of M00-009h; dependency M00-009h1 (`643f456`).
- **Objective:** Verify the executable initial-setup stream is byte-identical for identical input
  and observably different for the seed, seating, and source variations required by M00-009h.
- **Permission class:** P0 oracle inspection; P1 writes limited to this evidence file,
  `plans/EXECUTION_STATE.md`, and a narrowly scoped repository ignore entry for reproducible M00
  scratch output. No network or external-state changes.
- **Inputs/outputs:** five CLI invocations into a bounded temporary directory under the Rust repo;
  SHA-256 checks only are retained in evidence.
- **Compatibility class:** exact byte comparison for the supported `initial_setup` stream.
- **Explicit non-goals:** proving a full-game event trace, replay corpus, entropy capture, or
  cross-engine parity.

## Campaign

All commands used `PYTHONDONTWRITEBYTECODE=1` and `--validate-schema`:

- `repeat-a.ndjson` and `repeat-b.ndjson`: game `repeat`, seed `42`, seats `alpha,beta`, sources
  `full`.
- `seed-43.ndjson`: same input except seed `43`.
- `seats-swapped.ndjson`: same input except seats `beta,alpha`.
- `base.ndjson`: same input except sources `base`.

SHA-256 results:

| File | SHA-256 |
|---|---|
| `repeat-a.ndjson` | `A3BDF6C009313AF04A2EDCF74939407635A4AB9F97E3523A3B6CA685EF1A0862` |
| `repeat-b.ndjson` | `A3BDF6C009313AF04A2EDCF74939407635A4AB9F97E3523A3B6CA685EF1A0862` |
| `seed-43.ndjson` | `D5A0774B341679391D75C75A01FCD1D385BFD01B5CC38D851B5D274593F21AA6` |
| `seats-swapped.ndjson` | `0CF6B21AA987602DEB99613C685A113006228B1E64A078A29839FDB4CE8DF451` |
| `base.ndjson` | `20D3B7145A16039C8C26D46927A8B5E5DCA9971487528C52233CE0225481E4F3` |

The two identical-input hashes match; each required variation differs. The runtime environment
rejected deletion of the verified `D:\Projects\ti4-engine-rs\.tmp-m00-009h2` scratch directory.
It contains only the five reproducible campaign outputs, is outside tracked scope, and is covered
by the new `/.tmp-m00-*/` ignore entry; no output artifact is staged or committed.

## Validation and review

- Oracle guard after the campaign: HEAD `37061c511a4780d4c0719e0342533a498cd4b457`; clean tree
  `True`.
- `git diff --check` and the focused exporter suite are rerun before commit.
- Independent review is owner-waived under the recorded project policy; no reviewer is claimed.

## Status

**Complete:** recorded in the package commit; the authoritative branch and HEAD are maintained in
`plans/EXECUTION_STATE.md`.
