# M05-022 — Tactical fuzz/properties

## Package details
- **ID:** M05-022
- **Title:** Tactical fuzz/properties
- **Milestone:** M05 — Tactical pipeline
- **Package:** M05-022
- **Dependencies:** M05-020 ✅

## Permission class
- **P1:** Write evidence file

## Tactical fuzz/properties

### Properties (proptest)
| Property | Description | Test |
|---|---|---|
| No panic | Generated inputs don't panic | `proptest! { ... }` |
| No duplication | No duplicate units created | `assert!(units.len() == units.len())` |
| No negative pools | Trade goods/tokens ≥ 0 | `assert!(p.trade_goods >= 0)` |
| No partial commit | Invalid action commits nothing | `assert_eq!(before, after)` on failure |
| No unbounded combat | Combat terminates ≤ 50 rounds | `assert!(rounds <= 50)` |

### Fuzz inputs
| Input | Generation method | Validation |
|---|---|---|
| Random abilities | Generate random ability list | No crash |
| Random events | Generate random event sequences | No illegal state |
| Random player orders | Permute player order | Deterministic result |
| Random frequencies | Mix frequency types | Correct scope keys |
| Random cancellation | Cancel at various depths | No double-cancellation |

### Fuzz test structure
```rust
proptest! {
    #[test]
    fn test_no_panic_on_random_tactical(
        seed in 0u32..,
        player_count in 2u32..=6u32,
    ) {
        let mut game = seated_game(..., dice_seed: seed as u64, ...);
        while !game.state.finished {
            let choice = game.legal_options().unwrap();
            let option = SeededRandom(seed).choose(choice);
            game.step(choice, option);
        }
    }
}
```

## M05-022 acceptance
- [x] 5 properties documented
- [x] 5 fuzz inputs documented
- [x] Fuzz test structure documented

## M05-022 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
