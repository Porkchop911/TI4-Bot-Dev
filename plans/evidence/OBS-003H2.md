# Evidence — OBS-003h slice 2, reaction and remaining content context producers

## Scope and provenance

- Branch: `wp/tier-c-review-remediation-obs008c2b-003e1`.
- Base: `576150a` (`OBS-003h` slice 1), with the c2b/e1 Tier-C remediation already committed.
- Normative sources: `plans/STAGE2_COMPLETE_DECISION_CONTRACT.md` (`OBS-003h`) and
  `plans/OBS-003H2_REACTION_CONTENT_CONTEXT.md`; LRR 2, 22, 35, 45.4, and the named content text.
- Historical Python: not inspected and not used as an acceptance oracle.
- Permission: P1 only. No network, external write, destructive action, or committed generated
  artifact.

## Changed paths

- `crates/ti4-engine/src/exploration.rs`
- `crates/ti4-engine/src/faction_abilities.rs`
- `crates/ti4-engine/src/laws.rs`
- `crates/ti4-engine/src/reactions.rs`
- `crates/ti4-engine/src/relics.rs`
- `crates/ti4-engine/src/secrets.rs`
- `crates/ti4-engine/src/thunders_edge.rs`
- `plans/OBS-003H2_REACTION_CONTENT_CONTEXT.md`
- `plans/evidence/OBS-003H2.md`
- `plans/EXECUTION_STATE.md`

## Result

The seven remaining OBS-003h files now supply stable sources and subtypes on all their registered
choice producers. In particular, relic technology selection is no longer an unattributed helper:
each caller supplies the relic or exploration-card alias that caused the choice.

## Focused evidence

- The in-scope `Choice::new` inventory contains 17 sites. Each is immediately contextualized;
  this is checked from the scoped diff before review.
- `obs003h_exploration_choice_carries_card_context` resolves Ion Storm with a capturing decider and
  asserts `Content("ion_storm")` / `ion_storm_choose_reward`, proving that live delivery preserves
  typed context rather than relying on the prompt.

## Tests and checks

| command | result |
|---|---|
| `cargo check -p ti4-engine` | passed |
| `cargo test -p ti4-engine --lib obs003h_exploration_choice_carries_card_context --quiet` | 1 passed |
| `cargo test -p ti4-engine --lib obs003h --quiet` | 2 passed |
| `cargo clippy -p ti4-engine --all-targets -- -D warnings` | passed |
| `git diff --check` | passed |

The full engine suite (1,176 library + 4 integration + 5 doc tests), ordinary policy suite (201),
and training suite (133) also pass. `cargo fmt -p ti4-engine -- --check` reports only the
pre-existing formatting hunk in `strategy_cards.rs` from the prior remediation commit; that file is
outside this package and was not staged or rewritten.

## Compatibility and non-goals

- No option ID, label, legal set, state transition, prompt, feature, or replay contract changed.
- The contexts use bounded content aliases or rule references, never transient player/planet/system
  display identity.
- No vocabulary generation, bundle republish, or historical-Python comparison belongs to this slice.

## Independent Tier-C review

Approved after one P2 evidence correction. The reviewer independently counted 17 scoped
`Choice::new` sites (not the initially recorded 20); the evidence was corrected and rechecked.
All 17 are contextualized. No remaining finding: source/subtype identity, caller alias propagation,
and unchanged mechanics/option identities were accepted.
