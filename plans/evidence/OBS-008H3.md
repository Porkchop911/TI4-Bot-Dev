# Evidence — OBS-008h3, secret-objective-count content consequence surface

See `plans/OBS-008H3_SECRET_OBJECTIVE_COUNT_CONSEQUENCE_SURFACE.md` for design; this records checks.

## Changed paths

- `crates/ti4-engine/src/preview.rs` — new `Quantity::SecretObjectivesHeld` variant.
- `crates/ti4-engine/src/secrets.rs` — `enforce_hand_limit`'s return options attach an exact
  preview (via `held_count`, so a scored secret still counts toward the total).
- `crates/ti4-engine/src/thunders_edge.rs` — the expedition's "action_cards" and "secret" discard
  options each attach an exact preview.
- `crates/ti4-policy/src/features.rs` — `content_decision_features` gains a fifth preview quantity
  mapping (`SecretObjectivesHeld`).
- `plans/OBS-008H3_SECRET_OBJECTIVE_COUNT_CONSEQUENCE_SURFACE.md`,
  `plans/evidence/OBS-008H3.md`, `plans/EXECUTION_STATE.md`.

## Focused evidence

- `secrets.rs::obs008h3_return_over_secret_hand_limit_previews_the_exact_loss`: a hand one over the
  limit (4 held against a limit of 3) previews the return option `4 -> 3`.
- `thunders_edge.rs::obs008h3_expedition_action_card_discard_previews_the_exact_loss`: a two-card
  hand previews the first discard option `2 -> 1` (the second iteration, left with one card, asks
  nothing).
- `thunders_edge.rs::obs008h3_expedition_secret_discard_previews_the_exact_loss`: a two-secret hand
  previews the discard option `2 -> 1`.
- `features.rs::obs008h3_secret_objective_count_preview_reaches_the_policy`: maps to
  `content:secret-objectives-after` (3.0), and `-change` survives `projection::admits`.

## Tests and checks

| command | result |
|---|---|
| `cargo test -p ti4-engine --lib obs008h3` | 3 passed |
| `cargo test -p ti4-engine --lib secrets::` | 43 passed (42 pre-existing + 1 new) |
| `cargo test -p ti4-engine --lib thunders_edge::` | 3 passed (1 pre-existing + 2 new) |
| `cargo test -p ti4-policy --lib obs008h3` | 1 passed |
| `cargo test -p ti4-engine` | 1,207 lib (1,204 + 3 new) + 4 integration + 5 doc passed |
| `cargo test -p ti4-policy --lib` | 230 passed (229 + 1 new, incl. the 102-game campaign,
  305.95s, no regression) |
| `cargo test -p ti4-training` | 133 passed |
| `cargo clippy -p ti4-engine -p ti4-policy --all-targets -- -D warnings` | clean |
| `rustfmt --edition 2024 --check` (scoped files) + `git diff --check` | clean |

## Independent Tier-C review

OUTSTANDING (deferred per standing repo convention; reviewed elsewhere per user confirmation this
session). This closes the `ActionCardsHeld`/`SecretObjectivesHeld` pair `OBS-008h2` opened; the
remaining `content` subtypes (`titan_prototype_choose_builder`, `stellar_converter_choose_target`,
`crown_of_emphidia_choose_planet`, `dominus_orb_purge_to_move`, `neuraloop_choose_relic_to_purge`,
trade terms, most reactions/action-cards) stay without a preview, checked previously and found to
be identity or permission choices without a uniform per-option quantity, or left for a future pass.
