# M00-004a12 — ML Linear APIs Construction API Inventory

## Scope
`engine/ml/linear.py` (79 physical lines). Small dependency-free runtime for frozen sparse linear choice models.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/linear.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\linear.py
  → 79
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\linear.py
  → 4 matching lines (2 annotated constants + 1 @dataclass decorator + 1 class)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\linear.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `LINEAR_MODEL_SCHEMA_VERSION` (schema constant)
| Field | Value |
|---|---|
| Symbol | `LINEAR_MODEL_SCHEMA_VERSION` |
| Kind | Module-level constant |
| Signature/Value | `1` |
| Source line | 14 |
| Construction role | Schema version identifier for linear choice model files. Used in `LinearChoiceModel.load()` to validate loaded JSON payloads. |
| Rust relevance | Must be preserved in Rust model loading; any schema change requires migration path. |

### 2. `FEATURE_SCHEMA_VERSION` (feature schema constant)
| Field | Value |
|---|---|
| Symbol | `FEATURE_SCHEMA_VERSION` |
| Kind | Module-level constant |
| Signature/Value | `1` |
| Source line | 15 |
| Construction role | Feature schema version identifier. Used in `LinearChoiceModel.load()` to validate that the feature extraction pipeline matches the model's expectations. |
| Rust relevance | Must be preserved in Rust model loading; ensures feature pipeline compatibility. |

### 3. `LinearChoiceModel` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `LinearChoiceModel` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `LinearChoiceModel(model_id: str, option_kind: str, score_scale: float, weights: Mapping[str, float], metadata: Mapping[str, object])` |
| Source line | 19 |
| Construction role | Immutable linear ranking model value object holding fitted raw coefficients for strategy-card choices. Constructed via: (a) direct instantiation with model_id, option_kind, score_scale, weights, metadata; (b) `load()` classmethod which reads a JSON file, validates model_schema_version, feature_schema_version, option_kind == "strategy_card", validates weights are non-empty dict, validates score_scale is positive and finite, validates all weights are finite, verifies model_id matches SHA-256 hash of remaining payload fields. |
| Rust relevance | Core model type; Rust equivalent must carry model_id (String), option_kind (String), score_scale (f64), weights (HashMap<String, f64>), metadata (HashMap<String, serde_value>). Construction via `load()` requires JSON deserialization, multi-field schema validation, weight validation, and SHA-256 integrity check. Critical for strategy-card policy evaluation. |

### 3a. `LinearChoiceModel.load()` (classmethod factory)
| Field | Value |
|---|---|
| Symbol | `LinearChoiceModel.load` |
| Kind | `@classmethod` factory |
| Signature | `load(cls, path: str | Path) -> LinearChoiceModel` |
| Source line | 27 |
| Construction role | Loads a linear choice model from a JSON file at `path`. Validates: (1) `model_schema_version` matches `LINEAR_MODEL_SCHEMA_VERSION`; (2) `feature_schema_version` matches `FEATURE_SCHEMA_VERSION`; (3) `option_kind` == "strategy_card"; (4) `weights` is non-empty dict; (5) `score_scale` is positive and finite; (6) all weights are finite floats; (7) `model_id` matches SHA-256(`json.dumps(remaining_payload, sort_keys=True, separators=(",", ":"))`)[:12]. Returns `cls(actual_id, option_kind, score_scale, converted_weights, training_metadata)`. Raises `ValueError` on any validation failure. |
| Rust relevance | Rust constructor `LinearChoiceModel::load(path: &Path) -> Result<Self, Error>`. Must implement identical validation pipeline and SHA-256 integrity check. Critical for model loading reproducibility. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `__future__` import | import | 3 | Future annotations import |
| `hashlib` import | import | 5 | Stdlib import, not engine API |
| `json` import | import | 6 | Stdlib import, not engine API |
| `math` import | import | 7 | Stdlib import, not engine API |
| `dataclass` import | import | 8 | Stdlib import, not engine API |
| `Path` import | import | 9 | Stdlib import, not engine API |
| `Mapping` import | import | 10 | Stdlib import, not engine API |
| `option_features` import | import | 12 | External function from `engine.ml.model_features`, not constructed here |
| `LinearChoiceModel.applies()` | method | 53 | Applicability check, not construction |
| `LinearChoiceModel.scores()` | method | 57 | Scoring method, not construction |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 4 (2 constants + 1 dataclass + 1 classmethod factory) |
| Excluded top-level definitions | 11 (docstrings + stdlib imports + type imports + methods) |
| Source lines covered | 1–79 |
| Discovery matching lines | 4 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/linear.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
