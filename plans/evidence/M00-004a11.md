# M00-004a11 — ML Guard Construction API Inventory

## Scope
`engine/ml/guard.py` (67 physical lines). Narrow known-answer dominance relations for a learned option scorer.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/guard.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\guard.py
  → 67
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\guard.py
  → 3 matching lines (3 function definitions; 1 is private `_features` matched by underscore in character class)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\guard.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `tactical_dominance()` (dominance relation function)
| Field | Value |
|---|---|
| Symbol | `tactical_dominance` |
| Kind | Module-level function |
| Signature | `tactical_dominance(capture: dict) -> frozenset[tuple[str, str]]` |
| Source line | 16 |
| Construction role | Computes dominance relations between options in a tactical decision capture. Extracts numeric features per option via `_features()`, identifies deliverers (ground deliverable > 0), required loaders (ground loaded > 0), and checks for abandoned ground/uncontested planet consequences. Returns a frozenset of (option_id, "done_moving"/"done_loading"/"done_committing") pairs representing dominance relations. |
| Rust relevance | Core dominance computation; Rust equivalent must reproduce identical dominance relations for identical capture input. Depends on `_features` helper. Critical for policy hierarchy enforcement. |

### 2. `pairwise_guard()` (pairwise dominance query)
| Field | Value |
|---|---|
| Symbol | `pairwise_guard` |
| Kind | Module-level function |
| Signature | `pairwise_guard(capture: dict, left: str, right: str) -> int` |
| Source line | 60 |
| Construction role | Returns +1 if `left` dominates `right`, -1 if `right` dominates `left`, otherwise 0. Delegates to `tactical_dominance()` and checks if the pair appears in the dominance relations. |
| Rust relevance | Rust equivalent: `fn pairwise_guard(capture: &dict, left: &str, right: &str) -> i32`. Simple wrapper around dominance set lookup. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–8 | Documentation, not code |
| `_features` | private function | 12 | Private function (leading underscore), not public API |
| `__future__` import | import | 10 | Future annotations import |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (2 module-level functions) |
| Excluded top-level definitions | 3 (docstrings + private function + stdlib import) |
| Source lines covered | 1–67 |
| Discovery matching lines | 3 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/guard.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
