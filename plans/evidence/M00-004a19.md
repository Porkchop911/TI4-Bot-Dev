# M00-004a19 — Tactical Plan Feature APIs Construction API Inventory

## Scope
`engine/ml/tactical_plan_features.py` (268 physical lines). Sparse pre-action features for deployable tactical plan candidates.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/tactical_plan_features.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\tactical_plan_features.py
  → 268
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\tactical_plan_features.py
  → 4 matching lines (2 public functions + 2 private functions)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\tactical_plan_features.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `tactical_plan_features()` (sparse feature combination function)
| Field | Value |
|---|---|
| Symbol | `tactical_plan_features` |
| Kind | Module-level function |
| Signature | `tactical_plan_features(capture: dict, plan: dict) -> dict[str, float]` |
| Source line | 10 |
| Construction role | Combines tactical plan metadata with actor-visible game state to produce sparse feature dict. Extracts: plan bias (1.0), hand_score, move_count, cargo_count, landing_count, origin_count, capacity, unused_capacity, round, own_vp, leader_vp (max opponent VP), ground/fleet_required from context, faction-scoped plan keys (faction, round, purpose, variant, cross-terms), component counts, move unit/faction cross-terms, origin terms, gravity_drive_moves, cargo unit/faction cross-terms, landing planet/unit terms. Returns `dict[str, float]` with all values as 1.0 or float-cast inputs. |
| Rust relevance | Feature engineering function for ML training pipeline; must reproduce identical feature dict for identical capture/plan inputs. Depends on `actor` from `engine.ml.model_features`. Critical for tactical plan evaluation features. |

### 2. `tactical_plan_forecast_features()` (V2 forecast feature function)
| Field | Value |
|---|---|
| Symbol | `tactical_plan_forecast_features` |
| Kind | Module-level function |
| Signature | `tactical_plan_forecast_features(capture: dict, plan: dict) -> dict[str, float]` |
| Source line | 100 |
| Construction role | V2 package features with actor-visible destination forecast. Deliberately analytical (not copied-game rollout). Cannot see opponent private cards or future dice. Builds on `tactical_plan_features()` base, then adds: destination system lookup, moved_value/power/capacity, cargo_ground/fighters/ground_power, friendly/enemy ship value/power pre/post move, space_win_proxy, free/contested_landings, expected_planets_gained, enemy_ground_power, destination resources/influence/planets, ground/fleet surplus, unused_capacity_ratio, is_mecatol, custodians_available, purpose|variant cross-terms, moved_power/cargo_ground interaction terms with context (enemy_ship_power, enemy_ground_power, free/contested_landings, ground/fleet_required), objective interaction terms (objective_id|purpose, objective_id|expected_planets, objective_id|moved_value), move_unit|enemy_ship_bucket interaction terms. Returns `dict[str, float]` with forecast-prefixed keys. |
| Rust relevance | Feature engineering function for ML training pipeline; must reproduce identical forecast features for identical capture/plan inputs. Depends on `tactical_plan_features`, `_combat_power`, `unit_type`, `valuation`, `galaxy`. Critical for tactical plan V2 evaluation features. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `_combat_power` | private function | 82 | Private function (leading underscore), not public API |
| `_add` | private function | 96 | Private function (leading underscore), not public API |
| `__future__` import | import | 3 | Future annotations import |
| `actor` import | import | 5 | External function from `engine.ml.model_features`, not constructed here |
| `unit_type` import | import | 6 | External function from `engine.units`, not constructed here |
| `galaxy` import | import | 4 | External module from `engine`, not constructed here |
| `valuation` import | import | 4 | External module from `engine`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (2 module-level functions) |
| Excluded top-level definitions | 8 (docstrings + private functions + stdlib imports + type imports) |
| Source lines covered | 1–268 |
| Discovery matching lines | 4 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/tactical_plan_features.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
