# M00-004b9c — tools/evaluate_archived_stage1_six_player.py Entry Point Inventory

## Scope
`tools/evaluate_archived_stage1_six_player.py` (120 lines). Evaluate archived telemetry profiles in six-player Stage-1 environment.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/evaluate_archived_stage1_six_player.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\evaluate_archived_stage1_six_player.py
  → 120
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\evaluate_archived_stage1_six_player.py
  → 4 matching lines (1 module constant tuple + 1 public function + 1 public function + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\evaluate_archived_stage1_six_player.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main()` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main()` |
| Kind | Module-level function |
| Source line | 62 |
| Construction role | Parses args via `argparse.ArgumentParser`, loads base champions from six_player_checkpoint JSON, loads archived profiles for jolnar/l1z1x via `archived_profile()`, builds 4 specification bundles (native, g25-jolnar, g25-l1z1x, g25-both), sets `trainer.FACTIONS`, calls `trainer.evaluate()` with custom reward weights, computes per-arm/per-faction statistics (clear_rate, paired_clear_delta, planets/systems/units gained, score, errors), builds report JSON, writes to `--output` or stdout. |

### 2. `archived_profile()` — Archive loader
| Field | Value |
|---|---|
| Symbol | `archived_profile(generation: Path, faction: str, bundle: int) -> dict` |
| Kind | Module-level function |
| Source line | 30 |
| Construction role | Reads `profiles.parquet` from generation directory, matches faction+bundle, reconstructs policy profile from weights via `build_leaves()` + `set_value()`, validates profile hash, detaches nested values, returns independent profile dict. |

### 3. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 120 |
| Construction role | Invokes `main()` directly (no `SystemExit` wrapper). |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `six_player_checkpoint` | Path (positional) | **required** | Six-player checkpoint JSON |
| `generation` | Path (positional) | **required** | Generation directory with profiles.parquet |
| `--jolnar-bundle` | int | **required** | Jolnar profile bundle ID |
| `--l1z1x-bundle` | int | **required** | L1Z1X profile bundle ID |
| `--seed` | int | 81_000_000 | Starting seed |
| `--seeds` | int | 24 | Number of seeds |
| `--workers` | int | 30 | Worker count |
| `--output` | Path | None | Output report path |

## Module-level constants
| Symbol | Value | Line |
|---|---|---|
| `FACTIONS` | `("sol", "letnev", "xxcha", "hacan", "jolnar", "l1z1x")` | 26 |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `ruff: noqa` comment | linter directive | 3 | Ruff noqa directive |
| `ROOT` | module constant | 11 | Module-level path constant (sys.path manipulation) |
| `__future__` import | import | 5 | Future annotations import |
| `argparse` import | import | 7 | Stdlib import |
| `copy` import | import | 8 | Stdlib import |
| `json` import | import | 9 | Stdlib import |
| `statistics` import | import | 10 | Stdlib import |
| `sys` import | import | 11 | Stdlib import |
| `Path` import | import | 12 | Stdlib import |
| `pyarrow.parquet` import | import | 14 | External dependency |
| `evolve_save54_three_player` import | import | 16 | External module from tools |
| `training_telemetry` import | import | 17 | External module from tools |
| `baseline_profile` import | import | 18 | External function from tools |
| `validate_profile` import | import | 18 | External function from tools |
| `parameters` import | import | 19 | External function from tools |
| `set_value` import | import | 19 | External function from tools |
| `build_leaves` import | import | 20 | External function from tools |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (1 entry point function + 1 archive loader function) |
| Excluded top-level definitions | 18 (docstrings + linter directive + module constant + stdlib imports + external imports) |
| Module-level constants inventoried | 1 (FACTIONS) |
| Source lines covered | 1–120 |
| Discovery matching lines | 4 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/evaluate_archived_stage1_six_player.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b9.
