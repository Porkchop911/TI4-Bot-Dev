# M00-004b3a — tools/analyze_margin_by_faction.py Entry Point Inventory

## Scope
`tools/analyze_margin_by_faction.py` (153 lines). Faction-scoped override rate and VP delta analysis.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/analyze_margin_by_faction.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\analyze_margin_by_faction.py
  → 153
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\analyze_margin_by_faction.py
  → 5 matching lines (1 class-like function + 2 functions + __main__ block + MARGINS constant)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\analyze_margin_by_faction.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main()` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main()` |
| Kind | Module-level function |
| Source line | 117 |
| Construction role | Parses args via `parse_args()`, loads diagnostic panel JSON, loads `TacticalMacroModel` from configs, iterates margins (0.0–0.50) calling `selection_by_faction()`, prints per-faction override rate and VP delta table, optionally writes JSON report to `--output` path. Returns exit code 0. |

### 2. `parse_args()` — CLI argument parser
| Field | Value |
|---|---|
| Symbol | `parse_args()` |
| Kind | Module-level function |
| Source line | 100 |
| Construction role | Returns `argparse.ArgumentParser` with `description=__doc__`. Flags: `--dataset` (Path, default `ROOT/out/ml_sanitized_activation_validation_diagnostic.json`), `--artifact` (str, default `"tactical_macro_teacher_letnev_v1.json"`, help "file name under configs/ml"), `--output` (Path, default `None`). |

### 3. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 153 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--dataset` | Path | `ROOT/out/ml_sanitized_activation_validation_diagnostic.json` | Diagnostic panel input |
| `--artifact` | str | `"tactical_macro_teacher_letnev_v1.json"` | Model artifact name under configs/ml |
| `--output` | Path | `None` | Optional output report path |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–16 | Documentation, not code |
| `ROOT` | module constant | 26 | Module-level path constant (sys.path manipulation) |
| `MARGINS` | module constant | 29 | Tuple of override margin thresholds (0.0–0.50) |
| `selection_by_faction` | public function | 37 | Public function but not a CLI entry point; analysis helper |
| `__future__` import | import | 18 | Future annotations import |
| `argparse` import | import | 20 | Stdlib import |
| `json` import | import | 21 | Stdlib import |
| `statistics` import | import | 22 | Stdlib import |
| `sys` import | import | 23 | Stdlib import |
| `defaultdict` import | import | 24 | Stdlib import |
| `Path` import | import | 25 | Stdlib import |
| `TacticalMacroModel` import | import | 27 | External class from `engine.ml.tactical_macro_runtime`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (1 entry point function + 1 parser function) |
| Excluded top-level definitions | 12 (docstrings + module constants + helper functions + stdlib imports + type imports) |
| Source lines covered | 1–153 |
| Discovery matching lines | 5 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/analyze_margin_by_faction.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b3.
