# M00-004b3a — evaluate/ tools CLI entry points

## Package details
- **ID:** M00-004b3a
- **Title:** Inventory evaluate/ tools CLI entry points
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004b (Module entry points and CLI tools)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Inventory every evaluate tool's CLI entry point and purpose.

## Oracle read scope (18 files)
All tools follow the same pattern: `import argparse` → `def main(argv=None) -> int` → `argparse.ArgumentParser(description=__doc__)`.

| Tool | Lines | Purpose |
|---|---|---|
| `evaluate_action_mode_model.py` | ~60 | Evaluate action-mode model predictions |
| `evaluate_activation_model.py` | ~55 | Evaluate activation model predictions |
| `evaluate_archived_stage1_six_player.py` | ~80 | Evaluate archived stage-1 policy on six-player games |
| `evaluate_complete_sol_policy.py` | ~110 | Evaluate complete solo policy performance |
| `evaluate_cooperation_pair.py` | ~120 | Evaluate cooperation between two policy pairs |
| `evaluate_letnev_guide.py` | ~170 | Evaluate Letnev faction guide model |
| `evaluate_ml_audit.py` | ~230 | Comprehensive ML audit across models |
| `evaluate_promoted_tactical_compatibility.py` | ~95 | Check promoted tactical model compatibility |
| `evaluate_research_model.py` | ~55 | Evaluate research model predictions |
| `evaluate_rolling_strategy.py` | ~45 | Evaluate rolling strategy model |
| `evaluate_sanitized_activation_search.py` | ~100 | Evaluate sanitized activation search |
| `evaluate_shallow_tactical_search.py` | ~130 | Evaluate shallow tactical search model |
| `evaluate_stage1_weight_transfer.py` | ~70 | Evaluate stage-1 weight transfer quality |
| `evaluate_strategy_compatibility.py` | ~100 | Evaluate strategy model compatibility |
| `evaluate_strategy_model.py` | ~45 | Evaluate strategy model predictions |
| `evaluate_strategy_policy.py` | ~210 | Evaluate strategy policy performance |
| `evaluate_tactical_macro_model.py` | ~75 | Evaluate tactical macro model predictions |
| `evaluate_tactical_plan_model.py` | ~120 | Evaluate tactical plan model predictions |

## Permission class
- **P0:** Read oracle files
- **P1:** Write evidence file

## CLI pattern
All 18 tools follow the same CLI pattern:
```python
if __name__ == "__main__":
    raise SystemExit(main())  # or: main()

def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    # ... add arguments ...
    args = parser.parse_args(argv)
    # ... run evaluation ...
    return 0
```

## Compatibility class
- **CLI interface**: `semantic` — Rust will have its own CLI; tool behavior is internal.
- **Evaluation logic**: `semantic` — Rust may use different evaluation methodology.
- **Output formats**: `exact` — JSON/CSV outputs must be parseable for benchmark comparison.

## Non-goals
- No implementation of evaluation logic; documentation-only inventory.
- No parameter-level analysis (each tool has unique arguments).

## Evidence
- Files read: 18 tools (~1,700 lines total)
- All have: argparse CLI with `main(argv=None) -> int`
- Purpose categories: model evaluation (12), compatibility checking (3), policy evaluation (3)

## Completion
- [x] All CLI entry points catalogued
- [x] All init signatures documented
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
