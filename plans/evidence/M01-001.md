# M01-001 — Initialize Git repository

## Package details
- **ID:** M01-001
- **Title:** Initialize Git repository
- **Milestone:** M01 — Repository bootstrap
- **Package:** M01-001
- **Dependencies:** M00 (Oracle and baseline) ✅

## Permission class
- **P1:** Write evidence file

## Deliverable

### Repository state
- **Path:** `D:\Projects\ti4-engine-rs`
- **Branch:** `main` (default)
- **Python oracle:** `D:\Projects\ti4-engine` (read-only, no symlink, no submodule, no gitlink)

### .gitignore
```gitignore
# Local Pi RPC controller state and logs.
.pi-control/
.pi/

# Python artifacts
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
*.egg
dist/
build/
*.pyc

# Rust build artifacts
target/
**/target/
*.rlib
*.so
*.dll
*.dylib
*.pdb
*.ilk
*.lib

# Cargo registry/cache
.cargo/registry/
.cargo/git/
.cargo/.package-cache

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Secrets and credentials
*.pem
*.key
*.pub
.env
.env.*
!env.example

# Large files
*.pt
*.pkl
*.parquet
*.json.gz
*.checkpoint

# Benchmark outputs
benchmarks/
*.bench.json
*.bench.ndjson

# Generated fixtures (keep originals, not copies)
fixtures/
!fixtures/*.md
```

### Line-ending policy
- `core.autocrlf = input` (commit LF, checkout CRLF on Windows)
- All Cargo.toml, .toml, .md, .rs files: LF in repo
- No `.gitattributes` needed (Cargo handles this)

### Python isolation
- No submodule reference to `D:\Projects\ti4-engine`
- No `.gitlink` files
- No symlinks to Python repo
- No post-checkout hooks that touch Python repo
- Oracle integrity guard (M00-014) provides read-only verification

## M01-001 acceptance
- [x] Repository initialized on `main` branch
- [x] `.gitignore` covers all build/secret/large-file categories
- [x] Line-ending policy documented (`core.autocrlf = input`)
- [x] No linkage to Python oracle repository
- [x] Oracle integrity guard provides read-only verification

## M01-001 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
