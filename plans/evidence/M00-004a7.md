# M00-004a7 — ML Counterfactual Construction API Inventory

## Scope
`engine/ml/counterfactual.py` (44 physical lines). Privacy-preserving state preparation for copied policy evaluations.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/counterfactual.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\counterfactual.py
  → 44
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+ =' D:\Projects\ti4-engine\engine\ml\counterfactual.py
  → 1 matching line (1 function definition)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\counterfactual.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `sanitize_unseen_state()` (privacy-preserving mutator)
| Field | Value |
|---|---|
| Symbol | `sanitize_unseen_state` |
| Kind | Module-level function |
| Signature | `sanitize_unseen_state(game: Game, actor: str, bundle: RolloutSeedBundle) -> None` |
| Source line | 11 |
| Construction role | Removes opponent private identities (action_cards, secret_objectives, plots) from `game.state` and shuffles every hidden deck (action_card_deck, secret_deck, relic_deck, agenda_deck, exploration_decks) using deterministic PRNG seeded from `bundle.cards` and `bundle.exploration`. Mutates `game.state` in-place via `with_player()` and `with_()` functional updates. No return value — side-effect on the game object. |
| Rust relevance | Privacy sanitizer for counterfactual policy evaluation; must produce identical sanitization for identical inputs. Depends on `Game`, `RolloutSeedBundle` (from `engine.ml.rollout`). |

## Exclusions table
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `__future__` import | import | 3 | Future annotations import |
| `random` import | import | 5 | Stdlib import, not engine API |
| `Game` import | import | 7 | External type dependency from `engine.game`, not constructed here |
| `RolloutSeedBundle` import | import | 8 | External type dependency from `engine.ml.rollout`, not constructed here |
| Local `state` | local variable | 13 | Local variable, not public API |
| Local `player` | local variable | 14 | Loop variable, not public API |
| Local `cards` | local variable | 23 | Local PRNG instance, not public API |
| Local `decks` | local variable | 24 | Local list of sorted deck copies, not public API |
| Local `deck` | local variable | 30 | Loop variable, not public API |
| Local `exploration` | local variable | 32 | Local PRNG instance, not public API |
| Local `exploration_decks` | local variable | 33 | Local dict, not public API |
| Local `trait` | local variable | 34 | Loop variable, not public API |
| Local `values` | local variable | 34 | Loop variable, not public API |
| Local `shuffled` | local variable | 35 | Local sorted list, not public API |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (`sanitize_unseen_state` function) |
| Excluded definitions | 15 (docstrings + stdlib imports + type imports + local variables) |
| Source lines covered | 1–44 |
| Discovery matching lines | 1 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/counterfactual.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
