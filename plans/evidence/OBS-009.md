# Evidence — OBS-009, information-history audit

See `plans/OBS-009_INFORMATION_HISTORY_AUDIT.md` for the audit method, finding, and conclusion;
this records checks.

## Changed paths

- `crates/ti4-engine/src/vote.rs` — `VoteWindow::pending_choice`'s `Stage::Outcome` branch attaches
  a `current_votes` payload (each outcome's running tally, from `self.ballot.counts`) to every
  option.
- `crates/ti4-policy/src/features.rs` — no functional change; a test confirming the payload reaches
  the policy through the existing generic pipeline with zero new code.
- `plans/OBS-009_INFORMATION_HISTORY_AUDIT.md`, `plans/evidence/OBS-009.md`,
  `plans/EXECUTION_STATE.md`.

## Focused evidence

- `vote.rs::obs009_a_later_voter_sees_the_running_tally_of_every_outcome`: before anyone votes,
  both outcome options carry `current_votes: 0`; after the first voter (holding an influential
  planet) votes FOR and exhausts it, the second voter's FOR option carries the planet's exact
  influence, AGAINST still carries 0.
- `features.rs::obs009_vote_ledger_payload_reaches_the_policy`: a `current_votes: 3` payload
  reaches `payload-number:current_votes` = 3.0 and survives `mlp_option_features`; a `current_
  votes: 0` payload is confirmed absent from the feature vector (sparse-zero convention, not a
  regression).

## Tests and checks

| command | result |
|---|---|
| `cargo test -p ti4-engine --lib obs009` | 1 passed |
| `cargo test -p ti4-engine --lib vote::` | 24 passed (23 pre-existing + 1 new) |
| `cargo test -p ti4-policy --lib obs009` | 1 passed |
| `cargo test -p ti4-engine` | 1,208 lib (1,207 + 1 new) + 4 integration + 5 doc passed |
| `cargo test -p ti4-policy --lib` | 231 passed (230 + 1 new, incl. the 102-game campaign,
  287.87s, no regression) |
| `cargo test -p ti4-training` | 133 passed |
| `cargo clippy -p ti4-engine -p ti4-policy --all-targets -- -D warnings` | clean |
| `rustfmt --edition 2024 --check` (scoped files) + `git diff --check` | clean |

## Independent Tier-C review

OUTSTANDING (deferred per standing repo convention; reviewed elsewhere per user confirmation this
session). This is the first audit-shaped package this session rather than a mechanical read-and-
surface pass; the audit's conclusion (one real gap, everything else already covered) is a judgment
call worth an independent check, same as the earlier structural-subtype-matching judgment call
flagged in `OBS-008EFGHI1`. `neighbours_who_transacted`/`transacted_with` is flagged as a real,
durable, not-yet-surfaced fact for a future trade/relationship package -- not treated as a history
gap since it is already reconstructible from state.
