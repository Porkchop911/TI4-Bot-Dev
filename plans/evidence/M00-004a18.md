# M00-004a18 — Tactical Plan Rollout APIs Construction API Inventory

## Scope
`engine/ml/tactical_plan_rollout.py` (267 physical lines). Prefix-replayed Round-4 labels for deployable ex-ante tactical plans.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/tactical_plan_rollout.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\tactical_plan_rollout.py
  → 267
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\tactical_plan_rollout.py
  → 7 matching lines (1 @dataclass decorator + 1 class + 1 class + 3 functions)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\tactical_plan_rollout.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `TacticalPlanRolloutResult` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `TacticalPlanRolloutResult` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `TacticalPlanRolloutResult(actor: str, destination: str, variant: str, horizon_round: int, final_round: int, final_vp: int, finished: bool, tactical_end_state_fingerprint: str, final_state_fingerprint: str, replayed_prefix_length: int, plan_fallbacks: int, seed_bundle: RolloutSeedBundle \| None)` |
| Source line | 25 |
| Construction role | Immutable value object representing the complete result of a tactical plan rollout. 12 required fields: actor (player id), destination (system), variant (plan variant), horizon_round (4), final_round (clone state round), final_vp (actor victory points), finished (game finished flag), tactical_end_state_fingerprint (SHA-256 at tactical end), final_state_fingerprint (SHA-256 at completion), replayed_prefix_length (len(prefix)), plan_fallbacks (delta in tactical_plan_fallbacks), seed_bundle (optional). Constructed directly via constructor in `rollout_replayed_tactical_plan()` at final return. |
| Rust relevance | Immutable struct; Rust equivalent must carry all 12 fields identically. Construction via named args at rollout completion. Critical for training data collection — captures full rollout state for ex-ante tactical plan evaluation. |

### 2. `rollout_replayed_tactical_plan()` (full tactical plan rollout function)
| Field | Value |
|---|---|
| Symbol | `rollout_replayed_tactical_plan` |
| Kind | Module-level function |
| Signature | `rollout_replayed_tactical_plan(initial_game: Game, prefix: tuple[DecisionRecord, ...], target_state: GameState, choice: Choice, plan: TacticalPlan, *, horizon_round: int = 4, max_steps: int = 10_000, seed_bundle: RolloutSeedBundle | None = None) -> TacticalPlanRolloutResult` |
| Source line | 115 |
| Construction role | Installs one tactical plan at its first movement choice, then restores continuation. Validates: actor exists in target_state, choice has move options, plan destination matches active system, target round <= horizon. Forks game via `fork_game()`, captures source fingerprint. Creates `_ReplayPrefixThenInstallPlan` replay service that (1) replays prefix decision records, (2) validates target state fingerprint matches, (3) installs tactical plan on actor bot via `dataclasses.replace()` and `TacticalPlanCursor`. Runs steps until target reached, then runs tactical action until completion. Captures tactical_end_state_fingerprint. Restores original policy/continuations. Runs remaining rounds to horizon. Validates source fingerprint unchanged. Returns `TacticalPlanRolloutResult` with all rollout metadata. Raises `ValueError` on precondition failures, `RuntimeError` on max_steps exceeded, `ExpectedChoiceMismatch` on prefix divergence. |
| Rust relevance | Rust function `fn rollout_replayed_tactical_plan(initial_game: &Game, prefix: &[DecisionRecord], target_state: &GameState, choice: &Choice, plan: &TacticalPlan, horizon_round: u32, max_steps: u32, seed_bundle: Option<&RolloutSeedBundle>) -> Result<TacticalPlanRolloutResult, Error>`. Core tactical plan rollout; must reproduce identical result for identical game state. Depends on `fork_game`, `state_fingerprint`, `decision_fingerprint`, `_ReplayPrefixThenInstallPlan`, `TacticalPlanCursor`. Critical for training data collection. |

### 3. `shallow_replayed_tactical_plan()` (shallow tactical plan replay function)
| Field | Value |
|---|---|
| Symbol | `shallow_replayed_tactical_plan` |
| Kind | Module-level function |
| Signature | `shallow_replayed_tactical_plan(initial_game: Game, prefix: tuple[DecisionRecord, ...], target_state: GameState, choice: Choice, plan: TacticalPlan, *, seed_bundle: RolloutSeedBundle, max_steps: int = 10_000) -> ShallowTacticalOutcome` |
| Source line | 212 |
| Construction role | Replays to a nested movement choice and evaluates one sanitized shallow branch. Captures source fingerprint, forks game. Creates `_ReplayPrefixThenInstallPlan` replay service with `evaluate()` callback that (1) restores real continuations, (2) calls `shallow_tactical_outcome()` (from `engine.ml.tactical_search`), (3) restores replay seating. Runs steps until target reached, validates prefix_cursor == len(prefix) and exactly one shallow outcome found. Validates source fingerprint unchanged. Returns the single `ShallowTacticalOutcome`. Raises `RuntimeError` on max_steps exceeded, `ExpectedChoiceMismatch` on shallow replay mismatch. |
| Rust relevance | Rust function `fn shallow_replayed_tactical_plan(initial_game: &Game, prefix: &[DecisionRecord], target_state: &GameState, choice: &Choice, plan: &TacticalPlan, seed_bundle: &RolloutSeedBundle, max_steps: u32) -> Result<ShallowTacticalOutcome, Error>`. Shallow evaluation variant; must reproduce identical sanitized outcome. Depends on `fork_game`, `state_fingerprint`, `decision_fingerprint`, `_ReplayPrefixThenInstallPlan`, `shallow_tactical_outcome`. Critical for counterfactual policy evaluation. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `_forced` | private function | 40 | Private function (leading underscore), not public API |
| `_ReplayPrefixThenInstallPlan` | private class | 47 | Private class (leading underscore), not public API |
| `_tactical_in_progress` | private function | 109 | Private function (leading underscore), not public API |
| `__future__` import | import | 3 | Future annotations import |
| `dataclasses` import | import | 5 | Stdlib import, not engine API |
| `dataclass` import | import | 6 | Stdlib import, not engine API |
| `Callable` import | import | 7 | Stdlib import, not engine API |
| `Mapping` import | import | 7 | Stdlib import, not engine API |
| `Choice` import | import | 9 | External type dependency from `engine.choice`, not constructed here |
| `Decider` import | import | 9 | External type dependency from `engine.choice`, not constructed here |
| `DecisionRecord` import | import | 9 | External type dependency from `engine.choice`, not constructed here |
| `Option` import | import | 9 | External type dependency from `engine.choice`, not constructed here |
| `Game` import | import | 10 | External type dependency from `engine.game`, not constructed here |
| `ExpectedChoiceMismatch` import | import | 11 | External type from `engine.ml.rollout`, not constructed here |
| `RolloutSeedBundle` import | import | 12 | External type from `engine.ml.rollout`, not constructed here |
| `_prepare_future_rng` import | import | 13 | External function from `engine.ml.rollout`, not constructed here |
| `decision_fingerprint` import | import | 14 | External function from `engine.ml.rollout`, not constructed here |
| `fork_game` import | import | 15 | External function from `engine.ml.rollout`, not constructed here |
| `state_fingerprint` import | import | 16 | External function from `engine.ml.rollout`, not constructed here |
| `ShallowTacticalOutcome` import | import | 18 | External type from `engine.ml.tactical_search`, not constructed here |
| `shallow_tactical_outcome` import | import | 18 | External function from `engine.ml.tactical_search`, not constructed here |
| `GameState` import | import | 19 | External type dependency from `engine.state`, not constructed here |
| `TacticalPlan` import | import | 20 | External type from `engine.tactical_plans`, not constructed here |
| `TacticalPlanCursor` import | import | 20 | External type from `engine.tactical_plans`, not constructed here |
| `_ReplayPrefixThenInstallPlan.__init__` | method | 49 | Constructor of private class, not public API |
| `_ReplayPrefixThenInstallPlan._continuation` | method | 65 | Internal method of private class, not public API |
| `_ReplayPrefixThenInstallPlan.choose` | method | 70 | Internal method of private class, not public API |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 3 (1 dataclass + 2 functions) |
| Excluded top-level definitions | 28 (docstrings + private functions + private class + stdlib imports + type imports + private class methods) |
| Source lines covered | 1–267 |
| Discovery matching lines | 7 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/tactical_plan_rollout.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
