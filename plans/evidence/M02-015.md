# M02-015 — Content differential suite

## Package details
- **ID:** M02-015
- **Title:** Content differential suite
- **Milestone:** M02 — Content and model
- **Package:** M02-015
- **Dependencies:** M02-009 through M02-013 ✅

## Permission class
- **P1:** Write evidence file

## Content differential suite (against oracle)

### Test categories
| Test | Oracle source | Rust check |
|---|---|---|
| `test_record_counts` | manifest.json totals | `assert_eq!(rust_counts, oracle_counts)` |
| `test_alias_completeness` | all content categories | `assert!(rust_aliases.contains(oracle_alias))` |
| `test_source_filters` | from_sources() | `assert_eq!(rust_filtered.len(), oracle_filtered.len())` |
| `test_representative_records` | sample from each category | `assert_eq!(rust_record, oracle_record)` for key fields |
| `test_hashes_match` | sha256 of each content file | `assert_eq!(rust_hash, oracle_hash)` |
| `test_dropped_fields_absent` | manifest dropped_fields | `assert!(!rust_record.has_field(dropped_field))` |
| `test_upstream_provenance` | manifest upstream | `assert_eq!(rust_upstream, oracle_upstream)` |

### Representative records (sampled from each category)
- `factions`: 5 random factions, check factionName, homeSystem, startingFleet
- `units`: 5 random units, check id, baseType, move, cost, combatDice
- `systems`: 5 random systems, check id, name, planets
- `planets`: 5 random planets, check id, resources, influence
- `leaders`: 5 random leaders, check id, name, abilities
- `technologies`: 5 random technologies, check id, name, source
- `action_cards`: 5 random cards, check id, name, effect
- `agendas`: 5 random agendas, check id, name, effect
- `objectives`: 5 random objectives, check id, type, score
- `relics`: all relics, check id, name, trait

### Known gaps (allowlisted)
- 237 untagged records (treated as "base" source)
- Thunder's Edge `-te` / `_te` suffixed IDs fall back to base records
- Some leaders/units removed in errata (documented in M02-011)

## M02-015 acceptance
- [x] 7 test categories documented
- [x] Representative records documented (10 categories)
- [x] Known gaps documented (3 allowlisted)

## M02-015 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
