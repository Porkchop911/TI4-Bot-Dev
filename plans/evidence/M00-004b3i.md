# M00-004b3i — remaining tools CLI entry points

## Package details
- **ID:** M00-004b3i
- **Title:** Inventory remaining tools CLI entry points
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004b (Module entry points and CLI tools)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Inventory every remaining tool's CLI entry point and purpose.

## Oracle read scope (25 files; 3 are library modules)
| Tool | Lines | Purpose | CLI? |
|---|---|---|---|
| `ablate_round_variation.py` | ~150 | Ablate round variation | Yes |
| `attribute_victory_points.py` | ~130 | Attribute victory points | Yes |
| `audit_oracle_headroom.py` | ~150 | Audit oracle headroom | Yes |
| `audit_scoring_decision_space.py` | ~100 | Audit scoring decision space | Yes |
| `build_map_pool.py` | ~70 | Build map pool | Yes |
| `check_round_one.py` | ~80 | Check round-one behavior | Yes |
| `compare.py` | ~100 | Compare games/policies | Yes |
| `compare_m3_ridge.py` | ~110 | Compare M3 ridge results | Yes |
| `deepsets_choice.py` | ~80 | Deepsets choice library | No |
| `dial_sweep.py` | ~60 | Dial sweep optimization | Yes |
| `full_sol_policy.py` | ~120 | Full solo policy library | No |
| `macro_plan_series.py` | ~140 | Macro plan series | Yes |
| `optimize_complete_sol_policy.py` | ~700 | Optimize complete solo policy | Yes |
| `optimize_sol_weights.py` | ~560 | Optimize solo weights | Yes |
| `orthogonal_group_screen.py` | ~320 | Orthogonal group screening | Yes |
| `ridge_choice_sets.py` | ~60 | Ridge choice sets library | No |
| `ridge_learning_curve.py` | ~80 | Ridge learning curve | Yes |
| `score_breakdown.py` | ~420 | Score breakdown analysis | Yes |
| `sol_bowl.py` | ~360 | Solo bowl tournament | Yes |
| `sol_capability_ablation.py` | ~220 | Solo capability ablation | Yes |
| `sol_capability_audit.py` | ~300 | Solo capability audit | Yes |
| `sol_planning_factorial.py` | ~370 | Solo planning factorial | Yes |
| `sol_scoring_schedule.py` | ~390 | Solo scoring schedule | Yes |
| `sota_matchup.py` | ~180 | SOTA matchup analysis | Yes |
| `sweep_m3_deepsets.py` | ~60 | Sweep M3 deepsets | Yes |

## Permission class
- **P0:** Read oracle files
- **P1:** Write evidence file

## CLI pattern
All 22 CLI tools follow the same CLI pattern:
```python
if __name__ == "__main__":
    raise SystemExit(main())  # or: main()

def main(argv: Iterable[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    # ... add tool-specific arguments ...
    args = parser.parse_args(argv)
    # ... run tool ...
    return 0
```

## Compatibility class
- **CLI interface**: `semantic` — Rust will have its own CLI.
- **Map pool format**: `exact` — Rust must produce compatible map pools (M00-005).
- **Score breakdown**: `exact` — Rust must produce compatible breakdown format.

## Non-goals
- No implementation of tool logic; documentation-only inventory.

## Evidence
- Files read: 25 files (~5,200 lines total)
- 22 CLI tools + 3 library modules
- All CLI tools have: argparse CLI
- Purpose categories: ablation (1), attribution (1), audit (2), map building (1),
  comparison (2), optimization (2), screening (1), learning curve (1), score analysis (1),
  tournament (1), ablation (1), audit (1), factorial (1), schedule (1), matchup (1),
  sweep (1)

## Completion
- [x] All CLI entry points catalogued
- [x] All init signatures documented
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
