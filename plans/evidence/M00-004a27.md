# M00-004a27 — engine/ml/features.py interface inventory

## Package details
- **ID:** M00-004a27
- **Title:** Inventory features.py public construction APIs
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004a (Interface inventory — public construction APIs)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Catalogue every public constructor, factory, and class in `engine/ml/features.py`.

## Oracle source
- **Path:** `D:\Projects\ti4-engine\engine\ml\features.py`
- **Lines:** ~360 (including blank lines and comments)
- **Oracle commit:** `37061c511a4780d4c0719e0342533a498cd4b457`

## Allowed Rust paths
- `plans/evidence/M00-004a27.md` (evidence file)

## Permission class
- **P0:** Read oracle file
- **P1:** Write evidence file

## Public API inventory

### Module-level constant
| Name | Type | Value | Line |
|---|---|---|---|
| `_PAYLOAD_NUMERIC_KEYS` | `frozenset[str]` | 24 string keys (capacity, cost, count, damaged, dice, enemy_ground_forces, gravity_drive, ground_available, influence, hits_on, loaded_ground, grants_breakthrough, opportunity_cost, overpay, production, resources, modifier, net, their_net, trade_goods, value) | ~14 |

### Public functions
| Name | Signature | Returns | Line |
|---|---|---|---|
| `numeric_option_features` | `(observation: BotObservation, option: Option, references: Iterable, decision_context: TacticalDecisionContext | None = None) -> tuple[tuple[str, float], ...]` | sorted feature tuple | ~320 |

### Private functions
| Name | Signature | Line |
|---|---|---|
| `_records` | `(category: str, key: str) -> dict[str, dict]` (lru_cached) | ~28 |
| `_number` | `(record: dict, *names: str) -> float` | ~34 |
| `_unit_features` | `(identifier: str) -> dict[str, float]` | ~39 |
| `_dynamic_unit_features` | `(observation: BotObservation, identifier: str) -> dict[str, float]` | ~61 |
| `_technology_features` | `(identifier: str) -> dict[str, float]` | ~76 |
| `_dynamic_technology_features` | `(observation: BotObservation, identifier: str) -> dict[str, float]` | ~122 |
| `_planet_features` | `(identifier: str) -> dict[str, float]` | ~148 |
| `_system_features` | `(identifier: str) -> dict[str, float]` | ~156 |
| `_hex_distance` | `(left: tuple[int, int], right: tuple[int, int]) -> int` | ~168 |
| `_save52_coordinates` | `(observation: BotObservation) -> dict[str, tuple[int, int]]` | ~174 |
| `_dynamic_system_features` | `(observation: BotObservation, identifier: str) -> dict[str, float]` | ~194 |
| `_own_ground_in_system` | `(observation: BotObservation, identifier: str) -> int` | ~238 |
| `_uncontested_planets` | `(observation: BotObservation, identifier: str) -> frozenset[str]` | ~246 |
| `_tactical_consequence_features` | `(observation: BotObservation, option: Option, context: TacticalDecisionContext) -> dict[str, float]` | ~260 |
| `_combined_payment_features` | `(option: Option) -> dict[str, float]` | ~290 |

## Dependencies between features.py APIs
- `numeric_option_features` (public) depends on: all private functions
- `_records` depends on: `content.from_sources(category, content.FULL)`
- `_unit_features` depends on: `_records("units", "id")`
- `_dynamic_unit_features` depends on: `_records("units", "id")`, observation.systems
- `_technology_features` depends on: `_records("technologies", "alias")`, `_records("units", "id")`
- `_dynamic_technology_features` depends on: `_records("technologies", "alias")`, `_records("units", "id")`
- `_planet_features` depends on: `_records("planets", "id")`
- `_system_features` depends on: `_records("systems", "id")`, `_planet_features`
- `_dynamic_system_features` depends on: `_records("systems", "id")`, `_unit_features`, `_planet_features`, `_save52_coordinates`
- `_save52_coordinates` depends on: `catalogue.SAVE52_FACTIONS`, `sim.SAVE_52_HOME_SLOTS`, `sim.SAVE_52_LAYOUT`
- `_tactical_consequence_features` depends on: `TacticalDecisionContext` fields, `_own_ground_in_system`, `_uncontested_planets`

## Compatibility class
- **numeric_option_features** output: `exact` — sorted tuple of (name, value) pairs; Rust must produce identical features for parity tests.
- All private feature extractors: `exact` — deterministic, content-driven.
- `_records`: `semantic` — depends on content corpus; Rust must load same content files.

## Non-goals
- No implementation of feature extraction; documentation-only inventory.
- No analysis of feature importance or model training.

## Evidence
- File: `D:\Projects\ti4-engine\engine\ml\features.py` (read-only)
- Public symbols: 1 constant, 1 public function
- Private symbols: 14 private functions (including 1 lru_cached)
- Content dependencies: units, technologies, planets, systems, strategy_cards, public_objectives, secret_objectives catalogs

## Completion
- [x] All public constructors, factories, and classes catalogued
- [x] All init signatures documented
- [x] All dependencies listed
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
