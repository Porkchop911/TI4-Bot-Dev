# M00-004a17 — Tactical Search APIs Construction API Inventory

## Scope
`engine/ml/tactical_search.py` (234 physical lines). Information-safe shallow evaluation of same-destination tactical plans.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/tactical_search.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\tactical_search.py
  → 234
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\tactical_search.py
  → 5 matching lines (1 @dataclass decorator + 1 class + 1 class + 2 functions)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\tactical_search.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `ShallowTacticalOutcome` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `ShallowTacticalOutcome` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `ShallowTacticalOutcome(plan: TacticalPlan, macro: dict, fallback_count: int, decision_count: int)` |
| Source line | 29 |
| Construction role | Immutable value object representing the outcome of a shallow tactical plan evaluation. 4 required fields: plan (the evaluated TacticalPlan), macro (dict with activation_option_id, intent, summary, steps), fallback_count (int, computed as difference in tactical_plan_fallbacks), decision_count (int, equal to steps executed). Constructed directly via constructor in `shallow_tactical_outcome()`. |
| Rust relevance | Immutable struct; Rust equivalent must carry plan (TacticalPlan), macro (HashMap<String, serde_value>), fallback_count (u32), decision_count (u32). Construction via named args in `shallow_tactical_outcome()`. Critical for tactical plan evaluation results. |

### 2. `shallow_activation_outcome()` (sanitized activation outcome function)
| Field | Value |
|---|---|
| Symbol | `shallow_activation_outcome` |
| Kind | Module-level function |
| Signature | `shallow_activation_outcome(game: Game, actor: str, activation_option_id: str, *, seed_bundle: RolloutSeedBundle, max_steps: int = 1_000) -> dict` |
| Source line | 36 |
| Construction role | Plays one sanitized incumbent tactical action from an activation choice. Validates: active actor matches, pending state is "activate". Forks game via `fork_game()`, sanitizes unseen state via `sanitize_unseen_state()`, prepares future RNG via `_prepare_future_rng()`. Gets legal options, forces actor bot to choose the activation option, logs and applies the choice. Runs tactical steps (with max_steps cap) via `_tactical_in_progress()` check and `clone.step()`. Returns dict with activation_option_id, intent (from `ml_decision_context`), summary (from `_macro_summary`), and steps (as dicts). Raises `ValueError` on precondition failures, `RuntimeError` on max_steps exceeded. |
| Rust relevance | Rust function `fn shallow_activation_outcome(game: &Game, actor: &str, activation_option_id: &str, seed_bundle: &RolloutSeedBundle, max_steps: u32) -> Result<serde_value, Error>`. Core tactical activation evaluation; must reproduce identical sanitized outcome for identical game state. Depends on `fork_game`, `sanitize_unseen_state`, `_prepare_future_rng`, `_macro_summary`, `TacticalPlan`. Critical for counterfactual policy evaluation. |

### 3. `TacticalActivationSearchService` (non-frozen class)
| Field | Value |
|---|---|
| Symbol | `TacticalActivationSearchService` |
| Kind | `class` (non-dataclass, mutable service object) |
| Signature | `__init__(self, game: Game, actor: str, model: TacticalMacroModel, *, max_candidates: int = 8, min_override_margin: float = 0.30) -> None` |
| Source line | 82 |
| Construction role | Privileged engine-side service exposing only sanitized candidate scores for tactical activation search. 5 constructor parameters: game, actor, model (TacticalMacroModel), max_candidates (default 8), min_override_margin (default 0.30). Internal state: calls (int), failures (int), failure_reasons (Counter[str]), candidates_evaluated (int), overrides (int), last_scores (dict[str, float]). `__deepcopy__` returns None to prevent callback into parent game on copied games. `select()` method evaluates shortlisted activate options via `shallow_activation_outcome()`, scores them via model, and selects the best option (with incumbent override guard). |
| Rust relevance | Mutable service object; Rust equivalent must carry game, actor, model, max_candidates, min_override_margin as constructor params, plus mutable counters (calls, failures, candidates_evaluated, overrides) and last_scores. Construction via named args. Critical for tactical activation search runtime. |

### 4. `shallow_tactical_outcome()` (sanitized tactical outcome function)
| Field | Value |
|---|---|
| Symbol | `shallow_tactical_outcome` |
| Kind | Module-level function |
| Signature | `shallow_tactical_outcome(game: Game, actor: str, plan: TacticalPlan, *, seed_bundle: RolloutSeedBundle, max_steps: int = 1_000) -> ShallowTacticalOutcome` |
| Source line | 178 |
| Construction role | Executes one hypothetical tactical plan through tactical-action completion. Validates: active actor matches, pending state is "move", plan destination matches active system. Forks game via `fork_game()`, sanitizes unseen state via `sanitize_unseen_state()`, prepares future RNG via `_prepare_future_rng()`. Modifies actor_bot.policy with `tactical_intent=True, tactical_plans=True` via `dataclasses.replace()`, sets `_tactical_plan_cursor` and `_tactical_plan_attempted_destination`. Runs tactical steps (with max_steps cap) via `_tactical_in_progress()` check and `clone.step()`. Computes fallback_count as difference in tactical_plan_fallbacks. Returns `ShallowTacticalOutcome(plan, macro, fallback_count, decision_count)` with macro containing activation_option_id, intent, summary, steps. Raises `ValueError` on precondition failures, `RuntimeError` on max_steps exceeded. |
| Rust relevance | Rust function `fn shallow_tactical_outcome(game: &Game, actor: &str, plan: &TacticalPlan, seed_bundle: &RolloutSeedBundle, max_steps: u32) -> Result<ShallowTacticalOutcome, Error>`. Core tactical plan evaluation; must reproduce identical sanitized outcome for identical game state. Depends on `fork_game`, `sanitize_unseen_state`, `_prepare_future_rng`, `_macro_summary`, `TacticalPlanCursor`. Critical for counterfactual policy evaluation and training data collection. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–7 | Documentation, not code |
| `__future__` import | import | 9 | Future annotations import |
| `dataclasses` import | import | 11 | Stdlib import, not engine API |
| `hashlib` import | import | 12 | Stdlib import, not engine API |
| `Counter` import | import | 13 | Stdlib import, not engine API |
| `asdict` import | import | 14 | Stdlib import, not engine API |
| `dataclass` import | import | 14 | Stdlib import, not engine API |
| `Game` import | import | 16 | External type dependency from `engine.game`, not constructed here |
| `sanitize_unseen_state` import | import | 17 | External function from `engine.ml.counterfactual`, not constructed here |
| `RolloutSeedBundle` import | import | 18 | External type from `engine.ml.rollout`, not constructed here |
| `_prepare_future_rng` import | import | 18 | External function from `engine.ml.rollout`, not constructed here |
| `fork_game` import | import | 18 | External function from `engine.ml.rollout`, not constructed here |
| `_RecordingDecider` import | import | 19 | External class from `engine.ml.tactical_macro`, not constructed here |
| `_macro_summary` import | import | 20 | External function from `engine.ml.tactical_macro`, not constructed here |
| `_tactical_in_progress` import | import | 21 | External function from `engine.ml.tactical_macro`, not constructed here |
| `TacticalMacroModel` import | import | 23 | External type from `engine.ml.tactical_macro_runtime`, not constructed here |
| `TacticalPlan` import | import | 24 | External type from `engine.tactical_plans`, not constructed here |
| `TacticalPlanCursor` import | import | 24 | External type from `engine.tactical_plans`, not constructed here |
| `TacticalActivationSearchService.__deepcopy__` | method | 102 | Deepcopy method, not construction |
| `TacticalActivationSearchService.select` | method | 107 | Selection method, not construction |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 4 (1 dataclass + 1 function + 1 class + 1 function) |
| Excluded top-level definitions | 20 (docstrings + stdlib imports + type imports + methods) |
| Source lines covered | 1–234 |
| Discovery matching lines | 5 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/tactical_search.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
