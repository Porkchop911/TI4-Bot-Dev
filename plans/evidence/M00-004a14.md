# M00-004a14 — ML Catalogue Construction API Inventory

## Scope
`engine/ml/catalogue.py` (168 physical lines). Typed, closed vocabularies for learned bot features.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/catalogue.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\catalogue.py
  → 168
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\catalogue.py
  → 9 matching lines (including public/private functions, classes, decorators, and uppercase constants)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\catalogue.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `Namespace` (str-based Enum)
| Field | Value |
|---|---|
| Symbol | `Namespace` |
| Kind | `class(str, Enum)` |
| Source line | 22 |
| Construction role | Typed namespace enumeration for catalogue entries. 14 members: FACTION, TECHNOLOGY, UNIT, SYSTEM, PLANET, OBJECTIVE, STRATEGY_CARD, LEADER, BREAKTHROUGH, ACTION_CARD, RELIC, PROMISSORY, CHOICE_FAMILY, OPTION_KIND. Inherits from `str` for string comparison compatibility. |
| Rust relevance | Rust enum with `#[derive(EnumString, EnumIter)]` or equivalent; must preserve all 14 variants and their string values. Used as key type in catalogue lookup. |

### 2. `OPTION_KINDS` (option kind constant)
| Field | Value |
|---|---|
| Symbol | `OPTION_KINDS` |
| Kind | Module-level constant |
| Signature/Value | `frozenset({...})` — 91 statically named option kinds in the engine at schema version 1. Includes: ability, action, action_card, activate, agenda, annex, appoint, assimilate, borrow, breakthrough, build, capacity, capture, casualty, combat_modifier, combined_payment, commit, component, count, custodians, decline, destroy, discard, elect, explore, gain, garrison, gather, gift, ingress, law, leader, load, move, offer, open_transaction, order, outcome, pay, place, placement, planet, player, pool, prediction, prerequisite, produce, production, purge_fragment, quash, ready, ready_technology, recall, redistribute, relocate, remove, repair, repeal, replace, replenish, rescue_from, research, retreat, retreat_to, return, score, scuttle, ship, silence, skilled_retreat, speaker, specialty, spend, stop, strategy, strategy_card, supply, sustain, swap, system, tiebreak, token, transaction, transit, unit, vote, vote_planet. |
| Source line | 42 |
| Construction role | Closed set of known option kinds. A test compares this set with the source extractor used by the existing policy exporter to prevent silent unknown embeddings. Used in `catalogue()` to create OPTION_KIND and CHOICE_FAMILY namespace entries. |
| Rust relevance | Must be preserved as a static frozenset/HashSet. Critical for schema compatibility testing. |

### 3. `CatalogueEntry` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `CatalogueEntry` |
| Kind | `@dataclass(frozen=True, order=True)` |
| Signature | `CatalogueEntry(namespace: Namespace, id: str, name: str, source: str | None = None, faction: str | None = None, in_scope: bool = True, subtype: str | None = None)` |
| Source line | 63 |
| Construction role | Immutable catalogue entry value object. 3 required fields (namespace, id, name) and 4 optional fields with defaults. `__post_init__` validates: id is non-empty and un-padded; id contains no control characters (ord < 32). `key` property returns `f"{namespace.value}:{id}"`. |
| Rust relevance | Immutable struct with 7 fields (3 required, 4 optional with defaults). Construction requires validation in `__post_init__` equivalent. `order=True` enables natural comparison by (namespace, id). Critical for typed content registry. |

### 4. `Catalogue` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `Catalogue` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `Catalogue(schema_version: int, entries: tuple[CatalogueEntry, ...], _by_key: Mapping[str, CatalogueEntry])` |
| Source line | 84 |
| Construction role | Immutable catalogue container holding schema version, ordered entries tuple, and read-only key-indexed mapping. Constructed via: (a) direct instantiation; (b) `build()` classmethod which sorts entries by (namespace.value, id), validates no duplicate keys, wraps index in `MappingProxyType`. |
| Rust relevance | Immutable struct; Rust equivalent must carry schema_version (u32), entries (Vec<CatalogueEntry>), _by_key (HashMap<String, CatalogueEntry> wrapped in read-only view). Construction via `build()` requires sorting, dedup validation, and read-only wrapper. |

### 4a. `Catalogue.build()` (classmethod factory)
| Field | Value |
|---|---|
| Symbol | `Catalogue.build` |
| Kind | `@classmethod` factory |
| Signature | `build(cls, entries: Iterable[CatalogueEntry], schema_version: int = 1) -> Catalogue` |
| Source line | 89 |
| Construction role | Factory that builds a Catalogue from an iterable of entries. Sorts by (namespace.value, id), validates uniqueness of `key` property, wraps index in `MappingProxyType`. Raises `ValueError` on duplicate keys. Default schema_version is 1. |
| Rust relevance | Rust constructor `Catalogue::build(entries: impl IntoIterator<Item = CatalogueEntry>, schema_version: u32) -> Result<Self, Error>`. Must implement sorting, dedup validation, and read-only index wrapping. |

### 5. `catalogue()` (cached factory function)
| Field | Value |
|---|---|
| Symbol | `catalogue` |
| Kind | Module-level function (cached via `@lru_cache(maxsize=1)`) |
| Signature | `catalogue() -> Catalogue` |
| Source line | 155 |
| Construction role | Cached factory that builds a complete Catalogue from content sources. Iterates `_CONTENT_SPECS` (15 content spec tuples), extracts entries via `_entry()` from `content.from_sources()`, then adds OPTION_KIND and CHOICE_FAMILY entries for each of the 91 OPTION_KINDS. Returns `Catalogue.build(entries)`. Cached with `@lru_cache(maxsize=1)` for singleton-like behavior. |
| Rust relevance | Rust function `fn catalogue() -> &'static Catalogue` or `Result<Catalogue, Error>` with static caching. Must reproduce identical catalogue from identical content sources. Depends on `engine.content` module. Critical for ML feature namespace resolution. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–7 | Documentation, not code |
| `__future__` import | import | 9 | Future annotations import |
| `dataclass` import | import | 11 | Stdlib import, not engine API |
| `Enum` import | import | 12 | Stdlib import, not engine API |
| `lru_cache` import | import | 13 | Stdlib import, not engine API |
| `MappingProxyType` import | import | 14 | Stdlib import, not engine API |
| `Iterable` import | import | 15 | Stdlib import, not engine API |
| `Mapping` import | import | 15 | Stdlib import, not engine API |
| `content` import | import | 17 | External module dependency, not constructed here |
| `SAVE52_FACTIONS` | constant | 20 | Internal constant (used by _entry), not public API |
| `CatalogueEntry.__post_init__` | method | 73 | Validation method, not construction |
| `CatalogueEntry.key` | property | 79 | Property, not construction |
| `Catalogue.get()` | method | 94 | Lookup method, not construction |
| `Catalogue.contains()` | method | 101 | Lookup method, not construction |
| `Catalogue.ids()` | method | 105 | Lookup method, not construction |
| `_CONTENT_SPECS` | private constant | 113 | Private constant (leading underscore), not public API |
| `_entry` | private function | 130 | Private function (leading underscore), not public API |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 6 (1 Enum + 1 constant + 2 dataclasses + 1 classmethod factory + 1 cached factory) |
| Excluded top-level definitions | 18 (docstrings + stdlib imports + type imports + content import + private constant + private function + methods + property + public constant SAVE52_FACTIONS) |
| Source lines covered | 1–168 |
| Discovery matching lines | 9 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/catalogue.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
