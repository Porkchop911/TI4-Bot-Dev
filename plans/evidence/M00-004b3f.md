# M00-004b3f — LLM tools CLI entry points

## Package details
- **ID:** M00-004b3f
- **Title:** Inventory LLM tools CLI entry points
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004b (Module entry points and CLI tools)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Inventory every LLM tool's CLI entry point and purpose.

## Oracle read scope (5 files)
| Tool | Lines | Purpose |
|---|---|---|
| `llm_bias.py` | ~100 | LLM bias analysis |
| `llm_match.py` | ~90 | LLM model matching |
| `llm_probe.py` | ~70 | LLM model probing |
| `llm_regret.py` | ~60 | LLM regret analysis |
| `llm_series.py` | ~170 | LLM series evaluation |

## Permission class
- **P0:** Read oracle files
- **P1:** Write evidence file

## CLI pattern
All 5 tools follow the same CLI pattern:
```python
if __name__ == "__main__":
    raise SystemExit(main())

def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    # ... add LLM arguments ...
    args = parser.parse_args()
    # ... run LLM evaluation ...
    return 0
```

## Compatibility class
- **CLI interface**: `semantic` — Rust will have its own CLI.
- **LLM inference**: `semantic` — Rust policy inference will be independent.

## Non-goals
- No implementation of LLM logic; documentation-only inventory.

## Evidence
- Files read: 5 tools (~490 lines total)
- All have: argparse CLI
- Purpose categories: LLM analysis (5)

## Completion
- [x] All CLI entry points catalogued
- [x] All init signatures documented
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
