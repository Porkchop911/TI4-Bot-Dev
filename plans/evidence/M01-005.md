# M01-005 — Formatting and lint commands

## Package details
- **ID:** M01-005
- **Title:** Formatting and lint commands
- **Milestone:** M01 — Repository bootstrap
- **Package:** M01-005
- **Dependencies:** M01-004 ✅

## Permission class
- **P1:** Write evidence file

## Commands

### Formatting
```bash
# Format all crates
cargo fmt --all

# Check formatting (CI gate)
cargo fmt --all -- --check
```

### Linting
```bash
# Workspace clippy (all crates)
cargo clippy --workspace --all-targets --all-features -- -D warnings

# Per-crate clippy
cargo clippy -p ti4-model --all-targets --all-features -- -D warnings
cargo clippy -p ti4-content --all-targets --all-features -- -D warnings
cargo clippy -p ti4-engine --all-targets --all-features -- -D warnings
cargo clippy -p ti4-policy --all-targets --all-features -- -D warnings
cargo clippy -p ti4-sim --all-targets --all-features -- -D warnings
cargo clippy -p ti4-training --all-targets --all-features -- -D warnings
cargo clippy -p ti4-bridge --all-targets --all-features -- -D warnings
cargo clippy -p ti4-legacy --all-targets --all-features -- -D warnings
cargo clippy -p ti4-cli --all-targets --all-features -- -D warnings
```

### Documentation
```bash
# Generate docs
cargo doc --workspace --no-deps --document-private-items

# Check docs build without warnings
cargo doc --workspace --no-deps --document-private-items -- -D warnings
```

## M01-005 acceptance
- [x] Formatting command documented (`cargo fmt --all -- --check`)
- [x] Lint command documented (`cargo clippy --workspace --all-targets -- -D warnings`)
- [x] Documentation command documented (`cargo doc --workspace --no-deps`)

## M01-005 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
