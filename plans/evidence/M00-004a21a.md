# M00-004a21a — ML Capture Data-Type Slice Inventory

## Scope
`engine/ml/capture.py` lines 1–119. Data-type slice: schema constant, TypedReference, CapturedOption, CapturedChoice.

## Slice bounds
- Physical lines: 1–119
- Oracle commit: `37061c5` (codex/fully-learned-policy)
- Oracle path: `D:\Projects\ti4-engine\engine\ml\capture.py`

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/capture.py
  → 1 file
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\capture.py
  → 7 matching lines in lines 1–119 (1 constant + 3 @dataclass decorators + 3 classes)
```

## P0/P1 scope
- **P0**: read-only access to oracle lines 1–119
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `CAPTURE_SCHEMA_VERSION` (schema constant)
| Field | Value |
|---|---|
| Symbol | `CAPTURE_SCHEMA_VERSION` |
| Kind | Module-level constant |
| Signature/Value | `3` |
| Source line | 27 |
| Construction role | Schema version for CapturedChoice records. Used in `write_capture_shard()` header and embedded in every CapturedChoice. |
| Rust relevance | Must be preserved in Rust capture serialization; any schema change requires migration path. |

### 2. `TypedReference` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `TypedReference` |
| Kind | `@dataclass(frozen=True, order=True)` |
| Signature | `TypedReference(namespace: str, id: str)` |
| Source line | 31 |
| Construction role | Immutable value object representing a typed catalogue reference. 2 required fields: namespace (string), id (string). `order=True` enables natural comparison by (namespace, id). Constructed directly via constructor; used as element in `typed_references` tuples within CapturedOption. |
| Rust relevance | Immutable struct; Rust equivalent must carry namespace (String), id (String). Construction via named args. Used in typed reference lookups across ML modules. |

### 3. `CapturedOption` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `CapturedOption` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `CapturedOption(id: str, kind: str, label: str, payload_json: str, payload_sha256: str, typed_references: tuple[TypedReference, ...], numeric_features: tuple[tuple[str, float], ...], raw_components: tuple[tuple[str, float], ...], incumbent_components: tuple[tuple[str, float], ...], incumbent_score: float, incumbent_considered: bool)` |
| Source line | 37 |
| Construction role | Immutable value object representing a captured legal option in a choice matrix. 11 required fields: id, kind, label, payload_json (canonical JSON), payload_sha256 (SHA-256 of payload_json), typed_references (sorted tuple), numeric_features (option effect features), raw_components (raw feature breakdown), incumbent_components (incumbent feature breakdown), incumbent_score (float), incumbent_considered (boolean). Constructed directly via constructor in `build_choice_matrix()`. |
| Rust relevance | Core capture row type; Rust equivalent must carry all 11 fields identically. Construction via named args in `build_choice_matrix()`. Critical for ML training data (each option becomes a training row). |

### 4. `CapturedChoice` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `CapturedChoice` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `CapturedChoice(schema_version: int, run_id: str, decision_index: int, player: str, observation: BotObservation, observation_digest: str, choice_fingerprint: str, prompt: str, chosen_option_id: str, decision_context: TacticalDecisionContext | None, options: tuple[CapturedOption, ...])` |
| Source line | 52 |
| Construction role | Immutable value object representing a complete captured choice matrix after a scored decision. 11 required fields: schema_version, run_id, decision_index, player (actor id), observation (BotObservation), observation_digest (SHA-256 of observation), choice_fingerprint (SHA-256 of choice), prompt (decision prompt text), chosen_option_id, decision_context (optional TacticalDecisionContext), options (sorted tuple of CapturedOption). `as_dict()` method returns `asdict(self)`. `canonical_json()` method returns `json.dumps(asdict(self), sort_keys=True, separators=(",", ":"))`. Constructed directly via constructor in `build_choice_matrix()`. |
| Rust relevance | Core capture record type; Rust equivalent must carry all 11 fields identically. Construction via named args in `build_choice_matrix()`. Critical for ML training data (each decision becomes a captured choice record). |

## Exclusions table (top-level only, lines 1–119)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–7 | Documentation, not code |
| `_KIND_NAMESPACE` | private constant | 72 | Private constant (leading underscore), not public API |
| `_PAYLOAD_NAMESPACE` | private constant | 95 | Private constant (leading underscore), not public API |
| `__future__` import | import | 3 | Future annotations import |
| `hashlib` import | import | 9 | Stdlib import, not engine API |
| `json` import | import | 10 | Stdlib import, not engine API |
| `os` import | import | 11 | Stdlib import, not engine API |
| `tempfile` import | import | 12 | Stdlib import, not engine API |
| `asdict` import | import | 13 | Stdlib import, not engine API |
| `dataclass` import | import | 13 | Stdlib import, not engine API |
| `Path` import | import | 14 | Stdlib import, not engine API |
| `Callable` import | import | 15 | Stdlib import, not engine API |
| `Iterable` import | import | 15 | Stdlib import, not engine API |
| `Mapping` import | import | 15 | Stdlib import, not engine API |
| `Choice` import | import | 17 | External type dependency from `engine.choice`, not constructed here |
| `Decider` import | import | 17 | External type dependency from `engine.choice`, not constructed here |
| `Option` import | import | 17 | External type dependency from `engine.choice`, not constructed here |
| `Catalogue` import | import | 18 | External type from `engine.ml.catalogue`, not constructed here |
| `Namespace` import | import | 18 | External type from `engine.ml.catalogue`, not constructed here |
| `catalogue` import | import | 18 | External function from `engine.ml.catalogue`, not constructed here |
| `TacticalDecisionContext` import | import | 19 | External type from `engine.ml.context`, not constructed here |
| `numeric_option_features` import | import | 20 | External function from `engine.ml.features`, not constructed here |
| `BotObservation` import | import | 21 | External type from `engine.ml.observation`, not constructed here |
| `_canonical` import | import | 22 | External function from `engine.ml.rollout`, not constructed here |
| `choice_fingerprint` import | import | 22 | External function from `engine.ml.rollout`, not constructed here |
| `GameView` import | import | 23 | External type from `engine.views`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 4 (1 constant + 3 dataclasses) |
| Excluded top-level definitions | 26 (docstrings + private constants + stdlib imports + type imports) |
| Slice lines covered | 1–119 |
| Discovery matching lines | 7 |
| Unreconciled definitions | 0 |

## Deferred to later slices
- `typed_references()` function
- `_typed_references()` function
- `_payload_json()` function
- `ChoiceCaptureDecider` class
- `capture_scored_choice()` function
- `build_choice_matrix()` function
- `write_capture_shard()` function

## Status
- **Slice complete**: engine/ml/capture.py lines 1–119 data-type slice inventoried.
- **Not complete** for M00-004a21 (functions and ChoiceCaptureDecider deferred).
- **Not complete** for M00-004a.
- **Not complete** for M00-004.
