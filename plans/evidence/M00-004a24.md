# M00-004a24 — ML Feature APIs Construction API Inventory

## Scope
`engine/ml/features.py` (577 physical lines). Engine-derived numeric option effects for choice-matrix rows.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/features.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\features.py
  → 577
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\s*:|\s*=)' D:\Projects\ti4-engine\engine\ml\features.py
  → 17 matching lines (1 private constant + 15 private functions + 1 public function)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\features.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `numeric_option_features()` (numeric feature extraction function)
| Field | Value |
|---|---|
| Symbol | `numeric_option_features` |
| Kind | Module-level function |
| Signature | `numeric_option_features(observation: BotObservation, option: Option, references: tuple[TypedReference, ...], decision_context: TacticalDecisionContext | None) -> tuple[tuple[str, float], ...]` |
| Source line | 523 |
| Construction role | Extracts engine-derived numeric option effects for choice-matrix rows. Core feature function called from `build_choice_matrix()` in `capture.py`. Returns tuple of (name, float) pairs. Internally dispatches to `_tactical_consequence_features()` and `_combined_payment_features()` for option-kind-specific numeric extraction. |
| Rust relevance | Rust function `fn numeric_option_features(observation: &BotObservation, option: &Option, references: &[TypedReference], decision_context: Option<&TacticalDecisionContext>) -> Vec<(String, f64)>`. Critical numeric feature extraction for ML training; must reproduce identical numeric features for identical inputs. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `_PAYLOAD_NUMERIC_KEYS` | private constant | 16 | Private constant (leading underscore), not public API |
| `_records` | private function | 44 | Private function (leading underscore), not public API |
| `_number` | private function | 52 | Private function (leading underscore), not public API |
| `_unit_features` | private function | 60 | Private function (leading underscore), not public API |
| `_dynamic_unit_features` | private function | 87 | Private function (leading underscore), not public API |
| `_technology_features` | private function | 113 | Private function (leading underscore), not public API |
| `_dynamic_technology_features` | private function | 179 | Private function (leading underscore), not public API |
| `_planet_features` | private function | 226 | Private function (leading underscore), not public API |
| `_system_features` | private function | 238 | Private function (leading underscore), not public API |
| `_hex_distance` | private function | 257 | Private function (leading underscore), not public API |
| `_save52_coordinates` | private function | 263 | Private function (leading underscore), not public API |
| `_dynamic_system_features` | private function | 291 | Private function (leading underscore), not public API |
| `_own_ground_in_system` | private function | 403 | Private function (leading underscore), not public API |
| `_uncontested_planets` | private function | 419 | Private function (leading underscore), not public API |
| `_tactical_consequence_features` | private function | 443 | Private function (leading underscore), not public API |
| `_combined_payment_features` | private function | 501 | Private function (leading underscore), not public API |
| `__future__` import | import | 3 | Future annotations import |
| `json` import | import | 5 | Stdlib import, not engine API |
| `lru_cache` import | import | 6 | Stdlib import, not engine API |
| `Iterable` import | import | 7 | Stdlib import, not engine API |
| `content` import | import | 9 | External module from `engine`, not constructed here |
| `Option` import | import | 10 | External type dependency from `engine.choice`, not constructed here |
| `Namespace` import | import | 11 | External type from `engine.ml.catalogue`, not constructed here |
| `TacticalDecisionContext` import | import | 12 | External type from `engine.ml.context`, not constructed here |
| `BotObservation` import | import | 13 | External type from `engine.ml.observation`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (1 module-level function) |
| Excluded top-level definitions | 26 (docstrings + private constant + private functions + stdlib imports + type imports) |
| Source lines covered | 1–577 |
| Discovery matching lines | 17 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/features.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
