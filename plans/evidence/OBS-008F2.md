# Evidence — OBS-008f2, agenda vote consequence surface

See `plans/OBS-008F2_AGENDA_VOTE_CONSEQUENCE_SURFACE.md` for design; this records checks.

## Changed paths

- `crates/ti4-engine/src/preview.rs` — new `Quantity::Votes` variant.
- `crates/ti4-engine/src/vote.rs` — `VoteWindow::pending_choice`'s `Stage::Planets` branch attaches
  an exact preview to each planet-exhaust option.
- `crates/ti4-policy/src/features.rs` — `content_decision_features` gains an `option: &ChoiceOption`
  parameter and now also reads previews, mapping `Quantity::Votes` to `content:votes-*`.
- `plans/OBS-008F2_AGENDA_VOTE_CONSEQUENCE_SURFACE.md`, `plans/evidence/OBS-008F2.md`,
  `plans/EXECUTION_STATE.md`.

## Focused evidence

- `vote.rs::obs008f2_exhausting_planets_previews_the_running_vote_total`: a seat with two distinct
  influential planets sees the first exhaust option preview `0 -> influence1`; after resolving it,
  the second exhaust option previews `influence1 -> influence1 + influence2` — the running total,
  not a reset count.
- `features.rs::obs008f2_vote_exhaust_preview_reaches_the_policy`: a `vote_exhaust_planet` option
  previewing `3 -> 9` produces `content:votes-before` (3), `content:votes-after` (9),
  `content:votes-change` (6), all surviving `mlp_option_features`/`projection::admits`.

## Tests and checks

| command | result |
|---|---|
| `cargo test -p ti4-engine --lib obs008f2` | 1 passed |
| `cargo test -p ti4-engine --lib vote::` | 23 passed (22 pre-existing + 1 new) |
| `cargo test -p ti4-policy --lib obs008f2` | 1 passed |
| `cargo test -p ti4-engine` | 1,199 lib (1,198 + 1 new) + 4 integration + 5 doc passed |
| `cargo test -p ti4-policy --lib` | 227 passed (226 + 1 new, incl. the 102-game campaign, no
  regression) |
| `cargo test -p ti4-training` | 133 passed |
| `cargo clippy -p ti4-engine -p ti4-policy --all-targets -- -D warnings` | clean |
| `rustfmt --edition 2024 --check` (scoped files) + `git diff --check` | clean |

## Independent Tier-C review

OUTSTANDING (deferred per standing repo convention; reviewed elsewhere per user confirmation this
session). The other ~37 `content` subtypes remain without a preview, left for further opportunistic
passes; `OBS-008d1`'s four still-unpreviewed strategy subtypes remain open and unrelated to this
package.
