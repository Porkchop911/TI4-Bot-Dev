# M00-004a10 — Promoted Policy Construction API Inventory

## Scope
`engine/ml/promoted.py` (65 physical lines). Frozen, explicitly opt-in tactical-search promotions.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files engine/ml/promoted.py
  → 1 file
wc -l D:\Projects\ti4-engine\engine\ml\promoted.py
  → 65
rg -n '^class |^def |^__all__|^@dataclass|^[A-Z_]+(?:\\s*:|\\s*=)' D:\Projects\ti4-engine\engine\ml\promoted.py
  → 4 matching lines (1 @dataclass decorator + 1 class + 2 annotated constants)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\engine\ml\promoted.py`
- **P1**: write-only to this evidence file

## Public construction inventory

### 1. `TacticalSearchPromotion` (frozen dataclass)
| Field | Value |
|---|---|
| Symbol | `TacticalSearchPromotion` |
| Kind | `@dataclass(frozen=True)` |
| Signature | `TacticalSearchPromotion(artifact: str, model_id: str, max_candidates: int = 8, minimum_override_margin: float = 0.30)` |
| Source line | 12 |
| Construction role | Immutable promotion value object identifying a faction-specific tactical macro model override. Two required fields (`artifact`, `model_id`) and two optional override parameters with defaults. Constructed directly via constructor; also instantiated as dict values in `PROMOTED_TACTICAL_SEARCH`. |
| Rust relevance | Immutable struct; Rust equivalent must carry artifact (String), model_id (String), max_candidates (u32, default 8), minimum_override_margin (f64, default 0.30). Construction via named args or builder. |

### 2. `PROMOTED_TACTICAL_SEARCH` (faction→promotion mapping constant)
| Field | Value |
|---|---|
| Symbol | `PROMOTED_TACTICAL_SEARCH` |
| Kind | Module-level constant (Mapping) |
| Signature/Value | `Mapping[str, TacticalSearchPromotion]` — dict with 3 entries: `"hacan"`, `"letnev"`, `"xxcha"` → `TacticalSearchPromotion(artifact=..., model_id=...)` |
| Source line | 19 |
| Construction role | Registry of promoted tactical search factions and their corresponding model artifacts. Only these three factions have promoted tactical activation search. Used by `install_promoted_tactical_search()` to look up faction promotions. |
| Rust relevance | Must be preserved as a static mapping; Rust equivalent is a `static` or `const` HashMap. Critical for faction-specific policy promotion routing. |

### 3. `install_promoted_tactical_search()` (promotion installer function)
| Field | Value |
|---|---|
| Symbol | `install_promoted_tactical_search` |
| Kind | Module-level function |
| Signature | `install_promoted_tactical_search(game, seated: Mapping[str, object], *, config_directory: Path | None = None) -> dict[str, str]` |
| Source line | 35 |
| Construction role | Installs promoted tactical activation search for seated players whose faction has a promotion. For each seated player: (1) looks up faction in `PROMOTED_TACTICAL_SEARCH`; (2) if found, replaces `bot.policy` with `tactical_activation_search=True` via `dataclasses.replace`; (3) calls `bot.enable_tactical_activation_search(game, directory/artifact, max_candidates, min_override_margin)` to load the model; (4) validates returned model_id matches expected; (5) returns `{faction: model_id}` for installed factions. Raises `ValueError` if loaded model_id mismatches. |
| Rust relevance | Rust function `fn install_promoted_tactical_search(game: &Game, seated: &..., config_directory: Option<&Path>) -> Result<HashMap<String, String>, Error>`. Must implement identical promotion lookup, model loading, and ID validation pipeline. |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1 | Documentation, not code |
| `__future__` import | import | 3 | Future annotations import |
| `dataclasses` import | import | 5 | Stdlib import, not engine API |
| `dataclass` import | import | 6 | Stdlib import, not engine API |
| `Path` import | import | 7 | Stdlib import, not engine API |
| `Mapping` import | import | 8 | Stdlib import, not engine API |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 3 (1 dataclass + 1 constant mapping + 1 installer function) |
| Excluded top-level definitions | 6 (docstrings + stdlib imports) |
| Source lines covered | 1–65 |
| Discovery matching lines | 4 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: engine/ml/promoted.py public construction API inventoried.
- **Not complete** for M00-004a (other modules remain).
- **Not complete** for M00-004.
