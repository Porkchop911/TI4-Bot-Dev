# M00-004b2 — Benchmark/Bootstrap Tool Entry Point Inventory

## Scope
`tools/benchmark_engine.py` (240 lines), `tools/benchmark_m3_devices.py` (119 lines), `tools/bootstrap_ml_cuda.ps1` (14 lines). Benchmarking infrastructure and CUDA bootstrap.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/benchmark_engine.py tools/benchmark_m3_devices.py tools/bootstrap_ml_cuda.ps1
  → 3 files
wc -l D:\Projects\ti4-engine\tools\benchmark_engine.py
  → 240
wc -l D:\Projects\ti4-engine\tools\benchmark_m3_devices.py
  → 119
wc -l D:\Projects\ti4-engine\tools\bootstrap_ml_cuda.ps1
  → 14
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\benchmark_engine.py
  → 14 matching lines (1 class + 6 functions + __main__ block)
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\benchmark_m3_devices.py
  → 4 matching lines (1 class-like function + 1 main function + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to oracle paths above
- **P1**: write-only to this evidence file

---

## 1. `tools/benchmark_engine.py` — Measured constants regeneration tool

### Entry points
| Symbol | Kind | Source line | Construction role |
|---|---|---|---|
| `main()` | public function | 226 | CLI entry point. Parses args via `parse_args()`, runs `throughput()`, `single_game_and_decisions()`, `fork_and_rollout()`, `vp_dispersion()`, writes JSON report to `out/engine_benchmark.json`, prints summary. |
| `parse_args()` | public function | 214 | Returns `argparse.Namespace` with flags: `--workers` (int, default `min(16, os.cpu_count())`), `--games` (int, default 300), `--seeds` (int, default 20), `--samples` (int, default 30), `--horizon` (int, default 4), `--out` (Path, default `ROOT/out/engine_benchmark.json`). |
| `if __name__ == "__main__"` | script entry | 240 | Invokes `main()`. |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--workers` | int | `min(16, os.cpu_count())` | Worker count for throughput benchmark |
| `--games` | int | 300 | Throughput sample size |
| `--seeds` | int | 20 | VP dispersion sample size |
| `--samples` | int | 30 | Fork/rollout sample count |
| `--horizon` | int | 4 | Game horizon in rounds |
| `--out` | Path | `ROOT/out/engine_benchmark.json` | Output report path |

### Exclusions (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–15 | Documentation, not code |
| `ROOT` | module constant | 23 | Module-level path constant (sys.path manipulation) |
| `_one_game` | private function | 25 | Private function (leading underscore) |
| `throughput` | public function | 38 | Public function but not a CLI entry point; benchmark helper |
| `single_game_and_decisions` | public function | 53 | Public function but not a CLI entry point; benchmark helper |
| `_ForceFirst` | private class | 65 | Private class (leading underscore) |
| `fork_and_rollout` | public function | 83 | Public function but not a CLI entry point; benchmark helper |
| `vp_dispersion` | public function | 118 | Public function but not a CLI entry point; benchmark helper |
| `__future__` import | import | 3 | Future annotations import |
| `argparse` import | import | 5 | Stdlib import |
| `concurrent.futures` import | import | 6 | Stdlib import |
| `copy` import | import | 7 | Stdlib import |
| `json` import | import | 8 | Stdlib import |
| `os` import | import | 9 | Stdlib import |
| `statistics` import | import | 10 | Stdlib import |
| `sys` import | import | 11 | Stdlib import |
| `time` import | import | 12 | Stdlib import |
| `Path` import | import | 13 | Stdlib import |

---

## 2. `tools/benchmark_m3_devices.py` — M3 tensor-training throughput benchmark

### Entry points
| Symbol | Kind | Source line | Construction role |
|---|---|---|---|
| `main(argv)` | public function | 65 | CLI entry point with optional argv. Parses args via `argparse`, detects CPU/CUDA devices, runs `benchmark()` on each device, prints JSON report to stdout. Returns exit code 0. |
| `if __name__ == "__main__"` | script entry | 119 | Invokes `main()` via `raise SystemExit(main())`. |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--choices` | int | 256 | Number of choices |
| `--options` | int | 16 | Options per choice |
| `--features` | int | 1266 | Feature dimension |
| `--hidden` | int | 64 | Hidden dimension |
| `--iterations` | int | 20 | Training iterations |

### Exclusions (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–2 | Documentation, not code |
| `ROOT` | module constant | 16 | Module-level path constant (sys.path manipulation) |
| `benchmark` | public function | 23 | Public function but not a CLI entry point; benchmark helper |
| `__future__` import | import | 3 | Future annotations import |
| `argparse` import | import | 5 | Stdlib import |
| `json` import | import | 6 | Stdlib import |
| `sys` import | import | 7 | Stdlib import |
| `time` import | import | 8 | Stdlib import |
| `Path` import | import | 9 | Stdlib import |
| `torch` import | import | 11 | External dependency, not engine API |
| `DeepSetOptionScorer` import | import | 13 | External function from `tools.deepsets_choice`, not constructed here |
| `choice_loss` import | import | 13 | External function from `tools.deepsets_choice`, not constructed here |

---

## 3. `tools/bootstrap_ml_cuda.ps1` — CUDA environment bootstrap script

### Entry points
| Symbol | Kind | Source line | Construction role |
|---|---|---|---|
| Script entry | PowerShell script | 1–14 | Standalone PowerShell bootstrap. Creates `.venv-ml` virtualenv with Python 3.12 via `uv venv`, installs `requirements-ml.txt`, `requirements-ml-cu126.txt`, `requirements-analysis.txt` via `uv pip install`, then runs `verify_ml_cuda.py`. |
| `param()` | PowerShell parameter | 1 | `$Environment` (default `".venv-ml"`) — virtualenv directory name |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `-Environment` | string | `".venv-ml"` | Virtualenv directory name |

### Exclusions (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| `$ErrorActionPreference` | PowerShell variable | 3 | Error handling setting |
| `$Repository` | PowerShell variable | 4 | Repository path resolution |
| `$EnvironmentPath` | PowerShell variable | 5 | Virtualenv path |
| `$Python` | PowerShell variable | 6 | Python executable path |
| `uv venv` command | command | 8 | Virtualenv creation |
| `uv pip install` commands | commands | 9–11 | Package installation |
| `& $Python` command | command | 12 | CUDA verification execution |

---

## Summary
| File | Lines | Entry points | CLI flags |
|---|---|---|---|
| `tools/benchmark_engine.py` | 240 | `main()`, `parse_args()` | 6 flags (`--workers`, `--games`, `--seeds`, `--samples`, `--horizon`, `--out`) |
| `tools/benchmark_m3_devices.py` | 119 | `main(argv)` | 5 flags (`--choices`, `--options`, `--features`, `--hidden`, `--iterations`) |
| `tools/bootstrap_ml_cuda.ps1` | 14 | Script entry | 1 param (`-Environment`) |

## Status
- **Slice complete**: benchmark/bootstrap tool entry points inventoried.
- **Not complete** for M00-004b.
