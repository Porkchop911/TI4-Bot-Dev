# M00-004b8 — DeepSets/Sweep Tool Entry Point Inventory

## Scope
Three deepsets/sweep tools: `tools/deepsets_choice.py` (85 lines), `tools/dial_sweep.py` (100 lines), `tools/sweep_m3_deepsets.py` (145 lines).

---

## 1. tools/deepsets_choice.py (85 lines)

### Entry points
| Symbol | Kind | Line | Role |
|---|---|---|---|
| `DeepSetOptionScorer` | class | 10 | PyTorch nn.Module: permutation-equivariant option scorer with incumbent baseline. |
| `choice_loss()` | public function | 47 | Composite rollout-label loss: pairwise + value + KL terms with inverse-propensity weighting. |
| `__main__` guard | **absent** | — | No `if __name__ == "__main__"` block. |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| (none) | — | — | No CLI entry points |

### Exclusions (top-level)
| Symbol | Kind | Line | Reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `__future__` import | import | 3 | Future annotations |
| `torch` import | import | 5 | External dependency |
| `nn` import | import | 6 | External dependency |
| `functional as F` import | import | 7 | External dependency |

---

## 2. tools/dial_sweep.py (100 lines)

### Entry points
| Symbol | Kind | Line | Role |
|---|---|---|---|
| `main()` | function | 48 | CLI entry: runs untouched baseline, sweeps each dial at FLOOR/CEILING, prints VP gap with significance test, warns if no dial moves score past noise. |
| `parse_args()` | inline in main | 50 | Argparse parser with 3 flags. |
| `__main__` guard | script | 100 | `raise SystemExit(main())` |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--games` | int | 20 | Number of games per dial |
| `--seat` | str | `"sol"` | Player seat to test |
| `--dial` | append | [] | Repeatable; default is every dial in DIALS |

### Exclusions (top-level)
| Symbol | Kind | Line | Reason |
|---|---|---|---|
| Module docstring | docstring | 1–22 | Documentation |
| `FLOOR` | module constant | 32 | Clamp floor (0.3) |
| `CEILING` | module constant | 32 | Clamp ceiling (2.5) |
| `play` | public function | 34 | Game runner with optional dial pinning |
| `__future__` import | import | 24 | Future annotations |
| `argparse` import | import | 26 | Stdlib |
| `statistics` import | import | 27 | Stdlib |
| `sys` import | import | 28 | Stdlib |
| `Path` import | import | 29 | Stdlib |
| `bots` import | import | 31 | External module |
| `sim` import | import | 31 | External module |
| `DIALS` import | import | 32 | External constant from engine.llm.tuner |

---

## 3. tools/sweep_m3_deepsets.py (145 lines)

### Entry points
| Symbol | Kind | Line | Role |
|---|---|---|---|
| `main(argv)` | function | 41 | CLI entry: loads dataset JSONs, validates experiment spec consistency, splits into grouped folds, trains DeepSets per hidden_size×fold, reports cross-validated metrics, selects best hidden_size by soft-pair log loss, writes atomic JSON report. |
| `parse_args()` | inline in main | 43 | Argparse parser with 8 flags. |
| `__main__` guard | script | 145 | `raise SystemExit(main())` |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--dataset` | Path (append) | `ROOT/out/ml_tactical_macro_train_960x2.json` | Training shards (repeatable) |
| `--output` | Path | `ROOT/out/m3_deepsets_width_sweep.json` | Output report path |
| `--hidden-sizes` | int (nargs+) | (8, 16, 32, 64) | Hidden layer sizes to sweep |
| `--folds` | int | 5 | Cross-validation fold count |
| `--epochs` | int | 300 | Training epochs per fold |
| `--batch-size` | int | 64 | Training batch size |
| `--seed` | int | 52 | Random seed |
| `--device` | str | `"auto"` | Device (choices: auto, cpu, cuda) |
| `--ridge-baseline` | flag | False | Use RidgeChoiceScorer baseline |

### Exclusions (top-level)
| Symbol | Kind | Line | Reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `ROOT` | module constant | 13 | sys.path constant |
| `DEFAULT_DATASET` | module constant | 16 | Default dataset path |
| `DEFAULT_OUTPUT` | module constant | 17 | Default output path |
| `_folds` | private function | 20 | Fold splitter (leading underscore) |
| `__future__` import | import | 3 | Future annotations |
| `argparse` import | import | 5 | Stdlib |
| `json` import | import | 6 | Stdlib |
| `sys` import | import | 7 | Stdlib |
| `time` import | import | 8 | Stdlib |
| `Path` import | import | 9 | Stdlib |
| `numpy` import | import | 11 | External dependency |
| `torch` import | import | 12 | External dependency |
| `SparseFeatureEncoder` import | import | 14 | External class from tools |
| `tactical_macro_choice_sets` import | import | 14 | External function from tools |
| `write_atomic_json` import | import | 17 | External function from tools |
| `RidgeChoiceScorer` import | import | 18 | External class from tools |
| `with_ridge_baseline` import | import | 18 | External function from tools |
| `_metrics` import | import | 19 | External function from tools |
| `_predict` import | import | 19 | External function from tools |
| `train` import | import | 19 | External function from tools |

---

## Summary

| Tool | Lines | Entry points | Flags | Exclusions |
|---|---|---|---|---|
| `deepsets_choice.py` | 85 | 1 (no CLI entry) | 0 | 5 |
| `dial_sweep.py` | 100 | 2 (+ __main__) | 3 | 12 |
| `sweep_m3_deepsets.py` | 145 | 2 (+ __main__) | 9 | 20 |
| **Total** | **330** | **5** | **12** | **37** |

## Status
- M00-004b8 evidence complete.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b9 (if applicable).
