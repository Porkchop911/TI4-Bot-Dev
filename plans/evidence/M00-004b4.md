# M00-004b4 — Audit Tool Entry Point Inventory

## Scope
Three audit tools: `tools/audit_oracle_headroom.py` (107 lines), `tools/audit_scoring_decision_space.py` (160 lines), `tools/trace_equality.py` (166 lines).

---

## 1. tools/audit_oracle_headroom.py (107 lines)

### Entry points
| Symbol | Kind | Line | Role |
|---|---|---|---|
| `main()` | function | 72 | CLI entry: loads dataset JSON, calls `audit()`, prints incumbent best-arm rate / naive regret / cross-fit recoverable VP / pair label reliability / true signal SD, prints incumbent choice kinds and pair contrast counts, optionally writes JSON report. |
| `parse_args()` | function | 55 | Argparse parser with 6 flags. |
| `__main__` guard | script | 107 | `raise SystemExit(main())` |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--dataset` | Path | **required** | Dataset JSON file |
| `--arm-cap` | int | None | Sample down to this many arms |
| `--per-game` | float | None | Scale headroom to per-game figure |
| `--exclude-kind` | append | [] | Drop arms of this option kind (repeatable) |
| `--seed` | int | 52 | Random seed |
| `--out` | Path | None | Optional output report path |

### Exclusions (top-level)
| Symbol | Kind | Line | Reason |
|---|---|---|---|
| Module docstring | docstring | 1–33 | Documentation |
| `ROOT` | constant | 37 | sys.path constant |
| `_halves` | private function | 39 | Private (leading underscore) |
| `audit` | public function | 45 | Census helper, not CLI entry |
| `__future__` import | import | 35 | Future annotations |
| `argparse` import | import | 36 | Stdlib |
| `itertools` import | import | 37 | Stdlib |
| `json` import | import | 38 | Stdlib |
| `random` import | import | 39 | Stdlib |
| `statistics` import | import | 40 | Stdlib |
| `sys` import | import | 41 | Stdlib |
| `Counter` import | import | 42 | Stdlib |
| `Path` import | import | 43 | Stdlib |

---

## 2. tools/audit_scoring_decision_space.py (160 lines)

### Entry points
| Symbol | Kind | Line | Role |
|---|---|---|---|
| `main()` | function | 106 | CLI entry: generates game tasks, runs parallel `_play()`, counts decision kinds, prints gap list (families deciding VP with no model), prints modelled families, writes JSON report. |
| `parse_args()` | function | 82 | Argparse parser with 5 flags. |
| `__main__` guard | script | 160 | `raise SystemExit(main())` |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--seeds` | int | 6 | Number of seeds |
| `--first-seed` | int | 32_200_000 | Starting seed |
| `--workers` | int | `min(16, os.cpu_count())` | Worker count |
| `--horizon` | int | 4 | Game horizon |
| `--out` | Path | `ROOT/out/scoring_decision_space_audit.json` | Output report path |

### Exclusions (top-level)
| Symbol | Kind | Line | Reason |
|---|---|---|---|
| Module docstring | docstring | 1–25 | Documentation |
| `ROOT` | constant | 31 | sys.path constant |
| `MODELLED_FAMILIES` | constant | 34 | Hand-maintained model coverage map |
| `SCREENED_FAMILIES` | constant | 41 | Hand-maintained screened families map |
| `VP_CHANNEL` | constant | 47 | VP channel map |
| `_play` | private function | 55 | Private (leading underscore) |
| `status_of` | public function | 62 | Status lookup helper, not CLI entry |
| `__future__` import | import | 27 | Future annotations |
| `argparse` import | import | 29 | Stdlib |
| `concurrent.futures` import | import | 30 | Stdlib |
| `json` import | import | 31 | Stdlib |
| `os` import | import | 32 | Stdlib |
| `sys` import | import | 33 | Stdlib |
| `time` import | import | 34 | Stdlib |
| `Counter` import | import | 35 | Stdlib |
| `Path` import | import | 36 | Stdlib |
| `bots` import | import | 38 | External module |
| `sim` import | import | 38 | External module |

---

## 3. tools/trace_equality.py (166 lines)

### Entry points
| Symbol | Kind | Line | Role |
|---|---|---|---|
| `main(argv)` | function | 119 | CLI entry: loads champions JSON if `--champions`, calls `collect()`, prints trace count, optionally saves baseline JSON or checks against existing baseline, returns 0 (PASS) or 1 (FAIL). |
| `parse_args()` | inline in main | 121 | Argparse parser with 8 flags, uses `RawDescriptionHelpFormatter`. |
| `__main__` guard | script | 166 | `raise SystemExit(main())` |

### CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--champions` | Path | None | Champions table JSON (stock bots if omitted) |
| `--seeds` | int | 8 | Number of seeds |
| `--workers` | int | 30 | Worker count |
| `--first-seed` | int | 88_000_000 | Starting seed |
| `--rounds` | int (nargs+) | [1, 4] | Round horizons |
| `--boards` | str (nargs+) | ["six", "three"] | Board types (choices: six, three) |
| `--save` | Path | None | Save baseline traces |
| `--check` | Path | None | Check traces against baseline |

### Exclusions (top-level)
| Symbol | Kind | Line | Reason |
|---|---|---|---|
| Module docstring | docstring | 1–57 | Documentation |
| `ROOT` | constant | 59 | sys.path constant |
| `_PROFILES` | constant | 63 | Global state for ProcessPoolExecutor initializer |
| `_start` | private function | 65 | Private (leading underscore); initializer for parallel workers |
| `_trace` | private function | 69 | Private (leading underscore); per-task trace computation |
| `collect` | public function | 95 | Trace collection helper, not CLI entry |
| `__future__` import | import | 58 | Future annotations |
| `argparse` import | import | 60 | Stdlib |
| `concurrent.futures` import | import | 61 | Stdlib |
| `hashlib` import | import | 62 | Stdlib |
| `json` import | import | 63 | Stdlib |
| `sys` import | import | 64 | Stdlib |
| `Path` import | import | 65 | Stdlib |
| `Iterable` import | import | 66 | Stdlib |
| `bots` import | import | 68 | External module |
| `sim` import | import | 68 | External module |
| `apply_profile` import | import | 69 | External function from tools |

---

## Summary

| Tool | Lines | Entry points | Flags | Exclusions |
|---|---|---|---|---|
| `audit_oracle_headroom.py` | 107 | 2 (+ __main__) | 6 | 13 |
| `audit_scoring_decision_space.py` | 160 | 2 (+ __main__) | 5 | 17 |
| `trace_equality.py` | 166 | 2 (+ __main__) | 8 | 17 |
| **Total** | **433** | **6** | **19** | **47** |

## Status
- M00-004b4 evidence complete.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b5 (if applicable).
