# M00-004b9e — tools/evaluate_letnev_guide.py Entry Point Inventory

## Scope
`tools/evaluate_letnev_guide.py` (135 lines). Paired Save 52 evaluation of guided versus unguided Letnev.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/evaluate_letnev_guide.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\evaluate_letnev_guide.py
  → 135
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\evaluate_letnev_guide.py
  → 5 matching lines (1 private function + 1 private function + 1 private function + 1 private function + 1 public function + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\evaluate_letnev_guide.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main()` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main() -> int` |
| Kind | Module-level function |
| Source line | 118 |
| Construction role | Parses args via `argparse.ArgumentParser`, calls `_panel()` to run guided vs unguided paired evaluation over seeds×rotations via `ProcessPoolExecutor`, writes full result JSON to `--output`, prints printable subset (excluding pairs), returns 0. |

### 2. `parse_args()` | inline in main | Source line | 119 |
|---|---|---|---|
| Construction role | Argparse parser with 5 flags. |

### 3. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 135 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--seeds` | int | 20 | Number of seeds |
| `--first-seed` | int | 930_000 | Starting seed |
| `--horizon` | int | 20 | Game horizon in rounds |
| `--workers` | int | `min(16, os.cpu_count())` | Worker count |
| `--output` | Path | **required** | Output report path |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `ROOT` | module constant | 14 | Module-level path constant (sys.path manipulation) |
| `_one` | private function | 20 | Private (leading underscore) |
| `_pair` | private function | 53 | Private (leading underscore) |
| `_summary` | private function | 59 | Private (leading underscore) |
| `_panel` | private function | 84 | Private (leading underscore) |
| `__future__` import | import | 3 | Future annotations import |
| `argparse` import | import | 5 | Stdlib import |
| `collections` import | import | 6 | Stdlib import |
| `concurrent.futures` import | import | 7 | Stdlib import |
| `json` import | import | 8 | Stdlib import |
| `os` import | import | 9 | Stdlib import |
| `statistics` import | import | 10 | Stdlib import |
| `sys` import | import | 11 | Stdlib import |
| `replace` import | import | 12 | Stdlib import |
| `Path` import | import | 13 | Stdlib import |
| `bots` import | import | 16 | External module from `engine`, not constructed here |
| `objectives` import | import | 16 | External module from `engine`, not constructed here |
| `secrets` import | import | 16 | External module from `engine`, not constructed here |
| `sim` import | import | 16 | External module from `engine`, not constructed here |
| `technology` import | import | 16 | External module from `engine`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (1 entry point function) |
| Excluded top-level definitions | 21 (docstrings + module constant + private functions + stdlib imports + type imports) |
| Source lines covered | 1–135 |
| Discovery matching lines | 5 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/evaluate_letnev_guide.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b9.
