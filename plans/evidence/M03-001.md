# M03-001 — Option and Choice

## Package details
- **ID:** M03-001
- **Title:** Option and Choice
- **Milestone:** M03 — Choice, timing, and replay
- **Package:** M03-001
- **Dependencies:** M02-001 (Identifier newtypes) ✅

## Permission class
- **P1:** Write evidence file

## Option and Choice (from `engine/choice.py`)

### Option (frozen dataclass)
| Field | Python type | Rust type | Notes |
|---|---|---|---|
| `id` | `str` | `String` | Stable for given game state |
| `kind` | `str` | `String` | "ability", "decline", etc. |
| `label` | `str` | `String` | Display label (defaults to id) |
| `payload` | `dict` | `HashMap<String, serde_json::Value>` | Extra context (compare=False) |

### Option constants
- `DECLINE = Option(id="decline", kind="decline", label="Decline")` — always available when legal

### Choice (frozen dataclass)
| Field | Python type | Rust type | Notes |
|---|---|---|---|
| `player` | `str` | `PlayerId` | Actor for this choice |
| `prompt` | `str` | `String` | Description of the decision |
| `options` | `tuple[Option, ...]` | `Vec<Option>` | Legal options (ordered) |

### Choice methods
| Method | Python signature | Rust signature | Purpose |
|---|---|---|---|
| `option` | `(self, id) -> Option \| None` | `fn(&self, id) -> Option<&Option>` | Lookup by id |
| `ids` | `property -> tuple[str, ...]` | `fn(&self) -> &[String]` | All option ids |

### Option builders
| Function | Python signature | Rust signature | Purpose |
|---|---|---|---|
| `options_from` | `((id, label), kind) -> tuple[Option, ...]` | `fn((id, label), kind) -> Vec<Option>` | Build from pairs |
| `distinct_units` | `(units) -> dict[(type, damaged), int]` | `fn(units) -> HashMap<(UnitTypeId, bool), usize>` | Deduplicate interchangeable units |
| `unit_label` | `(verb, type_id, damaged) -> str` | `fn(verb, type_id, damaged) -> String` | "destroy dreadnought (damaged)" |
| `first_of_each` | `(items, key) -> dict` | `fn(items, key) -> HashMap<K, usize>` | First index of each distinct item |

### Golden tests
- Option id stability: same state → same option ids
- Choice option lookup: `choice.option(id) == option` for all offered options
- Decline option: `DECLINE.id == "decline"`, `DECLINE.kind == "decline"`
- distinct_units: identical units (same type, same damage) produce single option
- unit_label: damaged units labeled "(damaged)"

## M03-001 acceptance
- [x] Option documented (4 fields, 1 constant)
- [x] Choice documented (3 fields, 2 methods)
- [x] Option builders documented (4 functions)
- [x] Golden tests documented (5 tests)

## M03-001 status: COMPLETE

---

*Oracle commit: 37061c511a4780d4c0719e0342533a498cd4b457*
