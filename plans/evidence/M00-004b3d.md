# M00-004b3d — tools/measure_tactical_waste.py Entry Point Inventory

## Scope
`tools/measure_tactical_waste.py` (199 lines). Tactical action waste rate diagnostic.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/measure_tactical_waste.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\measure_tactical_waste.py
  → 199
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\measure_tactical_waste.py
  → 5 matching lines (1 private function + 2 public functions + 2 module constants + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\measure_tactical_waste.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main()` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main()` |
| Kind | Module-level function |
| Source line | 157 |
| Construction role | Parses args via `parse_args()`, generates game tasks over seeds×rotations, runs parallel via `ProcessPoolExecutor` calling `_play()`, aggregates waste tallies across all games, prints total actions/no-planet rate, breakdown by reason with "unambiguously wrong" markers, prints internally contradictory subset stats, writes JSON report to `--out`. |

### 2. `parse_args()` — CLI argument parser
| Field | Value |
|---|---|
| Symbol | `parse_args()` |
| Kind | Module-level function |
| Source line | 134 |
| Construction role | Returns `argparse.ArgumentParser` with `description=__doc__`. Flags: `--seeds` (int, default 40), `--first-seed` (int, default 24_000_000), `--workers` (int, default `min(16, os.cpu_count())`), `--horizon` (int, default 4), `--round` (int, default 1, help "restrict to one round; 0 counts every round in the horizon"), `--liftable-ground` (store_true, help "give every seat the liftable-ground policy flag"), `--out` (Path, default `ROOT/out/tactical_waste.json`). |

### 3. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 199 |
| Construction role | Invokes `main()` directly (no `SystemExit` wrapper). |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--seeds` | int | 40 | Number of seeds to simulate |
| `--first-seed` | int | 24_000_000 | Starting seed |
| `--workers` | int | `min(16, os.cpu_count())` | Parallel worker count |
| `--horizon` | int | 4 | Game horizon in rounds |
| `--round` | int | 1 | Restrict to one round (0 = every round) |
| `--liftable-ground` | flag | False | Enable liftable-ground policy flag |
| `--out` | Path | `ROOT/out/tactical_waste.json` | Output report path |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–16 | Documentation, not code |
| `ROOT` | module constant | 19 | Module-level path constant (sys.path manipulation) |
| `_play` | private function | 33 | Private function (leading underscore) |
| `REASONS` | module constant | 125 | Tuple of 6 waste reason categories |
| `UNAMBIGUOUS` | module constant | 132 | Tuple of 2 unambiguously wrong waste reasons |
| `__future__` import | import | 17 | Future annotations import |
| `argparse` import | import | 19 | Stdlib import |
| `collections` import | import | 20 | Stdlib import |
| `concurrent.futures` import | import | 21 | Stdlib import |
| `json` import | import | 22 | Stdlib import |
| `os` import | import | 23 | Stdlib import |
| `sys` import | import | 24 | Stdlib import |
| `Path` import | import | 25 | Stdlib import |
| `bots` import | import | 36 | External module from `engine`, not constructed here |
| `sim` import | import | 36 | External module from `engine`, not constructed here |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 2 (1 entry point function + 1 parser function) |
| Excluded top-level definitions | 15 (docstrings + module constants + private function + stdlib imports + type imports) |
| Source lines covered | 1–199 |
| Discovery matching lines | 5 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/measure_tactical_waste.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b3.
