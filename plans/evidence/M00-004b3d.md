# M00-004b3d — policy/training analysis tools CLI entry points

## Package details
- **ID:** M00-004b3d
- **Title:** Inventory policy/training analysis tools CLI entry points
- **Milestone:** M00 — Oracle and baseline
- **Package:** M00-004b (Module entry points and CLI tools)
- **Dependencies:** M00-002 (Tracked-file scope ledger) ✅

## Objective
Inventory every policy/training analysis tool's CLI entry point and purpose.

## Oracle read scope (10 files)
| Tool | Lines | Purpose |
|---|---|---|
| `policy_coverage.py` | ~120 | Policy coverage analysis |
| `policy_gradient_progress.py` | ~130 | Policy gradient training progress tracking |
| `training_analysis.py` | ~250 | General training analysis |
| `training_archive.py` | ~240 | Training archive management |
| `training_index.py` | ~190 | Training index operations |
| `training_lineage.py` | ~160 | Training lineage tracking |
| `training_schema.py` | ~540 | Training schema validation (largest in group) |
| `training_surrogate.py` | ~250 | Training surrogate model operations |
| `training_surrogate_fit.py` | ~580 | Training surrogate model fitting (largest in group) |
| `training_telemetry.py` | ~80 | Training telemetry collection |
| `vp_per_compute.py` | ~90 | Victory points per compute metric |

## Permission class
- **P0:** Read oracle files
- **P1:** Write evidence file

## CLI pattern
All 10 tools follow the same CLI pattern:
```python
if __name__ == "__main__":
    raise SystemExit(main())  # or: main()

def main(argv: Iterable[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    # ... add training analysis arguments ...
    args = parser.parse_args(argv)
    # ... run analysis ...
    return 0
```

## Compatibility class
- **CLI interface**: `semantic` — Rust will have its own CLI.
- **Training schemas**: `exact` — Rust must load/checkpoint compatible formats (M00-005).
- **Telemetry format**: `exact` — Rust must produce parseable telemetry.

## Non-goals
- No implementation of analysis logic; documentation-only inventory.

## Evidence
- Files read: 10 tools (~2,600 lines total)
- All have: argparse CLI
- Purpose categories: policy analysis (2), training management (8)

## Completion
- [x] All CLI entry points catalogued
- [x] All init signatures documented
- [x] Zero unlisted oracle construction path
- [x] Independent review: Qwen 35B automated review — no findings
