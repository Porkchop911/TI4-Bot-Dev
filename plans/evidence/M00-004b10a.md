# M00-004b10a — tools/evaluate_ml_audit.py Entry Point Inventory

## Scope
`tools/evaluate_ml_audit.py` (220 lines). Evaluate predeclared learned scorers on untouched uniform audit panel.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/evaluate_ml_audit.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\evaluate_ml_audit.py
  → 220
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\evaluate_ml_audit.py
  → 11 matching lines (1 module constant tuple + 1 private function + 1 private function + 1 private function + 1 private function + 1 private function + 1 private function + 1 private function + 1 private function + 1 public function + __main__ block)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\evaluate_ml_audit.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main(argv)` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main(argv: list[str] | None) -> int` |
| Kind | Module-level function |
| Source line | 140 |
| Construction role | Parses args via `argparse.ArgumentParser`, loads train/audit JSONs, validates audit seed block, calls `pair_rows()` for train/audit, evaluates M1 configs (family_heuristic/compact_contextual/full_bilinear) via `_m1_predictions()`, evaluates M2 via `_m2_predictions()`, evaluates hybrid via `_fit_hybrid()` + `_hybrid()`, builds report with `_metrics()`, `_cluster_bootstrap()`, `_diagnostics()`, `_label_split_half()`, writes atomic JSON report to `--output`, prints label split-half + heuristic + per-model metrics/bootstrap. Returns 0. |

### 2. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 220 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--train` | Path | `DEFAULT_TRAIN` | Training panel JSON |
| `--audit` | Path | `DEFAULT_AUDIT` | Audit panel JSON |
| `--output` | Path | `DEFAULT_OUTPUT` | Output report path |

## Module-level constants
| Symbol | Value | Line |
|---|---|---|
| `DEFAULT_TRAIN` | `ROOT/out/ml_effects_240x4.json` | 30 |
| `DEFAULT_AUDIT` | `ROOT/out/ml_audit_uniform_240x8.json` | 31 |
| `DEFAULT_OUTPUT` | `ROOT/out/ml_audit_report.json` | 32 |
| `M1_CONFIGS` | tuple of (str, float) pairs | 35 |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–1 | Documentation |
| `ROOT` | module constant | 24 | Module-level path constant (sys.path manipulation) |
| `_matrix` | private function | 40 | Private (leading underscore) |
| `_m1_predictions` | private function | 49 | Private (leading underscore) |
| `_m2_predictions` | private function | 62 | Private (leading underscore) |
| `_cluster_bootstrap` | private function | 77 | Private (leading underscore) |
| `_label_split_half` | private function | 103 | Private (leading underscore) |
| `_strata` | private function | 130 | Private (leading underscore) |
| `_diagnostics` | private function | 144 | Private (leading underscore) |
| `__future__` import | import | 3 | Future annotations import |
| `argparse` import | import | 5 | Stdlib import |
| `itertools` import | import | 6 | Stdlib import |
| `json` import | import | 7 | Stdlib import |
| `sys` import | import | 8 | Stdlib import |
| `defaultdict` import | import | 9 | Stdlib import |
| `Path` import | import | 10 | Stdlib import |
| `numpy` import | import | 12 | External dependency |
| `pearsonr` import | import | 13 | External dependency |
| `spearmanr` import | import | 13 | External dependency |
| `ExtraTreesRegressor` import | import | 14 | External dependency |
| `DictVectorizer` import | import | 15 | External dependency |
| `Ridge` import | import | 16 | External dependency |
| `MaxAbsScaler` import | import | 17 | External dependency |
| `write_atomic_json` import | import | 21 | External function from tools.ml_manifest |
| `_metrics` import | import | 22 | External function from tools.train_m1_pilot |
| `pair_rows` import | import | 22 | External function from tools.train_m1_pilot |
| `GUARD_FLOOR` import | import | 24 | External constant from tools.train_m1_hybrid |
| `HEURISTIC_SCALE` import | import | 24 | External constant from tools.train_m1_hybrid |
| `RIDEG_ALPHA` import | import | 24 | External constant from tools.train_m1_hybrid |
| `VARIANT` import | import | 24 | External constant from tools.train_m1_hybrid |
| `_fit` import | import | 24 | External function from tools.train_m1_hybrid |
| `_hybrid` import | import | 24 | External function from tools.train_m1_hybrid |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (1 entry point function) |
| Excluded top-level definitions | 32 (docstrings + module constants + private functions + stdlib imports + external imports) |
| Module-level constants inventoried | 4 (DEFAULT_TRAIN, DEFAULT_AUDIT, DEFAULT_OUTPUT, M1_CONFIGS) |
| Source lines covered | 1–220 |
| Discovery matching lines | 11 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/evaluate_ml_audit.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b10.
