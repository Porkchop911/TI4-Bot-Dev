# M00-004b10d — tools/evaluate_rolling_strategy.py Entry Point Inventory

## Scope
`tools/evaluate_rolling_strategy.py` (85 lines). Deployed strategy-card artifact evaluation on rolling on-policy panel.

## Discovery commands and counts
```
git -C D:\Projects\ti4-engine ls-files tools/evaluate_rolling_strategy.py
  → 1 file
wc -l D:\Projects\ti4-engine\tools\evaluate_rolling_strategy.py
  → 85
rg -n 'def |class |if __name__' D:\Projects\ti4-engine\tools\evaluate_rolling_strategy.py
  → 4 matching lines (1 module constant str + 1 public function + 3 module constants)
```

## P0/P1 scope
- **P0**: read-only access to `D:\Projects\ti4-engine\tools\evaluate_rolling_strategy.py`
- **P1**: write-only to this evidence file

---

## Entry points

### 1. `main(argv)` — CLI entry point
| Field | Value |
|---|---|
| Symbol | `main(argv: list[str] | None) -> int` |
| Kind | Module-level function |
| Source line | 28 |
| Construction role | Parses args via `argparse.ArgumentParser`, loads dataset JSON + LinearChoiceModel, validates continuation model_id/faction consistency, calls `pair_rows()` for dataset, validates kind_pair == "strategy_card <> strategy_card", validates captured_players == continuation faction, computes prediction from model weights × features, builds report with `_metrics()`, `_label_split_half()`, `_cluster_bootstrap()`, writes atomic JSON to `--output`, prints report JSON. Returns 0 or parser.error(). |

### 2. `if __name__ == "__main__"` — Script entry
| Field | Value |
|---|---|
| Symbol | `__main__` guard |
| Kind | Script invocation |
| Source line | 85 |
| Construction role | Invokes `main()` via `raise SystemExit(main())`. |

## CLI flags and defaults
| Flag | Type | Default | Description |
|---|---|---|---|
| `--dataset` | Path | `DEFAULT_DATASET` | On-policy panel JSON |
| `--model` | Path | `DEFAULT_MODEL` | Deployed model config JSON |
| `--output` | Path | `DEFAULT_OUTPUT` | Output report path |

## Module-level constants
| Symbol | Value | Line |
|---|---|---|
| `DEFAULT_DATASET` | `ROOT/out/ml_l1_strategy_onpolicy_120x16.json` | 22 |
| `DEFAULT_MODEL` | `ROOT/configs/ml/strategy_card_m1_v1.json` | 23 |
| `DEFAULT_OUTPUT` | `ROOT/out/ml_l1_strategy_onpolicy_report.json` | 24 |

## Exclusions table (top-level only)
| Symbol | Kind | Source line | Exclusion reason |
|---|---|---|---|
| Module docstring | docstring | 1–7 | Documentation |
| `ROOT` | module constant | 15 | Module-level path constant (sys.path manipulation) |
| `__future__` import | import | 3 | Future annotations import |
| `argparse` import | import | 5 | Stdlib import |
| `json` import | import | 6 | Stdlib import |
| `sys` import | import | 7 | Stdlib import |
| `Path` import | import | 8 | Stdlib import |
| `numpy` import | import | 10 | External dependency |
| `LinearChoiceModel` import | import | 13 | External class from engine.ml.linear |
| `_cluster_bootstrap` import | import | 14 | External function from tools.evaluate_ml_audit |
| `_label_split_half` import | import | 14 | External function from tools.evaluate_ml_audit |
| `write_atomic_json` import | import | 15 | External function from tools.ml_manifest |
| `_metrics` import | import | 16 | External function from tools.train_m1_pilot |
| `pair_rows` import | import | 16 | External function from tools.train_m1_pilot |

## Summary
| Category | Count |
|---|---|
| Public construction APIs inventoried | 1 (1 entry point function) |
| Excluded top-level definitions | 15 (docstrings + module constants + stdlib imports + external imports) |
| Module-level constants inventoried | 3 (DEFAULT_DATASET, DEFAULT_MODEL, DEFAULT_OUTPUT) |
| Source lines covered | 1–85 |
| Discovery matching lines | 4 |
| Unreconciled definitions | 0 |

## Status
- **Slice complete**: tools/evaluate_rolling_strategy.py entry point inventory done.
- **Not complete** for M00-004b.
- **Not complete** for M00-004b10.
