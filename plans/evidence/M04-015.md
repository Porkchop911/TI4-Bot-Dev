# M04-015 — Differential phase suite — BLOCKED

## Split record

The original phase-level differential suite is larger than an atomic package and the old blocker
predates the now-executable exporter. It is split without reducing its acceptance criterion:

| Child | Scope | Status |
|---|---|---|
| M04-015a | Native canonical public-state projection matching the executable oracle schema. | Complete |
| M04-015b | Versioned bounded source-state intake boundary. | Complete |
| M04-015c1 | Deterministic comparison of a native projection with a checked source public-state snapshot. | Complete |
| M04-015c2a | Public-only initial-state reconstruction, with exact projection equality against every retained source snapshot. | Complete |
| M04-015c2b1 | Checked native-driver consumption of the retained common decision prefix and explicit refusal at its first unsupported source action. | Complete |
| M04-015c2b2 | Native executable scenario construction, checked full scripted decisions/entropy, event projection, and selected cross-engine phase comparisons. | Blocked on missing action handlers, map metadata, entropy consumption, and event compatibility |

The parent remains incomplete until the cross-engine comparisons in M04-015c2b2 pass. A native
projection or public-only snapshot reconstruction alone is a prerequisite, not parity evidence.

## Historical package specification

- **Milestone/package:** M04-015, Differential phase suite; dependencies M04-004 through M04-012
  are implemented structurally, but the required M00 differential infrastructure is absent.
- **Oracle:** `D:\Projects\ti4-engine`, branch `codex/fully-learned-policy`, commit
  `37061c511a4780d4c0719e0342533a498cd4b457`, verified clean during the package check.
- **Source/tests inspected read-only:** `engine/game.py`, `engine/agenda.py`,
  `tests/test_game.py`, and `tests/test_three_player_strategy.py`.
- **Permission class:** P0 oracle read / P1 evidence and execution-state write. No network,
  external state, or oracle writes occurred.

## Blocking condition

M04-015 requires canonical state, choice, event, outcome, and error projections from the Python
oracle for selected fixtures. The required M00-009 oracle exporter does not exist, and there is no
selected/reproducible differential fixture corpus. This is confirmed by
`plans/EXECUTION_STATE.md`'s open blocker: M00-009 was documented but never built, and M04-015 is
explicitly listed as unimplementable until it exists.

Writing Rust-only assertions from source inspection would not satisfy the package's required
cross-engine comparison, event payload/UID ordering, or field-level checkpoint evidence. No test,
fixture, or production code was added, and no parity claim is made.

## Required next action

Implement and validate M00-009's versioned, byte-stable, read-only oracle projection exporter and
the M00-008 fixture selection it consumes. Then resume this package from the required reading order
and compare exported projections with Rust's canonical projections.
