# M03-016b — Integrate the timing chain with the content branch

## Active package specification

| Field | Value |
|---|---|
| Depends | Completed M03-008 through M03-016a and `wp/m06-003-structured-transactions`. M03-007c remains blocked; this is the owner-authorized integration response to blocker item 1, not an M03 exit claim. |
| Objective | Produce one local integration branch containing both the M03 timing chain and the content branch, resolving only the genuine overlap while preserving both behaviors. |
| Oracle | No new behavior. Preserve the verified `engine/timing.py` and content-source contracts carried by the parents. |
| Writable paths | Merge-resolved tracked files, this evidence file, `plans/EXECUTION_STATE.md`, and `plans/HANDOVER_2026-08-12.md`. |
| Permission | P0 Git/oracle inspection; P1 local branch, merge, tests, plans, and focused commit. No remote mutation, network, external services, or oracle writes. |
| Compatibility | Integration only. No changed rule semantics or parity claim is accepted without a focused source-derived package. |
| Non-goals | Adding more typed event vocabulary, resolving M03-007c, rebasing/merging another agent's branch in place, or changing content behavior during conflict resolution. |
| Tests | Workspace suite, strict linting, formatting, diff validation, and oracle integrity guard; preserve M03 timing and M06 content tests. |
| Review | Tier C because timing changes are carried through the integration. External/Pi review remains owner-waived; self-review will enumerate conflicts and their resolution. |

## Expected conflicts

`game.rs` and `wiring.rs` changed on both branches after their common base; both implementations
must remain. `plans/EXECUTION_STATE.md` is a durable ledger conflict and must retain the newest
truth rather than one branch's stale branch/next-action fields.

## Integration result

- Merging `wp/m06-003-structured-transactions` into the M03-016a branch auto-merged all source
  files, including both-sided changes to `game.rs` and `wiring.rs`. The merged driver contains both
  the M06 content windows and the M03 stateful resolver ownership, strategy timing boundary, and
  `emit_with_context` wiring guard.
- `plans/EXECUTION_STATE.md` was the only conflict. Its two historical branch checkpoints are
  explicitly labelled superseded and a new M03-016b checkpoint states the merged branch's live
  status. No historic acceptance evidence was discarded.
- `plans/CODEX_BLOCKERS_2026-08-12.md` arrives from the content branch, so the complaint and its
  source measurements now travel with the timing integration rather than being stranded in a
  separate checkout.

## Verification

All final checks passed on 2026-08-12:

```text
cargo fmt --all --check
cargo test -p ti4-engine --lib                                    # 571 passed
cargo test --workspace                                             # 571 engine, 20 legacy, 72 model, 1 doc-test; 0 failed
cargo clippy --workspace --all-targets -- -D warnings
git diff --check
PYTHONDONTWRITEBYTECODE=1 python tools/oracle_integrity_guard.py  # 238 files verified
```

## Review

Self-review compared the auto-merged `game.rs` and `wiring.rs` call sites against both parents,
checked that no conflict markers remain, and verified the full combined test/lint suite. The
content branch's behavior was not rewritten during the merge. External Tier C review remains
owner-waived and is not claimed as independent review.

## Status

**Complete; committed as `Integrate timing chain with content work`.**
