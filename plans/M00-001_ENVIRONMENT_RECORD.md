# M00-001 — Environment record

## ID and title
M00-001 — Environment record

## Milestone and dependencies
- Milestone: M00 — Oracle and baseline
- Dependencies: none (first package in M00)

## One-sentence objective
Record the Windows host, CPU, memory, Rust toolchain, Python oracle version, installed dependencies, and reproducible commands so that all future benchmarks and parity tests can be regenerated on this or an equivalent machine.

## Exact Python source references
- Read-only: `D:\Projects\ti4-engine` (oracle repository)
- Branch: `codex/fully-learned-policy`
- Commit: `37061c511a4780d4c0719e0342533a498cd4b457`

## Exact Python test references
- Read-only: `D:\Projects\ti4-engine\tests\` (12 MB, part of 429 tracked files)

## Allowed Rust edit paths
- `plans/M00-001_ENVIRONMENT_RECORD.md` (this file)
- `plans/evidence/M00-001.md` (evidence file)
- `plans/EXECUTION_STATE.md` (execution state update)

## Permission class and scoped access declaration
Permission class: **P1** — Allowed inside the Rust repository.
- Writable paths: `D:\Projects\ti4-engine-rs\plans\`, `D:\Projects\ti4-engine-rs\plans\evidence\`
- Read-only external paths: `D:\Projects\ti4-engine` (oracle, must remain clean)
- Network access: none required for this package
- Processes/ports: none
- Expected generated artifacts: environment manifest JSON/markdown (~1 KB)
- Destructive actions: none
- External-state changes: none

## Inputs and outputs
**Inputs:**
- Host system information (OS, CPU, memory)
- Rust toolchain version and configuration
- Python interpreter version and full dependency list from oracle
- Oracle repository commit hash and clean-tree status

**Outputs:**
- `plans/evidence/M00-001.md` — reproducible environment manifest with all measurements
- Updated `plans/EXECUTION_STATE.md` — next package pointer

## Invariants and compatibility class
- **Invariant:** The oracle tree at `37061c5` must remain clean (no uncommitted changes).
- **Compatibility class:** exact — the recorded values are machine-specific baselines, not behavioral contracts. Future runs on different hardware will produce different numbers but must follow the same measurement protocol.

## Explicit non-goals
- Not a performance benchmark run (that is M00-012/M00-013).
- Not a test suite execution (that is M00-011).
- Not modifying or staging any files in the Python oracle repository.
- Not installing new Rust crates or creating workspace structure (M01).

## Tests to add
No code tests — this package produces an environment record, not implementation code. Verification:
1. `git status` on oracle shows clean tree at correct commit.
2. Environment manifest contains all required fields and can be regenerated by re-running the same commands.
3. Rust toolchain is available and compiles a trivial program.

## Commands to run
```bash
# Oracle verification
cd D:\Projects\ti4-engine && git status && git rev-parse HEAD

# Python environment
python --version
pip list

# Rust toolchain
rustc --version
cargo --version
rustup show

# System info (MSYS2)
uname -a
cat /proc/cpuinfo | head -20  # or equivalent
cat /proc/meminfo | head -5   # or equivalent

# Oracle scope
cd D:\Projects\ti4-engine && git ls-files | wc -l
du -sh engine/ bridge/ tools/ tests/
```

## Expected evidence
`plans/evidence/M00-001.md` containing:
- Host OS and kernel version
- CPU model, core count, logical thread count
- Total and available memory
- Rust toolchain (rustc, cargo, rustup versions)
- Python interpreter path and version
- Full pip dependency list with versions
- Oracle commit hash and branch name
- Oracle tree size (tracked files, directory sizes)
- Confirmation that oracle tree is clean

## Known traps
- MSYS2 bash on Windows may report different memory/CPU info than native PowerShell — record the tool used.
- `pip list` output can vary if editable installs are present — capture full output including editable locations.
- Do not run any command that writes to the oracle repository (no `pip install`, no cache generation, no `__pycache__` creation).

## Definition of done
- [x] Oracle commit verified at `37061c5` with clean working tree ✅
- [x] Rust toolchain confirmed functional (rustc + cargo versions recorded) ✅
- [x] Python environment fully enumerated (version + all pip packages — full list in evidence) ✅
- [x] System hardware info captured (CPU, memory, OS) ✅
- [x] Oracle scope quantified (file counts: **296 tracked .py**, not ~15K on-disk) ✅
- [x] Evidence file `plans/evidence/M00-001.md` written with all data (CORRECTED version) ✅
- [ ] `plans/EXECUTION_STATE.md` updated to reflect completion and next package
- [x] Independent frontier review completed
> **NOTE:** Corrected evidence passed independent frontier review after the package-count and Windows product-label fixes. Only the EXECUTION_STATE update remains before formal completion; review: `plans/reviews/M00-001_FRONTIER_REVIEW.md`.
