# Handover — 2026-08-12

Objective:
Continue the Rust migration autonomously. The immediate corrective package resolves the recorded
timing complaint without claiming the still-blocked M03 replay exit is complete.

Oracle commit:
`37061c511a4780d4c0719e0342533a498cd4b457` on `codex/fully-learned-policy`; integrity guard
passed before and after inspection with 238 files verified.

Active milestone/package:
M03-016a — stateful timing context and strategy driver boundary. It is an owner-authorized child
work package because M03-007c still blocks the actual M03-016 critical review.

Status and completed acceptance criteria:

- `Game` owns `Resolver` and `EventSequence` and provides the same state, content, source scope,
  table, dice, and RNG to timing abilities through `TimingContext`.
- Stateful timing effects cannot run through detached `Resolver::emit`; that failure is explicit.
- `STRATEGY_CARD_CHOSEN` is a typed Game-driven event with the oracle's player/card/goods facts.
  WHEN cancellation is atomic, and the first mandatory stateful effect mutates the live game.
- Nested stateful emissions share one event sequence; resolver lifecycle counters synchronize from
  `GameState`; `wiring.rs` guards the driver call.
- This does **not** wire combat, invasion, production, technology, cards, or every string event.
  Those need individually settled vocabulary and named source-rule packages (`M03-016b` or later).

Current branch and HEAD:
`wp/m03-016a-stateful-timing`, at focused M03-016a commit `Wire stateful timing through strategy
selection`.

Working-tree state:
Clean after the M03-016a package commit. No oracle changes.

Tests last run and exact results:

- `cargo test -p ti4-engine --lib` — 484 passed, 0 failed.
- `cargo test --workspace` — 484 engine, 20 legacy, 72 model, and 1 doc-test passed; 0 failed.
- `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and
  `git diff --check` — passed.
- `python tools/oracle_integrity_guard.py` — 238 files verified before and after inspection.

Compatibility evidence:
`plans/evidence/M03-016a.md` records the source references, exact scope split, factual payload,
cancellation, context, nested-ID, lifecycle, and driver-wiring checks. It makes no full timing or
replay parity claim.

Decisions made and rationale:

- A distinct `emit_with_context` preserves existing event-only resolver callers while making use of
  game state explicit and unskippable for rules that need it.
- The resolver's internal table remains only for old event-only calls; contextual resolution uses
  the game-owned table so decisions are collected/replayed from one source.
- `GameState` owns the actual turn/round lifecycle; copying it into the resolver before emission
  preserves frequency scope semantics without attempting to infer phase transitions.

Open review findings or blockers:

- M03-007c/M04-015c2b2 remain blocked by source-equivalent executable scenarios, entropy,
  event projection, and legal option surfaces.
- The external Tier C review and original M03-016 gate are owner-waived/blocked respectively; only
  self-review occurred for this correction.
- The remaining typed driver emissions need settled vocabulary. Do not bulk-wrap existing string
  diagnostics and call that timing integration.

Next exact action/command:
`git status --short --branch`, then inspect the next dependency-ready package against its named
oracle source before creating a new package branch.

Files to read first after compaction:

1. `AGENTS.md`
2. `plans/SCOPED_PERMISSIONS.md`
3. `plans/EXECUTION_STATE.md`
4. `plans/MASTER_PLAN.md`
5. `plans/PI_WORK_PACKAGE_STANDARD.md`
6. `plans/M03_CHOICE_TIMING_REPLAY.md`
7. `plans/evidence/M03-016a.md`, `M03-015.md`, and `M03-014.md`
8. `plans/CODEX_BLOCKERS_2026-08-12.md` in the primary checkout
