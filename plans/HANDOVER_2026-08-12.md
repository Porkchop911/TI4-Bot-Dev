# Handover — 2026-08-12

Objective:
Continue the Rust migration autonomously, ignoring Pi. The immediate package is M02-004 system
state; M04 replay compatibility remains explicitly blocked rather than simulated.

Oracle commit:
`37061c511a4780d4c0719e0342533a498cd4b457` on `codex/fully-learned-policy`.
`python tools/oracle_integrity_guard.py` last passed with **238 files verified**.

Active milestone/package:
M02 — Content and model / M02-004 system state. M02-001, 002, and 003 are complete. M02-004's
existing implementation already has the source `SystemState` fields and helpers; inspect it against
the source before deciding whether an atomic gap remains. Do not trust the old documentation-only
completion claims without tests/evidence.

Status and completed acceptance criteria:

- `6d6e8e1` M04-015c2a imports all 100 checked initial public snapshots and requires exact native
  public projection equality. It is deliberately non-executable.
- `83df0b2` M04-015c2b1 feeds retained decisions through production `Scripted`; it accepts six
  strategy-card selections and refuses the first unsupported source action
  `component|expedition|secret`.
- `b063dcb` M02-002 adds strict runtime `SchemaEnvelope` validation: supported version only,
  lowercase 40/64-hex provenance, nonblank RNG/compatibility metadata, stable serde wire values,
  and unknown-field rejection.

Current branch and HEAD:
`wp/m02-004-system-state`, based on `b063dcb Add versioned schema envelope`.

Working-tree state:
Clean when this handover was written.

Tests last run and exact results:

- `cargo test --workspace` — 121 `ti4-content`, 478 `ti4-engine`, 20 `ti4-legacy`, 72
  `ti4-model`, and 1 doc-test passed; 0 failed.
- `cargo clippy --workspace --all-targets -- -D warnings` — passed.
- `cargo fmt --all --check` and `git diff --check` — passed.
- Oracle integrity guard — 238 files verified.

Compatibility evidence:

- `plans/evidence/M04-015c2a.md` and `M04-015c2b1.md` state precisely what was proven and what
  was refused. Neither claims executable replay or cross-engine parity.
- `plans/evidence/M02-002.md` records the correction of its prior documentation-only status.

Decisions made and rationale:

- Public snapshots retain no hidden deck/card/map metadata. Opaque private-card placeholders only
  preserve public counts; they must never be used for gameplay.
- Scripted replay uses generated legal options. Unsupported source IDs must fail, never be skipped
  or substituted.
- M04-015c2b2 is blocked by absent component actions, Save 52/54 map metadata, injected source
  entropy, and source-equivalent typed driver events. Do not add permissive adapters.
- Independent external/Pi review remains owner-waived; record self-review honestly.

Open review findings or blockers:

- M03-007c / M04-015c2b2 remain blocked as above.
- The pinned oracle no longer contains the historical exporter paths referenced by older evidence;
  consume only the checksummed retained corpus unless a new approved exporter exists.

Next exact action/command:
`cargo test -p ti4-model state --lib`, then compare `crates/ti4-model/src/state.rs::SystemState`
with `D:\Projects\ti4-engine\engine\state.py::SystemState`; only implement a named behavioral
gap, and update M02-004 evidence/state before committing.

Files to read first after compaction:

1. `AGENTS.md`
2. `plans/SCOPED_PERMISSIONS.md`
3. `plans/EXECUTION_STATE.md`
4. `plans/MASTER_PLAN.md`
5. `plans/PI_WORK_PACKAGE_STANDARD.md`
6. `plans/M02_CONTENT_AND_MODEL.md`
7. `plans/evidence/M02-004.md`, `plans/evidence/M02-002.md`, and `plans/evidence/M04-015c2b1.md`
8. `crates/ti4-model/src/state.rs` and `D:\Projects\ti4-engine\engine\state.py`
