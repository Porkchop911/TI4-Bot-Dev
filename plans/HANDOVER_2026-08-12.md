# Handover — 2026-08-12

Objective:
Continue the Rust migration from the combined content/timing branch. The original timing complaint
is resolved through stateful context, a real Game boundary, a wiring guard, and branch integration;
the remaining event vocabulary is intentionally not overstated as complete.

Oracle commit:
`37061c511a4780d4c0719e0342533a498cd4b457` on `codex/fully-learned-policy`; the integrity guard
last passed with 238 files verified.

Active milestone/package:
M03-016b — integration of M03 timing with `wp/m06-003-structured-transactions`. M03-007c remains
blocked, so the M03 milestone exit and its critical review remain incomplete.

Status and completed acceptance criteria:

- `Game` owns the timing resolver and typed event sequence. Stateful effects receive live state,
  content/source scope, table, dice, RNG, and allocator only through `TimingContext`.
- `STRATEGY_CARD_CHOSEN` is a Game-driven typed event with player/card/goods payload; cancellation
  is atomic, table usage is shared, nested IDs are shared, and timing lifecycle counters track state.
- `wiring.rs` fails if the Game driver loses its contextual timing call.
- The M03 chain and M06 content branch now compile and test together. The only merge conflict was
  the execution-state ledger; the source auto-merged and both parent behaviors remain.
- Remaining typed emissions (combat, invasion, production, technology, cards) require separately
  settled vocabulary/source packages. Existing string diagnostics are not a safe replacement.

Current branch and HEAD:
`wp/m03-m06-timing-integration`, at merge commit `Integrate timing chain with content work`.

Working-tree state:
Clean after the M03/M06 integration merge. No oracle changes.

Tests last run and exact results:

- `cargo test -p ti4-engine --lib` — 571 passed, 0 failed.
- `cargo test --workspace` — 571 engine, 20 legacy, 72 model, and 1 doc-test passed; 0 failed.
- `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and
  `git diff --check` — passed.
- `python tools/oracle_integrity_guard.py` — 238 files verified.

Compatibility evidence:
`plans/evidence/M03-016a.md` records the stateful timing contract. `M03-016b.md` records the
integration and full combined checks; neither claims complete source-game event or replay parity.

Decisions made and rationale:

- The content branch was merged into a new local integration branch, not into its active primary
  checkout. This preserves other worktree ownership while providing a tested landing target.
- The only conflict was documentation. Both historical checkpoints remain marked superseded rather
  than being silently erased; the current M03-016b checkpoint is authoritative.

Open review findings or blockers:

- M03-007c/M04-015c2b2 remain blocked by executable source scenarios, source entropy, legal option
  surfaces, and source-equivalent event projection.
- External Tier C review is owner-waived. The original M03-016 gate still cannot close before 007c.
- M00-013 performance baseline and state-level source scope remain independent open work.

Next exact action/command:
`git status --short --branch`, then inspect the next dependency-ready package against its named
oracle source before creating a new package branch.

Files to read first after compaction:

1. `AGENTS.md`
2. `plans/SCOPED_PERMISSIONS.md`
3. `plans/EXECUTION_STATE.md`
4. `plans/MASTER_PLAN.md`
5. `plans/PI_WORK_PACKAGE_STANDARD.md`
6. `plans/M03_CHOICE_TIMING_REPLAY.md`
7. `plans/evidence/M03-016a.md`, `M03-016b.md`, and `M03-015.md`
8. `plans/CODEX_BLOCKERS_2026-08-12.md`
